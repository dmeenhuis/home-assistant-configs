blueprint:
  name: Cover Control Automation (CCA)
  description: "# ⭐ Cover Control Automation (CCA) ⭐\nCover Control Automation is
    a comprehensive Home Assistant blueprint which automatically manages your window
    coverings (such as roller shutters and blinds) based on time, the position of
    the sun, and weather conditions. It adapts intelligently to your preferences,
    largely eliminating the need for manual adjustments.\n\n**Version**: 2026.01.26\n\n###
    \U0001F517 Resources & Support\n\U0001F4E2 **Latest Changes**: [Changelog](https://github.com/hvorragend/ha-blueprints/blob/main/blueprints/automation/CHANGELOG.md)
    | ✨ **Highlights**: [Key Features](https://github.com/hvorragend/ha-blueprints/blob/main/blueprints/automation/README.md#-key-features)\n\U0001F680
    **Setup**: [Quick Start Guide](https://github.com/hvorragend/ha-blueprints/blob/main/blueprints/automation/README.md#-quick-start-5-steps)
    | [Prerequisites](https://github.com/hvorragend/ha-blueprints/blob/main/blueprints/automation/README.md#-essential-prerequisites)\n\U0001F4DA
    **Docs**: ❓ [FAQ](https://github.com/hvorragend/ha-blueprints/blob/main/blueprints/automation/FAQ.md)
    | ☀️ [Dynamic Sun Elevation](https://github.com/hvorragend/ha-blueprints/blob/main/blueprints/automation/DYNAMIC_SUN_ELEVATION.md)
    | ⏰ [Time Control Visualization](https://github.com/hvorragend/ha-blueprints/blob/main/blueprints/automation/TIME_CONTROL_VISUALIZATION.md)\n\U0001F527
    **Validator**: Validate YAML for errors, typos, and deprecated parameters with
    the [CCA Configuration Validator](https://hvorragend.github.io/ha-blueprints/validator/)\n✨
    **Online Tools**:  \U0001F50D [CCA Trace Analyzer](https://hvorragend.github.io/ha-blueprints/trace-analyzer/)
    | \U0001F4C8 [CCA Trace Compare](https://hvorragend.github.io/ha-blueprints/trace-compare/)\n\U0001F4AC
    **Get Help**: [Forum Thread](https://community.home-assistant.io/t/cover-control-automation-cca-a-comprehensive-and-highly-configurable-roller-blind-blueprint/680539)
    | [GitHub Issues](https://github.com/hvorragend/ha-blueprints/issues) | [Source
    Code](https://github.com/hvorragend/ha-blueprints/blob/main/blueprints/automation/cover_control_automation.yaml)\n❤️
    **Enjoying my work?** [Buy me a Coffee](https://buymeacoffee.com/herr.vorragend)
    or tip via [PayPal](https://www.paypal.com/donate/?hosted_button_id=NQE5MFJXAA8BQ).
    Thank you! \U0001F64C\n<sub>License: Open-source. Attribution and original repo
    link required for modifications. \"Take Control\" copies are unsupported.</sub>\n"
  source_url: https://github.com/hvorragend/ha-blueprints/blob/main/blueprints/automation/cover_control_automation.yaml
  domain: automation
  homeassistant:
    min_version: 2024.10.0
  input:
    blind:
      name: "\U0001F3E0 Cover"
      description: 'Which blind or roller shutter should be automated?

        <details> <summary><code><strong>CLICK HERE:</strong> Information about a
        cover group</code></summary>


        In principle, you can use a group here. But please note that there are problems
        with position detection for a group of covers! For example, one cover may
        be at position 100% and the other cover at position 0%. This results in a
        wrong group-value of 50%.


        My clear recommendation is to create **one automation for each cover**.

        </details>'
      selector:
        entity:
          filter:
          - domain:
            - cover
          reorder: false
          multiple: false
    feature_section:
      name: Automation Options
      icon: mdi:window-shutter-cog
      collapsed: true
      input:
        auto_options:
          name: "\U0001F449 What should CCA control?"
          description: "**Basic Controls (When to move):**\nOptions 1-4 control WHEN
            your cover opens/closes based on time, brightness or sun elevation\n\n**Advanced
            Features (How to react):**\nOptions 5-6 add special behaviors like ventilation
            and sun protection\n\n**⚠️ Common Misunderstanding:**\n- **Sun Elevation**
            (☀️) = Controls WHEN cover opens/closes based on sunrise/sunset\n- **Sun
            Protection** (\U0001F975) = Controls HOW MUCH cover closes when sun hits
            the window\nThese are two completely different features!\n\n<details>
            <summary><code><strong>\U0001F4D6 CLICK HERE:</strong> Detailed explanation
            with example</code></summary>\n\n- **Sun Elevation Control** = Opens cover
            when sun rises, closes when sun sets (like a sunrise alarm)\n- **Sun Protection/Shading**
            = Partially closes cover during midday to block heat (like sunglasses)\n\nThink
            of it this way:\n- Sun Elevation = \"Follow the sun's daily cycle\"\n-
            Sun Protection = \"React to direct sunlight hitting the window\"\n\n**Example
            of a summer day:**\n- **6:00** — Sun Elevation triggers opening (sun rose
            above your threshold)\n- **7:00** — Time-based opening would have happened
            (but cover is already open)\n- **12:00** — Sun Protection activates (sun
            now shines directly on window → partial close)\n- **18:00** — Sun Protection
            deactivates (sun moved away → cover opens again)\n- **21:00** — Sun Elevation
            triggers closing (sun dropped below your threshold)\n</details>\n\n<details>
            <summary><code><strong>ℹ️ CLICK HERE:</strong> Important notes on configuration</code></summary>\n\nConfiguring
            the Cover Status Helper is <ins>mandatory</ins> for utilizing ventilation
            mode or for sun protection and sunshade control.\n\nThis can be enhanced
            with additional conditions (see below). However, it's crucial that options
            remain activated; otherwise, the specified conditions will not function.\n\nPlease
            ensure that the relevant sensors are also included. For instance, the
            brightness control will only operate if a brightness sensor is specified.\n</details>\n\n<details>
            <summary><code><strong>⚙️ CLICK HERE:</strong> Notes on multiple triggering</code></summary>\n\nEven
            if multiple opening, closing, shading, etc. is activated, this only works
            if a trigger is available. However, the numeric state triggers only trigger
            under certain circumstances. Please see notes here [Numeric state triggers](https://www.home-assistant.io/docs/automation/trigger/#numeric-state-trigger)\n\n<em>Crossing
            the threshold means that the trigger only fires if the state wasn't previously
            within the threshold. If the current state of your entity is `50` and
            you set the threshold to `below: 75`, the trigger would not fire if the
            state changed to e.g. `49` or `72` because the threshold was never crossed.
            The state would first have to change to e.g. `76` and then to e.g. `74`
            for the trigger to fire.</em>\n</details>"
          default:
          - auto_up_enabled
          - auto_down_enabled
          - auto_sun_enabled
          selector:
            select:
              options:
              - label: "\U0001F53C Morning Opening — Automatically open based on configured
                  time"
                value: auto_up_enabled
              - label: "\U0001F53B Evening Closing — Automatically close based on
                  configured time"
                value: auto_down_enabled
              - label: "\U0001F505 Add Brightness Condition — Open/close earlier when
                  brightness sensor reaches threshold"
                value: auto_brightness_enabled
              - label: ☀️ Add Sun Elevation Condition — Open/close earlier based on
                  sun height (not sun protection!)
                value: auto_sun_enabled
              - label: "\U0001F4A8 Ventilation Mode — React to open/tilted windows,
                  prevent lockout (requires helper)"
                value: auto_ventilate_enabled
              - label: "\U0001F975 Sun Protection / Shading — Partially close when
                  sun shines on window (requires helper)"
                value: auto_shading_enabled
              multiple: true
              sort: false
              custom_value: false
              mode: list
        individual_config:
          name: ⚙️ Behavior Customization
          description: '**Fine-tune automation behavior for your specific hardware
            and preferences.**


            This section lets you customize how CCA behaves in different scenarios:

            - **Position Management**: Control cover movements between states (e.g.,
            avoid raising when closing for the evening)

            - **Feature Control**: Disable automatic transitions (e.g., stay shaded
            after sun disappears, don''t open after ventilation ends)

            - **Daily Frequency Limits**: Prevent repeated actions (e.g., open only
            once per day)

            - **Hardware Compatibility**: Work around device-specific quirks with
            position/tilt commands


            <details> <summary><code><strong>CLICK HERE:</strong> Hardware-Specific
            Details</code></summary>


            <ins>Why use custom actions instead of default services?</ins><br />

            Some devices (e.g., Shelly, Homematic) have issues when ''set_cover_position''
            and ''set_cover_tilt_position'' are executed sequentially.


            **Solutions:**

            - Shelly: Use script [cover_position_tilt.yaml](https://gist.github.com/lukasvice/b364724d84c3ac4e160f7a7d8fa37066)

            - Homematic: Use custom service [homematicip_local.set_cover_combined_position](https://github.com/SukramJ/custom_homematic?tab=readme-ov-file#homematicip_localset_cover_combined_position)

            - Other devices: Implement via "Additional Actions" in the Service Calls
            section

            </details>'
          default: []
          selector:
            select:
              options:
              - label: ↕️ Don't raise cover when closing for the evening
                value: prevent_higher_position_closing
              - label: ⬇️ Keep shading position during evening close (don't lower
                  further)
                value: prevent_lowering_when_closing_if_shaded
              - label: ☀️ Don't end shading if cover is already closed
                value: prevent_shading_end_if_closed
              - label: '☀️ Stay shaded: Don''t open cover when sun shading ends'
                value: prevent_opening_after_shading_end
              - label: "\U0001FA9F Keep closed: Don't open cover when ventilation
                  ends"
                value: prevent_opening_after_ventilation_end
              - label: "\U0001F527 Use custom actions only (disable default set_cover_position/set_cover_tilt_position)"
                value: prevent_default_cover_actions
              - label: ⏱️ Open cover only once per day
                value: prevent_opening_multiple_times
              - label: ⏱️ Close cover only once per day
                value: prevent_closing_multiple_times
              - label: ⏱️ Shade cover only once per day
                value: prevent_shading_multiple_times
              multiple: true
              sort: false
              custom_value: false
              mode: list
    helper_section:
      name: Cover Status Helper
      icon: mdi:form-textbox
      collapsed: true
      input:
        cover_status_options:
          name: "\U0001F9AE Status detection of the cover"
          description: 'It is essential that the cover has a position detection attribute
            or sensor. Regardless of which option is selected here, the position detection
            is based on this attribute. The <em>current_tilt_position</em> attribute
            is <ins>not</ins> taken into account.


            <strong>Important note:</strong><br /> - I strongly recommend using the
            Cover Status Helper! The full range of functions is only available if
            this is configured.<br /> - The helper is required for shading, ventilation
            and lockout protection!<br />


            <details> <summary><code><strong>CLICK HERE:</strong> Further descriptions</code></summary>


            - <ins>Check the current position</ins><br /> The automation only moves
            the cover if it is positioned at one of the defined positions (ventilate
            position, shading position, open position or closed position). Otherwise,
            the cover has been moved manually. After a manual drive, it is unclear
            what is intended to be achieved, so the automation no longer takes action.
            To automate the drive of the cover again, it must be moved to one of the
            defined positions beforehand. The advantage of this is that you don''t
            have to create a helper in Home Assistant. Only basic features are available
            with this setting.


            - <ins>Use an external Cover Status Helper</ins><br /> If you want to
            be able to do manual overrides and have the automation do the next drive
            as usual, then a helper is necessary. Without a helper, the manual override
            would always be overwritten by the automation. This has the advantage
            that the cover does not necessarily have to be in a defined position.
            With this choice, advanced functions are possible and you can always rely
            on the automation.


            </details>

            '
          default: cover_helper_disabled
          selector:
            select:
              options:
              - label: '#️⃣ Check the current position'
                value: cover_helper_disabled
              - label: "\U0001F9AE Also use an external Cover Status Helper (better)"
                value: cover_helper_enabled
              multiple: false
              sort: false
              custom_value: false
        cover_status_helper:
          name: "\U0001F9AE Cover Status Helper"
          description: Helper used to store the last cover event. A separate helper
            must be created for each CCA automation. *Attention:* You will need to
            manually create a [input_text](https://my.home-assistant.io/redirect/helpers/)
            entity with a <ins>length of 254 chars</ins> for this.
          default: []
          selector:
            entity:
              domain:
              - input_text
              reorder: false
              multiple: false
        drive_time:
          name: ⏲️ Cover Drive Time
          description: Can be used to recognise manual control. Please round up a
            little and do not adjust too precisely. Is used to delay the trigger if
            too much or incorrect position data is sent back. <br /><br /> Within
            this time, it is assumed that CCA has carried out the last action. Otherwise,
            CCA would react to its own commands and recognize them as manual intervention.
          default: 90
          selector:
            number:
              min: 0.0
              max: 180.0
              unit_of_measurement: seconds
              step: 1.0
              mode: slider
    position_section:
      name: Cover Position Settings
      icon: mdi:window-shutter-settings
      description: '<center> <br /> <strong>Important notes on configuring the position
        values</strong> <br /> <strong>Position Logic:</strong><br /> - For Blinds/Shutters:
        open_position (100%) > shading_position (25%) > close_position (0%)<br />
        - For Awnings: open_position (0%) < shading_position (75%) < close_position
        (100%)<br /> Please ensure that all position values are unique and do not
        conflict with each other. <br /><br /> </center>'
      collapsed: true
      input:
        position_source:
          name: "\U0001F4CD Position Source Type"
          description: "How does your cover provide position information? <details>
            <summary><code><strong>CLICK HERE: When to use each option</strong></code></summary>\n\n
            \ - **current_position attribute** (Standard): Most covers use this. If
            CCA detects positions correctly, keep this setting.\n  - **position attribute**:
            Select this if your cover entity has a 'position' attribute but no 'current_position'
            attribute. Check in Developer Tools → States if unsure.\n  - **Custom
            sensor**:   Use this if: Your cover doesn't report positions at all /
            You have an external sensor tracking the cover position / Manual position
            changes aren't detected with the other options\n\n</details>"
          default: current_position_attr
          selector:
            select:
              options:
              - label: Use current_position attribute (standard)
                value: current_position_attr
              - label: Use position attribute
                value: position_attr
              - label: Use custom position sensor
                value: custom_sensor
              mode: dropdown
              multiple: false
              sort: false
              custom_value: false
        custom_position_sensor:
          name: "\U0001F4CD Custom Position Sensor"
          description: Select sensor that provides position (0-100%). Only used if
            'Custom sensor' selected above
          default: []
          selector:
            entity:
              domain:
              - sensor
              reorder: false
              multiple: false
        cover_type:
          name: "\U0001F3AF Cover Type"
          description: 'Select the type of cover you want to control. <br /><br />
            <strong>Blind/Roller Shutter:</strong> Position 0% = closed (down), 100%
            = open (up). Shading uses lower positions. <br /><br /> <strong>Awning/Sunshade:</strong>
            Position 0% = retracted (closed), 100% = extended (open). Shading uses
            higher positions. <br /><br /> ⚠️ <strong>Note:</strong> Ventilation and
            tilt features are not available for awnings. <br /><br /> <details> <summary><code><strong>CLICK
            HERE:</strong> Position value examples</code></summary>

            <strong>For Blinds/Shutters:</strong><br /> - Open Position: 100% (fully
            up)<br /> - Shading Position: 25% (partially down for sun protection)<br
            /> - Ventilate Position: 30% (slightly down for air flow)<br /> - Close
            Position: 0% (fully down)<br /> <br /> <strong>For Awnings:</strong><br
            /> - Open Position: 0% (retracted/closed)<br /> - Shading Position: 75%
            (extended for sun protection)<br /> - Close Position: 100% (fully extended)<br
            /> <br /> <em>Note: Ventilate Position is not used for awnings.</em>

            </details>'
          default: blind
          selector:
            select:
              options:
              - label: "\U0001FA9F Blind / Roller Shutter (Standard)"
                value: blind
              - label: ☂️ Awning / Sunshade (Inverted)
                value: awning
              mode: dropdown
              multiple: false
              sort: false
              custom_value: false
        open_position:
          name: "\U0001F53C Open Position"
          description: What position should the cover be moved into when opening?
          default: 100
          selector:
            number:
              min: 0.0
              max: 100.0
              unit_of_measurement: '%'
              step: 1.0
              mode: slider
        close_position:
          name: "\U0001F53B Close Position"
          description: What position should the cover be moved into when closing?
          default: 0
          selector:
            number:
              min: 0.0
              max: 100.0
              unit_of_measurement: '%'
              step: 1.0
              mode: slider
        ventilate_position:
          name: "\U0001F4A8 Ventilate Position"
          description: What position should the cover move to when the window is tilted?
            If closing is triggered and the contact sensor is 'on', the cover will
            move to this position instead of closing completely. <br /><br />Should
            not be 100. In this case please use 99. And please also note the information
            in the position tolerance.
          default: 30
          selector:
            number:
              min: 0.0
              max: 100.0
              unit_of_measurement: '%'
              step: 1.0
              mode: slider
        shading_position:
          name: "\U0001F975 Sun Shading Position"
          description: To which position should the cover be moved for shading?
          default: 25
          selector:
            number:
              min: 0.0
              max: 100.0
              unit_of_measurement: '%'
              step: 1.0
              mode: slider
        position_tolerance:
          name: 〰️ Position Tolerance
          description: Tolerance to be applied when comparing the current position
            with the to be position. These are absolute values. Not relative to the
            previous position values.
          default: 0
          selector:
            number:
              min: 0.0
              max: 20.0
              unit_of_measurement: '%'
              step: 1.0
              mode: slider
    tilt_position_section:
      name: Cover Tilt Position Settings
      icon: mdi:image-filter-tilt-shift
      collapsed: true
      input:
        cover_tilt_wait_mode:
          name: "\U0001F504 Tilt Wait Mode"
          description: 'Choose how CCA waits before sending tilt commands:

            **Fixed Delay (Standard)**: Uses the configured tilt delay value. Works
            well for most devices.

            **Wait Until Idle (Z-Wave)**: Waits until cover reports ''open'' or ''closed''
            state before sending tilt. Prevents tilt commands from being ignored on
            devices that block tilt during motor movement (e.g., Shelly Qubino Wave
            Shutter).

            If your tilt positions are unreliable, try switching to ''Wait Until Idle''
            mode.'
          default: fixed_delay
          selector:
            select:
              options:
              - label: ⏱️ Fixed Delay (Standard)
                value: fixed_delay
              - label: ⏸️ Wait Until Idle (Z-Wave devices)
                value: wait_idle
              mode: dropdown
              multiple: false
              sort: false
              custom_value: false
        cover_tilt_wait_timeout:
          name: "\U0001F504 Tilt Wait Timeout"
          description: Maximum time to wait for cover to become idle before sending
            tilt command. Only used when 'Wait Until Idle' mode is selected.
          default: 30
          selector:
            number:
              min: 5.0
              max: 60.0
              unit_of_measurement: seconds
              step: 1.0
              mode: slider
        tilt_delay:
          name: "\U0001F55B Default Tilt Delay"
          description: Delay between <em>set_cover_position</em> and <em>set_cover_tilt_position</em>.
            Only necessary when using the tilt functions. This separates the two commands
            in terms of time. <br /><br />`Optional`
          default: 0
          selector:
            number:
              min: 0.0
              max: 600.0
              unit_of_measurement: seconds
              step: 1.0
              mode: slider
        cover_tilt_config:
          name: "\U0001F4D0 Tilt Position Feature"
          description: ⚠️ <strong>Note:</strong> Tilt control is only available for
            blinds/shutters, not for awnings. <br /><br /> If the cover and the integration
            support it, the tilt position of the cover can be set. The standard attribute
            ‘current_tilt_position’ is used for this.
          default: cover_tilt_disabled
          selector:
            select:
              options:
              - label: ✅ Enable Tilt Position Control
                value: cover_tilt_enabled
              - label: ❌ Disable Tilt Position Control
                value: cover_tilt_disabled
              multiple: false
              sort: false
              custom_value: false
              mode: list
        cover_tilt_reposition_config:
          name: "\U0001F4D0 Tilt Reposition Feature"
          description: If Tilt Reposition Feature is enabled you can choose if the
            blinds are closed before tilting to a new position. In some cases tilting
            the blinds in small steps can lead to false positions. This is because
            of the minimum time the motor needs to run. If the runtime between current
            tilt position and target tilt position is to small the motor will not
            stop at the right position. Thus the blinds will be preclosed to 0 and
            then run to the target position.
          default: cover_tilt_reposition_disabled
          selector:
            select:
              options:
              - label: ✅ Enable Tilt Reposition Control
                value: cover_tilt_reposition_enabled
              - label: ❌ Disable Tilt Reposition Control
                value: cover_tilt_reposition_disabled
              multiple: false
              sort: false
              custom_value: false
              mode: list
        open_tilt_position:
          name: "\U0001F53C Open Tilt Position"
          description: To which tilt position should the cover be moved when opening?
          default: 50
          selector:
            number:
              min: 0.0
              max: 100.0
              unit_of_measurement: '%'
              step: 1.0
              mode: slider
        close_tilt_position:
          name: "\U0001F53B Close Tilt Position"
          description: To which tilt position should the cover be moved when closing?
          default: 50
          selector:
            number:
              min: 0.0
              max: 100.0
              unit_of_measurement: '%'
              step: 1.0
              mode: slider
        ventilate_tilt_position:
          name: "\U0001F4A8 Ventilate Tilt Position"
          description: To which tilt position should the cover be moved for ventilation?
          default: 50
          selector:
            number:
              min: 0.0
              max: 100.0
              unit_of_measurement: '%'
              step: 1.0
              mode: slider
        shading_tilt_position_0:
          name: "\U0001F975 Sun Shading Tilt Position"
          description: Minimum tilt position for shading. The cover will be tilted
            to this position if the sun elevation is below the value of elevation
            1.
          default: 0
          selector:
            number:
              min: 0.0
              max: 100.0
              unit_of_measurement: '%'
              step: 1.0
              mode: slider
        shading_tilt_position_1:
          name: "\U0001F975 Sun Shading Tilt Position 1"
          description: To which tilt position should the cover be moved for shading
            when the sun is above elevation 1?
          default: 20
          selector:
            number:
              min: 0.0
              max: 100.0
              unit_of_measurement: '%'
              step: 1.0
              mode: slider
        shading_tilt_elevation_1:
          name: "\U0001F975 Sun Shading Tilt Elevation 1"
          description: Sun elevation for tilt position 1.
          default: 20
          selector:
            number:
              min: 0.0
              max: 90.0
              unit_of_measurement: °
              mode: slider
              step: 1.0
        shading_tilt_position_2:
          name: "\U0001F975 Sun Shading Tilt Position 2"
          description: To which tilt position should the cover be moved for shading
            when the sun is above elevation 2?
          default: 37
          selector:
            number:
              min: 0.0
              max: 100.0
              unit_of_measurement: '%'
              step: 1.0
              mode: slider
        shading_tilt_elevation_2:
          name: "\U0001F975 Sun Shading Tilt Elevation 2"
          description: Sun elevation for tilt position 2.
          default: 30
          selector:
            number:
              min: 0.0
              max: 90.0
              unit_of_measurement: °
              mode: slider
              step: 1.0
        shading_tilt_position_3:
          name: "\U0001F975 Sun Shading Tilt Position 3"
          description: To which tilt position should the cover be moved for shading
            when the sun is above elevation 3?
          default: 50
          selector:
            number:
              min: 0.0
              max: 100.0
              unit_of_measurement: '%'
              step: 1.0
              mode: slider
        shading_tilt_elevation_3:
          name: "\U0001F975 Sun Shading Tilt Elevation 3"
          description: Sun elevation for tilt position 3.
          default: 48
          selector:
            number:
              min: 0.0
              max: 90.0
              unit_of_measurement: °
              mode: slider
              step: 1.0
    time_section:
      name: Time Control Configuration
      icon: mdi:clock-time-two-outline
      collapsed: true
      description: '<center> <br /> <strong>Time Control Modes:</strong><br /> - Input
        Fields: Use time inputs directly in this section (recommended)<br /> - Schedule
        Helper: Use external schedule entity for flexible weekday configuration<br
        /> - Disabled: No time-based triggers, only brightness/sun/manual control<br
        /> <br /> For comprehensive explanations, examples, and timing diagrams, see:
        <a href="https://github.com/hvorragend/ha-blueprints/blob/main/blueprints/automation/TIME_CONTROL_VISUALIZATION.md">Time
        Control Guide</a><br /> <br /><br /> </center>'
      input:
        time_control:
          name: ⏲️ Selection of time control options
          description: '<details> <summary><code><strong>CLICK HERE:</strong> Further
            descriptions</code></summary> <br /> <ins>Input fields for time control</ins><br
            /> The times for opening and closing the cover are configured here in
            the blueprint. There are various input fields here for this purpose. Even
            if you only want to control the covers using brightness values or the
            sun elevation, it is important to specify times. The times are used to
            divide the day into "morning" (up) and "evening" (down). <br /><br />
            <ins>Calendar Control</ins><br /> You can use a Home Assistant calendar
            to define when covers should be open or closed. Create calendar events
            with titles "Open Cover" (for daytime) or "Close Cover" (for evening).
            The automation reacts immediately when events start or end. <br /><br
            /> <ins>Disable time control and all time triggers</ins><br /> Finally,
            you can also deactivate the time control completely. This means that the
            roller shutters are not opened or closed via time triggers. The sun-elevation
            and brightness control then also runs completely independently.

            </details>'
          default: time_control_input
          selector:
            select:
              options:
              - label: ✏️ Use the time input fields in this blueprint section
                value: time_control_input
              - label: "\U0001F4C5 Use a Home Assistant calendar"
                value: time_control_calendar
              - label: "\U0001F6AB Disable time control and all time triggers"
                value: time_control_disabled
              sort: false
              multiple: false
              custom_value: false
        time_up_early:
          name: "\U0001F53C Time For Drive Up - Early On Workdays"
          description: 'The earliest time at which the cover may be opened. The cover
            will be opened if <ins>AFTER</ins> this time the defined brightness or
            sun-elevation value is high enough. (**NOTE**: A resident must also be
            awake if one is defined).'
          default: 06:00:00
          selector:
            time: {}
        time_up_early_non_workday:
          name: "\U0001F53C Time For Drive Up - Early On Non-Workdays"
          description: As directly above, but for non-workdays.
          default: 07:00:00
          selector:
            time: {}
        time_up_late:
          name: "\U0001F53C Time For Drive Up - Late On Workdays"
          description: 'The latest time at which the cover should be opened. If the
            required brightness or sun-elevation value has <ins>NOT</ins> yet been
            reached by this time, the cover will still be opened. (**NOTE**: If a
            resident has been defined and the resident is still asleep, then the cover
            will NOT be opened.)'
          default: 08:00:00
          selector:
            time: {}
        time_up_late_non_workday:
          name: "\U0001F53C Time For Drive Up - Late On Non-Workdays"
          description: As directly above, but for non-workdays.
          default: 08:00:00
          selector:
            time: {}
        time_down_early:
          name: "\U0001F53B Time For Drive Down - Early On Workdays"
          description: The earliest time at which the cover may be closed. The cover
            will be closed if <ins>AFTER</ins> this time the defined brightness or
            sun-elevation value is low enough.
          default: '16:00:00'
          selector:
            time: {}
        time_down_early_non_workday:
          name: "\U0001F53B Time For Drive Down - Early On Non-Workdays"
          description: As directly above, but for non-workdays.
          default: '16:00:00'
          selector:
            time: {}
        time_down_late:
          name: "\U0001F53B Time For Drive Down - Late On Workdays"
          description: The latest time at which the cover should be closed. If the
            required brightness or sun-elevation value has <ins>NOT</ins> yet been
            reached by this time, the cover will still be closed. <br /> Please do
            not enter 0:00, because that would be the next day!
          default: '22:00:00'
          selector:
            time: {}
        time_down_late_non_workday:
          name: "\U0001F53B Time For Drive Down - Late On Non-Workdays"
          description: As directly above, but for non-workdays. <br /> Please do not
            enter 0:00, because that would be the next day!
          default: '22:00:00'
          selector:
            time: {}
        workday_sensor:
          name: "\U0001F4BC Sensor For Workday Today"
          description: 'It may be desired to open a cover at a different time on work
            days than on non-work days. The corresponding binary sensor can be defined
            here. If not set, the cover will open every time at time_up_early. <br
            /><br /> I recommend using the [Workday integration](https://www.home-assistant.io/integrations/workday/).
            <br /><br /> Example: `binary_sensor.workday_today` <br /><br />`Optional`'
          default: []
          selector:
            entity:
              filter:
              - domain:
                - binary_sensor
              reorder: false
              multiple: false
        workday_sensor_tomorrow:
          name: "\U0001F4BC Sensor For Workday Tomorrow (only for closing)"
          description: 'When <ins>closing</ins> the blinds, you have the option of
            checking the times for tomorrow rather than the current day. This has
            the advantage that you can <ins>close</ins> the blinds earlier if <ins>tomorrow</ins>
            is a working day. This makes sense if, for example, there is school tomorrow
            but today is actually still the weekend. But the child has to go to bed
            earlier.<br /> If this field is not configured here, the normal working
            day sensor is used. <br /><br /> I recommend using the [Workday integration](https://www.home-assistant.io/integrations/workday/).
            <br /><br /> Example: `binary_sensor.workday_tomorrow` <br /><br />`Optional`'
          default: []
          selector:
            entity:
              filter:
              - domain:
                - binary_sensor
              reorder: false
              multiple: false
        calendar_entity:
          name: "\U0001F4C5 Calendar Entity"
          description: "Select a Home Assistant calendar for time control.\n<details>
            <summary><code><strong>CLICK HERE:</strong> How to use calendar control</code></summary>\n\n**Event
            Titles (case-insensitive):**\n  - **\"Open Cover\"** - Marks the daytime
            window (cover should be open)\n  - **\"Close Cover\"** - Marks the evening
            window (cover should be closed)\n\n**How it works:**\n  - When an \"Open
            Cover\" event starts, the automation treats it like morning time\n  -
            When a \"Close Cover\" event starts, the automation treats it like evening
            time\n  - Events can span multiple days\n  - Multiple events per day are
            supported\n\n**Example schedule:**\n  - Monday to Friday: \"Open Cover\"
            from 06:00-20:00\n  - Saturday/Sunday: \"Open Cover\" from 08:00-22:00\n
            \ - Automatically handles weekday/weekend differences\n\n**Advantages
            over time input:**\n  - No need for separate workday/non-workday times\n
            \ - Can define different times for each day\n  - Can create exceptions
            (holidays, vacations)\n  - Changes take effect immediately without automation
            reload\n\n</details>"
          default: []
          selector:
            entity:
              filter:
              - domain:
                - calendar
              multiple: false
              reorder: false
        calendar_open_title:
          name: "\U0001F4C2 Open Event Title"
          description: 'Title of calendar events that define the daytime window (case-insensitive
            match). Default: "Open Cover".'
          default: Open Cover
          selector:
            text:
              multiple: false
              multiline: false
        calendar_close_title:
          name: "\U0001F4C1 Close Event Title"
          description: 'Title of calendar events that define the evening/close window
            (case-insensitive match). Default: "Close Cover".'
          default: Close Cover
          selector:
            text:
              multiple: false
              multiline: false
    brightness_section:
      name: Brightness Configuration
      description: "<br /> <center><code>Settings if the feature ‘\U0001F505 - Add
        Brightness Condition’ has been activated above.</code></center><br />"
      icon: mdi:brightness-5
      collapsed: true
      input:
        default_brightness_sensor:
          name: "\U0001F505 Default Brightness Sensor"
          description: This default brightness sensor can be defined here, which is
            used for daily up and down.
          default: []
          selector:
            entity:
              filter:
              - domain:
                - sensor
              reorder: false
              multiple: false
        brightness_time_duration:
          name: "\U0001F505 Brightness Time Duration"
          description: Defines the time to given brightness sensor must be stay above/below
            the thresholds.
          default: 30
          selector:
            number:
              min: 0.0
              max: 600.0
              mode: slider
              step: 1.0
              unit_of_measurement: seconds
        brightness_up:
          name: "\U0001F505 Brightness Value For Opening The Cover"
          description: At what brightness value should the cover be opened?
          default: 0
          selector:
            number:
              min: 0.0
              max: 100000.0
              unit_of_measurement: lx
              step: 1.0
              mode: slider
        brightness_down:
          name: "\U0001F505 Brightness Value For Closing The Cover"
          description: At what brightness value should the cover be closed? Must be
            lower then the brightness up value.
          default: 0
          selector:
            number:
              min: 0.0
              max: 100000.0
              unit_of_measurement: lx
              step: 1.0
              mode: slider
        brightness_hysteresis:
          name: "\U0001F505 Brightness Hysteresis Value"
          description: Cover will open only when brightness exceeds (brightness_up
            + hysteresis), and close only when it drops below (brightness_down - hysteresis).
            Prevents frequent open/close cycles.
          default: 0
          selector:
            number:
              min: 0.0
              max: 10000.0
              step: 100.0
              mode: slider
              unit_of_measurement: lx
    sun_section:
      name: Sun Elevation Settings
      description: <br /> <center><code>Settings if the feature ‘☀️ - Add Sun Elevation
        Condition’ has been activated above.</code></center><br />
      icon: mdi:weather-sunny
      collapsed: true
      input:
        default_sun_sensor:
          name: ☀️ Sun Sensor
          description: Which sensors provides attributes with current azimuth and
            elevation of sun. I strongly suggest to use sun.sun ([Sun integration](https://www.home-assistant.io/integrations/sun/)).
            Please make sure that the integration is activated and provides the attributes.
            This sensor is also used for sun protection / sunshade control. <br /><br
            /> <ins>A few examples of threshold values:</ins> <ul> <li>+18° Astronomical
            Dusk</li> <li>+12° Nautical Dusk</li> <li>+6° Dusk</li> <li>0° Sunrise/Sunset
            (Default)</li> <li>-6° Civil Dawn</li> <li>-12° Nautical Dawn</li> <li>-18°
            Astronomical Dawn/Night</li> </ul> <br />`Optional` / `Shading`
          default: sun.sun
          selector:
            entity:
              filter:
              - domain:
                - sun
              reorder: false
              multiple: false
        sun_time_duration:
          name: ☀️ Sun Time Duration
          description: Defines the time to given sun sensor must be stay above/below
            the thresholds.
          default: 30
          selector:
            number:
              min: 0.0
              max: 600.0
              mode: slider
              step: 1.0
              unit_of_measurement: seconds
        sun_elevation_mode:
          name: ☀️ Sun Elevation Mode
          description: "**Select how sun elevation thresholds are determined:** <br
            /><br /> **\U0001F512 Fixed Mode** (Default) <br /> Uses only the fixed
            values configured below (Sun Elevation Value For Opening/Closing). Sensors
            are ignored even if configured. <br /><br /> **\U0001F4CA Dynamic Mode**
            <br /> Uses only the sensor values (Sun Elevation Up/Down Sensor). Fixed
            values are ignored. Sensors must be configured and provide valid numeric
            values. <br /><br /> **\U0001F504 Hybrid Mode** <br /> Combines both:
            Sensor value + Fixed value as offset. Example: Sensor=2.0° + Fixed=1.5°
            = Threshold of 3.5°. Allows seasonal adaptation with manual fine-tuning
            offset. <br /><br /> `Optional` / `Default: Fixed`"
          default: fixed
          selector:
            select:
              options:
              - label: "\U0001F512 Fixed - Use only fixed values"
                value: fixed
              - label: "\U0001F4CA Dynamic - Use only sensor values"
                value: dynamic
              - label: "\U0001F504 Hybrid - Sensor + fixed value as offset"
                value: hybrid
              sort: false
              multiple: false
              custom_value: false
        sun_elevation_up:
          name: ☀️ Sun Elevation Value For Opening The Cover
          description: '**Fixed Mode**: Direct threshold value for opening. <br />
            **Dynamic Mode**: Ignored (sensor value is used). <br /> **Hybrid Mode**:
            Added as offset to sensor value. <br /><br /> The cover will be <ins>opened</ins>
            if the sun elevation is over the calculated threshold.'
          default: 0
          selector:
            number:
              min: -90.0
              max: 90.0
              unit_of_measurement: °
              step: 0.1
              mode: slider
        sun_elevation_down:
          name: ☀️ Sun Elevation Value For Closing The Cover
          description: '**Fixed Mode**: Direct threshold value for closing. <br />
            **Dynamic Mode**: Ignored (sensor value is used). <br /> **Hybrid Mode**:
            Added as offset to sensor value. <br /><br /> The cover will be <ins>closed</ins>
            if the sun elevation is under the calculated threshold.'
          default: 0
          selector:
            number:
              min: -90.0
              max: 90.0
              unit_of_measurement: °
              step: 0.1
              mode: slider
        sun_elevation_up_sensor:
          name: ☀️ Sun Elevation Up Sensor (Dynamic/Hybrid - Optional)
          description: 'Optional sensor that provides a **dynamic elevation threshold**
            for opening based on season. This sensor value is compared with the **sun
            elevation from the sun integration** (`sun.sun` attribute `elevation`).
            <br /><br /> **Behavior depends on Sun Elevation Mode:** <br /> • **Fixed
            Mode**: Sensor is ignored even if configured <br /> • **Dynamic Mode**:
            Sensor value is used directly as threshold (required) <br /> • **Hybrid
            Mode**: Sensor value + Fixed value as combined threshold <br /><br />
            **How it works**: The cover opens when the **current sun elevation is
            higher** than the calculated threshold. The sensor provides the threshold
            (in degrees) — dynamic instead of fixed in the blueprint. <br /><br />
            **Example (Dynamic Mode)**: Sensor value = 2.5° → Cover opens when `sun.sun`
            elevation rises above 2.5°. In summer (sensor = 5.0°) the cover opens
            later than in winter (sensor = -2.0°), matching seasonal sunrise times.
            <br /><br /> **Example (Hybrid Mode)**: Sensor value = 2.0°, Fixed value
            = 1.5° → Threshold = 3.5°. Allows seasonal adaptation with manual fine-tuning.
            <br /><br /> **Multiple covers**: For different behavior per cover (e.g.,
            east vs. west facing), create multiple template sensors with different
            thresholds. <br /><br /> **Recommended options**:<br /> • **Template sensor**
            with seasonal interpolation for automatic year-round adaptation ([Guide](https://github.com/hvorragend/ha-blueprints/blob/main/blueprints/automation/DYNAMIC_SUN_ELEVATION.md))<br
            /> • **Input Number helper** for manual GUI-based configuration via dashboard
            <br /><br /> `Optional` / `Required for Dynamic Mode`'
          default: []
          selector:
            entity:
              filter:
              - domain:
                - sensor
                - input_number
              reorder: false
              multiple: false
        sun_elevation_down_sensor:
          name: ☀️ Sun Elevation Down Sensor (Dynamic/Hybrid - Optional)
          description: 'Optional sensor that provides a **dynamic elevation threshold**
            for closing based on season. This sensor value is compared with the **sun
            elevation from the sun integration** (`sun.sun` attribute `elevation`).
            <br /><br /> **Behavior depends on Sun Elevation Mode:** <br /> • **Fixed
            Mode**: Sensor is ignored even if configured <br /> • **Dynamic Mode**:
            Sensor value is used directly as threshold (required) <br /> • **Hybrid
            Mode**: Sensor value + Fixed value as combined threshold <br /><br />
            **How it works**: The cover closes when the **current sun elevation is
            lower** than the calculated threshold. The sensor provides the threshold
            (in degrees) — dynamic instead of fixed in the blueprint. <br /><br />
            **Example (Dynamic Mode)**: Sensor value = 0.5° → Cover closes when `sun.sun`
            elevation sets below 0.5°. In summer (sensor = 2.0°) the cover closes
            later than in winter (sensor = -4.0°), matching seasonal sunset times.
            <br /><br /> **Example (Hybrid Mode)**: Sensor value = 0.0°, Fixed value
            = -1.0° → Threshold = -1.0°. Allows seasonal adaptation with manual fine-tuning.
            <br /><br /> **Multiple covers**: For different behavior per cover (e.g.,
            east vs. west facing), create multiple template sensors with different
            thresholds. <br /><br /> **Recommended options**:<br /> • **Template sensor**
            with seasonal interpolation for automatic year-round adaptation ([Guide](https://github.com/hvorragend/ha-blueprints/blob/main/blueprints/automation/DYNAMIC_SUN_ELEVATION.md))<br
            /> • **Input Number helper** for manual GUI-based configuration via dashboard
            <br /><br /> `Optional` / `Required for Dynamic Mode`'
          default: []
          selector:
            entity:
              filter:
              - domain:
                - sensor
                - input_number
              reorder: false
              multiple: false
    contacts_section:
      name: Contact Sensors for Ventilation
      description: "<br /> <center><code>Settings if the feature ‘\U0001F4A8 - Enable
        ventilation mode’ has been activated above.</code></center><br /> <center><code>All
        these settings are optional / A Cover Status Helper is required!</code></center><br
        />"
      icon: mdi:door-closed-lock
      collapsed: true
      input:
        contact_window_opened:
          name: "\U0001F6AA Contact Sensor For Open Window (Full Ventilation)"
          description: 'Contact sensor of a door or window handle for detecting <ins>total
            opening</ins>. If this sensor switches to on/true, the cover is <ins>fully
            opened</ins>. At the same time, a lockout protection is <ins>always</ins>
            activated. The cover is not closed and the sun shading is not activated
            when the contact is open.

            <details> <summary><code><strong>CLICK HERE:</strong> Further descriptions</code></summary>


            It must be a binary two-way contact sensor. If a three-way sensor is available,
            it must be converted to a binary two-way sensor using a [template sensor](https://www.home-assistant.io/integrations/template/).
            See also the [following posts](https://community.home-assistant.io/t/cover-control-automation-cca-a-comprehensive-and-highly-configurable-roller-blind-blueprint/680539/593)
            in the forum.


            <strong>Important note:</strong> Please do not enter the same sensor in
            both fields for the contact sensors. This does not work and leads to strange
            situations.

            </details>'
          default: []
          selector:
            entity:
              filter:
              - domain:
                - binary_sensor
                - input_boolean
              reorder: false
              multiple: false
        contact_window_tilted:
          name: "\U0001F4A8 Contact Sensor For Tilted Window (Partial Ventilation)"
          description: 'The contact sensor is required for the <ins>partial</ins>
            ventilation mode. If the contact changes to on/true, the cover is moved
            to the <ins>ventilation</ins> position. The prerequisite is that the cover
            is already closed. After the status changes to off/false, the close position
            is activated again. The same applies in the shading-out situation.

            <details> <summary><code><strong>CLICK HERE:</strong> Further descriptions</code></summary>


            It must be a binary two-way contact sensor. If a three-way sensor is available,
            it must be converted to a binary two-way sensor using a [template sensor](https://www.home-assistant.io/integrations/template/).
            See also the [following posts](https://community.home-assistant.io/t/cover-control-automation-cca-a-comprehensive-and-highly-configurable-roller-blind-blueprint/680539/593)
            in the forum.


            <strong>Important note:</strong> Please do not enter the same sensor in
            both fields for the contact sensors. This does not work and leads to strange
            situations.

            </details>'
          default: []
          selector:
            entity:
              filter:
              - domain:
                - binary_sensor
                - input_boolean
              reorder: false
              multiple: false
        lockout_tilted_options:
          name: "\U0001F4A8 Lockout protection for window tilted"
          description: For the tilted window (or door, of course), you can individually
            specify where a lockout protection should be used.
          default: []
          selector:
            select:
              options:
              - label: "\U0001F6E1️ Lockout protection when closing the cover"
                value: lockout_tilted_closing
              - label: "\U0001F6E1️ Lockout protection when starting the sun shading"
                value: lockout_tilted_shading_start
              - label: "\U0001F6E1️ Lockout protection when the sun shading is ended"
                value: lockout_tilted_shading_end
              sort: false
              multiple: true
              custom_value: false
        auto_ventilate_options:
          name: "\U0001F4A8 Ventilation Configuration"
          description: "Various different ventilation options.\n<details> <summary><code><strong>CLICK
            HERE:</strong> Further descriptions</code></summary>\n\n\n  - <ins>Enables
            a calculated delay after the window is closed:</ins>\n    <br />\n    Normally,
            when the window contact is closed, there is no delay in the upcoming drives.
            If you do want this, you can activate it here.\n    <br /><br />\n    The
            \"Fixed Drive Delay\" and \"Random Drive Delay\" settings which are already
            used everywhere are then used.\n    <br /><br />\n  - <ins>Allow ventilation
            even if cover is already in a higher position:</ins>\n    <br />\n    Activate
            ventilation mode even if the current position of the cover is already
            higher than the ventilation position.\n    <br /><br />\n  - <ins>Using
            the ventilation position when the sun shade is ended:</ins>\n    <br />\n
            \   The cover can also be moved to the ventilation position when the sun
            protection/sun shading is ended.\n    Normally, the cover would be fully
            opened when the shading is ended.\n    <br />\n    To be honest, it makes
            no sense to switch to the ventilation position during the day if more
            air can flow in when the cover is open.\n\n</details>"
          default: []
          selector:
            select:
              options:
              - label: "\U0001F4A8 Enables a calculated delay after the window is
                  closed"
                value: ventilation_delay_enabled
              - label: "\U0001F4A8 Allow ventilation even if cover is already in a
                  higher position"
                value: ventilation_if_lower_enabled
              - label: "\U0001F4A8 Using the ventilation position when the sun shading
                  is ended (instead of opening it completely)"
                value: ventilation_after_shading_end
              multiple: true
              sort: false
              custom_value: false
              mode: list
        contact_delay_trigger:
          name: "\U0001F55B Contact Trigger Delay"
          description: 'How many seconds must the status of the contact sensors be
            valid for the automation to trigger?

            ⚠️ **Race Condition Protection:** If you have multiple contact sensors
            (e.g., window sensor + lock sensor) that can change simultaneously, increase
            this value to prevent race conditions.

            **Symptoms of race conditions:** - Cover closes despite lockout sensor
            being active - Check Home Assistant logs for "max_exceeded: warning" messages
            - If you see these warnings frequently, increase this delay'
          default: 2
          selector:
            number:
              min: 0.0
              max: 10.0
              unit_of_measurement: seconds
              step: 1.0
              mode: slider
        contact_delay_status:
          name: "\U0001F55B Contact Sensor Status Delay"
          description: How long should the automation wait until a trigger of a contact
            sensor becomes valid? This may be necessary if the status of a three-state
            sensor has an intermediate value for a short time.
          default: 3
          selector:
            number:
              min: 0.0
              max: 10.0
              unit_of_measurement: seconds
              step: 1.0
              mode: slider
    shading_section:
      name: Sun Shading / Sun Protection
      description: "<br /> <center><code>Settings if the feature '\U0001F975 - Enable
        automatic sun protection / sunshade control' has been activated above.</code></center><br
        /> <center><code>All these settings are optional / A Cover Status Helper is
        required! / The attributes of default sun sensor (configured above) is used.</code></center><br
        />"
      icon: mdi:shield-sun-outline
      collapsed: true
      input:
        shading_conditions_start_and:
          name: "\U0001F31E Shading START - Required Conditions (AND)"
          description: '**Conditions that MUST ALL be met to START shading**

            <details> <summary><code><strong>CLICK HERE:</strong> How START conditions
            work</code></summary>


            **Required (AND) conditions for START:**

            - ALL selected conditions must be valid

            - If ANY fails, shading will NOT start

            - Use for critical conditions


            **Example - Conservative approach:**

            - Select ALL: Azimuth, Elevation, Brightness, Temperature

            - Result: Shading only when everything is perfect


            **Example - Flexible approach:**

            - Select: Azimuth, Elevation (sun position)

            - Leave others for OR list

            - Result: Sun must be in range, plus other criteria

            </details>'
          default:
          - cond_azimuth
          - cond_elevation
          - cond_brightness
          - cond_temp1
          - cond_temp2
          - cond_forecast_temp
          - cond_forecast_weather
          selector:
            select:
              multiple: true
              mode: dropdown
              options:
              - label: "\U0001F4D0 Sun Azimuth (within configured range)"
                value: cond_azimuth
              - label: "\U0001F4C8 Sun Elevation (within configured range)"
                value: cond_elevation
              - label: "\U0001F506 Brightness (above start threshold)"
                value: cond_brightness
              - label: 1️⃣ Temperature Sensor 1 (above threshold)
                value: cond_temp1
              - label: 2️⃣ Temperature Sensor 2 (above threshold)
                value: cond_temp2
              - label: "\U0001F4CA Forecast Temperature (above threshold)"
                value: cond_forecast_temp
              - label: "\U0001F326️ Forecast Weather Conditions (matching configured
                  conditions)"
                value: cond_forecast_weather
              sort: false
              custom_value: false
        shading_conditions_start_or:
          name: "\U0001F31E Shading START - Optional Conditions (OR)"
          description: '**Conditions where AT LEAST ONE must be met to START shading**

            <details> <summary><code><strong>CLICK HERE:</strong> When to use OR conditions</code></summary>


            **Optional (OR) conditions for START:**

            - At least ONE must be valid

            - Useful for redundant sensors

            - Useful for alternative triggers


            **Example - Redundant temperature sensors:**

            - AND: Azimuth, Elevation, Brightness

            - OR: Temp1, Temp2

            - Result: Sun + Brightness + (Temp1 OR Temp2)

            - If Temp1 fails, Temp2 can still trigger


            **Combined logic:**

            - Final = (ALL AND conditions) AND (ONE OR condition)

            </details>'
          default: []
          selector:
            select:
              multiple: true
              mode: dropdown
              options:
              - label: "\U0001F4D0 Sun Azimuth (within configured range)"
                value: cond_azimuth
              - label: "\U0001F4C8 Sun Elevation (within configured range)"
                value: cond_elevation
              - label: "\U0001F506 Brightness (above start threshold)"
                value: cond_brightness
              - label: 1️⃣ Temperature Sensor 1 (above threshold)
                value: cond_temp1
              - label: 2️⃣ Temperature Sensor 2 (above threshold)
                value: cond_temp2
              - label: "\U0001F4CA Forecast Temperature (above threshold)"
                value: cond_forecast_temp
              - label: "\U0001F326️ Forecast Weather Conditions (matching configured
                  conditions)"
                value: cond_forecast_weather
              sort: false
              custom_value: false
        shading_conditions_end_and:
          name: "\U0001F325️ Shading END - Required Conditions (AND)"
          description: '**Conditions that MUST ALL become invalid to END shading**

            <details> <summary><code><strong>CLICK HERE:</strong> How END conditions
            work</code></summary>


            **Required (AND) conditions for END:**

            - ALL selected conditions must become invalid

            - Shading continues if ANY is still valid

            - Use for stable shading (avoid flickering)


            **Example - Quick response:**

            - AND: (empty)

            - OR: Azimuth, Elevation, Brightness

            - Result: End immediately when ANY condition fails


            **Example - Stable shading:**

            - AND: Brightness, Temp1

            - OR: (empty)

            - Result: Keep shading until BOTH brightness AND temp drop


            **Tip:** Usually END should be MORE permissive than START

            </details>'
          default: []
          selector:
            select:
              multiple: true
              mode: dropdown
              options:
              - label: "\U0001F4D0 Sun Azimuth (outside configured range)"
                value: cond_azimuth
              - label: "\U0001F4C8 Sun Elevation (outside configured range)"
                value: cond_elevation
              - label: "\U0001F506 Brightness (below end threshold)"
                value: cond_brightness
              - label: 1️⃣ Temperature Sensor 1 (below threshold)
                value: cond_temp1
              - label: 2️⃣ Temperature Sensor 2 (below threshold)
                value: cond_temp2
              - label: "\U0001F4CA Forecast Temperature (above threshold)"
                value: cond_forecast_temp
              - label: "\U0001F326️ Forecast Weather Conditions (matching configured
                  conditions)"
                value: cond_forecast_weather
              sort: false
              custom_value: false
        shading_conditions_end_or:
          name: "\U0001F325️ Shading END - Optional Conditions (OR)"
          description: '**Conditions where AT LEAST ONE must become invalid to END
            shading**

            <details> <summary><code><strong>CLICK HERE:</strong> Typical END configurations</code></summary>


            **Optional (OR) conditions for END:**

            - At least ONE must become invalid to end

            - Shading ends quickly when conditions change


            **Recommended: Quick sun position response**

            - AND: (empty)

            - OR: Azimuth, Elevation

            - Result: End immediately when sun moves out of range


            **Alternative: Keep shading longer**

            - AND: Azimuth, Elevation

            - OR: Brightness, Temperature

            - Result: End only when sun is wrong AND (dark OR cold)


            **Combined logic:**

            - Final = (ALL AND invalid) OR (ONE OR invalid)

            </details>'
          default:
          - cond_azimuth
          - cond_elevation
          - cond_brightness
          - cond_temp1
          - cond_temp2
          - cond_forecast_temp
          - cond_forecast_weather
          selector:
            select:
              multiple: true
              mode: dropdown
              options:
              - label: "\U0001F4D0 Sun Azimuth (outside configured range)"
                value: cond_azimuth
              - label: "\U0001F4C8 Sun Elevation (outside configured range)"
                value: cond_elevation
              - label: "\U0001F506 Brightness (below end threshold)"
                value: cond_brightness
              - label: 1️⃣ Temperature Sensor 1 (below threshold)
                value: cond_temp1
              - label: 2️⃣ Temperature Sensor 2 (below threshold)
                value: cond_temp2
              - label: "\U0001F4CA Forecast Temperature (above threshold)"
                value: cond_forecast_temp
              - label: "\U0001F326️ Forecast Weather Conditions (matching configured
                  conditions)"
                value: cond_forecast_weather
              sort: false
              custom_value: false
        shading_azimuth_start:
          name: "\U0001F4D0 Sun Shading - Azimuth Start Value"
          description: What is the minimum azimuth at which the sun hits the window?
            (Shading will start)
          default: 95
          selector:
            number:
              min: 0.0
              max: 365.0
              unit_of_measurement: °
              step: 1.0
              mode: slider
        shading_azimuth_end:
          name: "\U0001F4D0 Sun Shading - Azimuth End Value"
          description: What is the maximum azimuth at which the sun hits the window?
            (Shading will stop)
          default: 265
          selector:
            number:
              min: 0.0
              max: 365.0
              unit_of_measurement: °
              step: 1.0
              mode: slider
        shading_elevation_min:
          name: "\U0001F4C8 Sun Shading - Elevation Minimum Value"
          description: Starting from which elevation of the sun should the window
            be shaded? (Here it makes sense to consider surrounding buildings, trees,
            etc.).
          default: 25
          selector:
            number:
              min: -90.0
              max: 90.0
              unit_of_measurement: °
              step: 0.1
              mode: slider
        shading_elevation_max:
          name: "\U0001F4C8 Sun Shading - Elevation Maximum Value"
          description: What is the maximal elevation for elevation? (In most cases,
            90 degrees is probably the most reasonable value. However, this can also
            be different due to surrounding buildings, etc.).
          default: 90
          selector:
            number:
              min: -90.0
              max: 90.0
              unit_of_measurement: °
              step: 0.1
              mode: slider
        shading_brightness_sensor:
          name: "\U0001F506 Sun Shading - Brightness Sensor"
          description: This sensor is only used for shading.
          default: []
          selector:
            entity:
              filter:
              - domain:
                - sensor
              reorder: false
              multiple: false
        shading_sun_brightness_start:
          name: "\U0001F506 Sun Shading - Brightness Start Value"
          description: The minimum brightness value from which shading should start.
            (Must be above the value of brightness end!)
          default: 35000
          selector:
            number:
              min: 0.0
              max: 100000.0
              unit_of_measurement: lx
              step: 1.0
              mode: slider
        shading_sun_brightness_end:
          name: "\U0001F506 Sun Shading - Brightness End Value"
          description: The brightness value from which shading is no longer necessary.
            (Must be below the value of brightness start!).
          default: 25000
          selector:
            number:
              min: 0.0
              max: 100000.0
              unit_of_measurement: lx
              step: 1.0
              mode: slider
        shading_sun_brightness_hysteresis:
          name: "\U0001F506 Sun Shading - Brightness Hysteresis"
          description: 'Hysteresis value to prevent flickering. The brightness must
            exceed (start + hysteresis) to activate shading and fall below (end -
            hysteresis) to deactivate it. See: [FAQ - How does hysteresis work?](https://github.com/hvorragend/ha-blueprints/blob/main/blueprints/automation/FAQ.md#q-how-does-hysteresis-work)'
          default: 0
          selector:
            number:
              min: 0.0
              max: 10000.0
              unit_of_measurement: lx
              step: 100.0
              mode: slider
        shading_temperatur_sensor1:
          name: 1️⃣ Sun Shading - Temperature Sensor 1 (eg. indoor)
          description: This is the first temperature sensor used for sun shading logic.<br
            /> For example, you can use the current **indoor temperature** as a condition
            to trigger shading.
          selector:
            entity:
              filter:
              - domain:
                - sensor
              reorder: false
              multiple: false
          default: []
        shading_min_temperatur1:
          name: 1️⃣ Sun Shading - Temperature Sensor 1 Minimum Value
          description: Minimum temperature for sensor 1 above which shading should
            occur.
          default: 18
          selector:
            number:
              min: 0.0
              max: 50.0
              step: 0.1
              mode: slider
              unit_of_measurement: °C
        shading_temperature_hysteresis1:
          name: 1️⃣ Sun Shading - Temperature Sensor 1 Hysteresis Value
          description: 'Shading will end only when temperature drops below (minimum
            - hysteresis value) to prevent frequent open/close cycles. See: [FAQ -
            How does hysteresis work?](https://github.com/hvorragend/ha-blueprints/blob/main/blueprints/automation/FAQ.md#q-how-does-hysteresis-work)'
          default: 0.2
          selector:
            number:
              min: 0.0
              max: 5.0
              step: 0.1
              mode: slider
              unit_of_measurement: °C
        shading_temperatur_sensor2:
          name: 2️⃣ Sun Shading - Temperature Sensor 2 (eg. outdoor)
          description: This is an optional secondary temperature sensor, typically
            used for **outdoor temperature**. <br /> It can serve as an additional
            condition for sun shading logic. <br /><br /> This sensor also plays a
            role in the calculation of the <ins>Sun Shading - Forecast Temperature
            Value</ins>. Please refer to that section for more details.
          default: []
          selector:
            entity:
              filter:
              - domain:
                - sensor
              reorder: false
              multiple: false
        shading_min_temperatur2:
          name: 2️⃣ Sun Shading - Temperature Sensor 2 Minimum Value
          description: Minimum temperature for sensor 2 above which shading should
            occur.
          default: 18
          selector:
            number:
              min: 0.0
              max: 50.0
              step: 0.1
              mode: slider
              unit_of_measurement: °C
        shading_temperature_hysteresis2:
          name: 2️⃣ Sun Shading - Temperature Sensor 2 Hysteresis Value
          description: 'Shading will end only when temperature drops below (minimum
            - hysteresis value) to prevent frequent open/close cycles. See: [FAQ -
            How does hysteresis work?](https://github.com/hvorragend/ha-blueprints/blob/main/blueprints/automation/FAQ.md#q-how-does-hysteresis-work)'
          default: 0.2
          selector:
            number:
              min: 0.0
              max: 5.0
              step: 0.1
              mode: slider
              unit_of_measurement: °C
        shading_forecast_sensor:
          name: "\U0001F4CA Sun Shading - Forecast Weather Entity"
          description: "Weather entity for forecast data (temperature & conditions).
            This is the primary and recommended method for forecast-based shading.\n<details>\n
            \ <summary><code><strong>CLICK HERE:</strong> How to configure</code></summary>\n\n
            \ Select a weather entity (e.g., weather.home, weather.openweathermap).\n\n
            \ The system will:\n\n  - Query forecast temperature for comparison\n\n
            \ - Check weather conditions if configured\n\n  - Use daily or hourly
            forecast based on your selection below\n\n  **Most users should use this
            field.**\n\n  The idea is that it can happen, especially in spring, that
            the value of the\n  <em>Forecast Temperature Value</em> is exceeded by
            strong solar radiation and\n  the shading would be started. However, in
            spring you may not want shading,\n  but the solar radiation as a welcome,
            free heating is desired.\n  So you can define via the forecast that shading
            is only started at an\n  expected daily maximum temperature.\n\n</details>"
          default: []
          selector:
            entity:
              filter:
              - domain:
                - weather
              reorder: false
              multiple: false
        shading_forecast_type:
          name: "\U0001F4CA Sun Shading - Forecast Source"
          description: 'Please select whether you want to use the **daily** or **hourly**
            weather forecast. This only works if a weather entity has been configured
            above. <br /> The first entry in the forecast array will always be used
            — this corresponds to the current day or current hour. <br /><br /> Note:
            Your weather entity must support `weather.get_forecasts`, which was introduced
            in Home Assistant 2023.9. <br /><br /> Alternatively, you can choose **not
            to use** the forecast service at all. In that case, the current weather
            attributes from the weather entity will be used instead. <br /><br />
            **Recommendation:** Using the **daily forecast** is generally preferred
            for sun shading purposes. <br /><br /> **Ignored when using temperature
            sensor below.**'
          default: daily
          selector:
            select:
              options:
              - label: Use the daily weather forecast service
                value: daily
              - label: Use the hourly weather forecast service
                value: hourly
              - label: Do not use a weather forecast, but the current weather attributes
                value: weather_attributes
              multiple: false
              sort: false
              custom_value: false
        shading_forecast_temp_sensor:
          name: "\U0001F4CA Sun Shading - Direct Temperature Sensor (Alternative)"
          description: "**Alternative method:** Use a sensor that directly provides
            forecasted max temperature.\n<details>\n  <summary><code><strong>CLICK
            HERE:</strong> When to use this</code></summary>\n\n  Use this if:\n\n
            \ - Your weather integration provides dedicated forecast sensors\n\n  -
            You want better performance (no forecast service calls)\n\n  - You have
            custom template sensors for forecast temperature\n\n  Examples:\n\n  -
            sensor.pirateweather_daytime_high_apparent_temperature_0d\n\n  - sensor.met_no_forecast_temperature_max\n\n
            \ - sensor.openweathermap_forecast_temperature\n\n  **Priority:** If configured,
            this takes priority over weather entity above.\n\n  **Limitation:** Weather
            condition checks are not available with sensors.\n\n</details>"
          default: []
          selector:
            entity:
              filter:
              - domain:
                - sensor
              reorder: false
              multiple: false
        shading_forecast_temp:
          name: "\U0001F4CA Sun Shading - Forecast Temperature Value"
          description: "This setting defines the <strong>minimum temperature threshold</strong>
            based on the forecast at which shading should be activated. If the forecasted
            temperature exceeds this value, the shading system will respond accordingly.\n-
            Minimum temperature threshold for shading activation.\n- Works with both
            weather entity and temperature sensor.\n- Leave empty to disable temperature-based
            forecast shading.\n<details>\n  <summary><code><strong>CLICK HERE:</strong>
            Further description</code></summary>\n\n  To enhance reliability, the
            system can compare this threshold against two sources:\n\n  - The forecasted
            temperature (this comparison is always active)\n\n  - Temperature Sensor
            2 (e.g. outdoor) (can be enabled via the checkbox in the next configuration
            field)\n\n</details>"
          default: []
          selector:
            number:
              min: 0.0
              max: 50.0
              step: 0.1
              mode: slider
              unit_of_measurement: °C
        shading_forecast_temp_hysteresis:
          name: "\U0001F4CA Sun Shading - Forecast Temperature Hysteresis"
          description: 'Prevents frequent on/off cycles near threshold.

            - Shading starts: forecast > (threshold + hysteresis)

            - Shading ends: forecast < (threshold - hysteresis)


            See: [FAQ - How does hysteresis work?](https://github.com/hvorragend/ha-blueprints/blob/main/blueprints/automation/FAQ.md#q-how-does-hysteresis-work)'
          default: 0
          selector:
            number:
              min: 0.0
              max: 5.0
              step: 0.1
              mode: slider
              unit_of_measurement: °C
        shading_weather_conditions:
          name: "\U0001F326️ Sun Shading - Weather Conditions"
          description: 'Check the following weather conditions when activating the
            shading. Be cautious when making your selection, as weather forecasts
            may not always be accurate and could lead to incorrect shading decisions.
            And as mentioned above, weather conditions can only be checked if a weather
            entity has been configured under ‘Forecast Weather Sensor’. <br /><br
            /> **Only works with weather entity, not with temperature sensor.** <br
            /> Be cautious: weather forecasts may not always be accurate.'
          default: []
          selector:
            select:
              multiple: true
              options:
              - clear-night
              - clear
              - cloudy
              - fog
              - hail
              - lightning
              - lightning-rainy
              - partlycloudy
              - pouring
              - rainy
              - snowy
              - snowy-rainy
              - sunny
              - windy
              - windy-variant
              - exceptional
              sort: false
              custom_value: false
        shading_config:
          name: "\U0001F975 Sun Shading - Configuration"
          description: "These options allow you to fine-tune how the system handles
            temperature-based shading. <p><em>Click on the titles to get further help.</em></p>
            <details> <summary><code><strong>Independent Shading via Temperature Comparison</strong></code></summary>\n\n
            \ Enables shading based solely on temperature — <ins>independently of
            other conditions</ins> like brightness, sun position, or time.\n  - By
            default, only the external <em>forecasted temperature</em> is compared
            with the configured threshold in <em>\"Forecast Temperature Value\"</em>.\n
            \ - You can also enable another comparison by selecting the other checkbox.\n\n
            \ If any value exceeds the threshold, shading will be activated — even
            if all other shading conditions are false.\n\n  This is especially useful
            in the early morning: the system can already determine whether shading
            will be needed later in the day. Instead of fully opening the blinds,
            it can move them directly into the shading position.\n\n  Additionally,
            one hour before the earliest possible opening time, the system retrieves
            the latest weather forecast.\n\n  This allows it to compare the updated
            forecasted temperature with the configured threshold and make an early
            shading decision based on the most current data.\n\n</details> <br />
            <details> <summary><code><strong>Additionally compare forecast temperature
            with Sensor 2</strong></code></summary>\n\n  This activates an extended
            comparison, checking not only whether the external forecasted temperature
            is above the threshold configured in <em>\"Forecast Temperature Value\"</em>,
            but also whether the current reading from <em>\"Temperature Sensor 2\"</em>
            (e.g. outdoor) is above the threshold configured for it. The shading condition
            will be true if <em>one</em> of the following conditions is met:\n  -
            The external forecasted temperature exceeds the configured threshold in
            <em>\"Forecast Temperature Value\"</em>, or<br />\n  - the <em>\"Temperature
            Sensor 2\"</em> reports a higher temperature than in <em>\"Forecast Temperature
            Value\"</em>.\n\n  This mechanism improves the responsiveness and reliability
            of the shading system by using real-time sensor input as an optional condition,
            especially when forecast data is uncertain.\n  Note: This function has
            nothing to do with normal temperature comparison, but is used exclusively
            in the context of the forecast function.\n</details>"
          default: []
          selector:
            select:
              options:
              - label: Independent Shading via Temperature Comparison
                value: shading_temp_comparison_independent
              - label: Additionally compare 'Forecast Temperature Value' with 'Temperature
                  Sensor 2'
                value: shading_compare_forecast_with_sensor2
              multiple: true
              sort: false
              custom_value: false
              mode: list
        shading_waitingtime_start:
          name: "\U0001F975 Sun Shading - Start Waiting Time"
          description: To avoid overloading the motor, a waiting time can be defined
            here for the start of shading. The shade will only start if <ins>all</ins>
            mandatory conditions are fulfilled for the entire waiting time. This waiting
            time is also used for the periodic condition checks within the retry loop.
          default: 300
          selector:
            number:
              min: 0.0
              max: 3600.0
              unit_of_measurement: seconds
              step: 1.0
              mode: slider
        shading_start_max_duration:
          name: "\U0001F975 Sun Shading - Maximum duration for shading start retry
            loop"
          description: 'Maximum time to keep retrying shading start conditions after
            initial trigger.

            The Start Waiting Time is used for the periodic condition checks during
            this retry loop.

            If conditions remain unstable for longer than this timeout, the retry
            loop is stopped.

            Shading will not start and waits for a new shading start trigger.


            **Use case:** Prevents automation from being stuck in "waiting for stable
            conditions"

            when weather is highly unstable (rapidly changing clouds).


            **0 = disabled** (no periodic retry, stops immediately - old "trigger_reset"
            behavior)

            **Recommended: 3600-7200 seconds (1-2 hours)**

            '
          default: 7200
          selector:
            number:
              min: 0.0
              max: 14400.0
              step: 300.0
              unit_of_measurement: seconds
              mode: slider
        shading_waitingtime_end:
          name: "\U0001F975 Sun Shading - End Waiting Time"
          description: To avoid excessive load on the motor, a waiting time can be
            defined here before the shading is ended. Shading ends if one of the conditions
            is not fulfilled for the entire waiting time. This waiting time is also
            used for the periodic condition checks within the retry loop.
          default: 300
          selector:
            number:
              min: 0.0
              max: 3600.0
              unit_of_measurement: seconds
              step: 1.0
              mode: slider
        shading_end_max_duration:
          name: "\U0001F975 Sun Shading - Maximum duration for shading end retry loop"
          description: 'Maximum time to keep retrying shading end conditions after
            initial trigger.

            The End Waiting Time is used for the periodic condition checks during
            this retry loop.

            If conditions remain unstable for longer than this timeout, the retry
            loop is stopped.

            Shading remains active and waits for a new shading end trigger.


            **Use case:** Prevents automation from being stuck in "waiting for stable
            conditions"

            when weather is highly unstable (rapidly changing clouds).


            **0 = disabled** (no periodic retry, behaves like old version)

            **Recommended: 3600-7200 seconds (1-2 hours)**

            '
          default: 7200
          selector:
            number:
              min: 0.0
              max: 14400.0
              step: 300.0
              unit_of_measurement: seconds
              mode: slider
        shading_end_immediate_by_sun_position:
          name: "\U0001F975 End Sun Shading - Immediately When Out Of Range"
          description: If enabled, shading will end immediately (only a few seconds
            later) when sun position moves outside the defined azimuth or elevation
            range. If disabled, the configured waiting time will be used before ending
            the shading.
          default: false
          selector:
            boolean: {}
    resident_section:
      name: Resident Settings
      description: <center> <br /> (1) The purpose of resident mode is to the close
        the cover (without checking the defined times) when the resident sensor switches
        to ‘on/true’. For example, when a resident goes to sleep. <br /> (2) The cover
        will stay closed as long as the sensor remains in this state. <br /> (3) When
        the resident sensor switches to ‘off/false’, the cover is automatically opened
        in the morning. <br /> (4) In addition, the usual automatic opening of the
        cover is prevented as long as the sensor is set to ‘on/true’ or the resident.
        <br /><br /> All these settings are optional. <br /><br /> </center>
      icon: mdi:human-male-female
      collapsed: true
      input:
        resident_sensor:
          name: "\U0001F6CC Resident Sensor"
          description: You can use this to define a resident (input_boolean or binary_sensor)
            for the room
          default: []
          selector:
            entity:
              domain:
              - input_boolean
              - binary_sensor
              - switch
              reorder: false
              multiple: false
        resident_config:
          name: "\U0001F6CC Resident Configuration"
          description: 'Configure how the automation responds to resident sensor changes.


            - **Opening enabled**: Cover opens when resident wakes up, but only if
            time, brightness, and sun elevation conditions are met

            - **Closing enabled**: Cover closes when resident goes to sleep (immediate,
            ignores other conditions)

            '
          default: []
          selector:
            select:
              options:
              - label: "\U0001F53C Enable automatic opening when resident wakes up
                  (respects all opening conditions)"
                value: resident_opening_enabled
              - label: "\U0001F53B Enable automatic closing when resident goes to
                  sleep (immediate)"
                value: resident_closing_enabled
              - label: "\U0001F975 Allow sun protection when resident is still present"
                value: resident_allow_shading
              - label: "\U0001F53C Allow opening the cover when resident is still
                  present"
                value: resident_allow_opening
              - label: "\U0001F4A8 Allow ventilation when resident is still present"
                value: resident_allow_ventilation
              multiple: true
              sort: false
              custom_value: false
              mode: list
    override_section:
      name: Manual Override
      description: <br /> <center><code>A Cover Status Helper is required!</code></center><br
        />
      icon: mdi:debug-step-over
      collapsed: true
      input:
        ignore_after_manual_config:
          name: "\U0001F590️ Ignoring/override after manual position changes"
          description: "Ignore or override the following actions after manual position
            changes.\n<details> <summary><code><strong>CLICK HERE:</strong> Further
            description</code></summary>\n\nUltimately, this means that the cover
            will not be opened, closed, etc. if a manual interaction has previously
            been made, e.g. using a wall switch.\n\nThe reason behind this is that
            the human being wins with his decision and his conscious decision is weighted
            higher than the upcoming action of the automation.\n\nAs soon as a cover
            has been moved manually, the status is recorded in the Cover Status Helper.
            This usually means that a person has deliberately decided against a status.\n\n
            \ - If option is not activated: The covers are moved even if a manual
            correction has been made.\n  - If option is activated: The action to open,
            close, etc. is not performed because a conscious decision was made to
            do otherwise due to a manual intervention.\n\n\nA Cover Status Helper
            is required!\n</details>"
          default: []
          selector:
            select:
              options:
              - label: "\U0001F53C Ignore/override next automatic opening after manual
                  position changes"
                value: ignore_opening_after_manual
              - label: "\U0001F53B Ignore/override next automatic closing after manual
                  position changes"
                value: ignore_closing_after_manual
              - label: "\U0001F4A8 Ignore/override next automatic ventilation after
                  manual position changes"
                value: ignore_ventilation_after_manual
              - label: "\U0001F975 Ignore/override next automatic sun shading after
                  manual position changes"
                value: ignore_shading_after_manual
              multiple: true
              sort: false
              custom_value: false
              mode: list
        reset_override_config:
          name: "\U0001F5D1️ Reset manual override"
          description: If the detection of the manual position change was activated
            above, you may need a way to reset this status. Otherwise, the next cover
            movements will be permanently ignored or overridden. Or you have not activated
            an individual action, e.g. when closing the covers, which resets the status.
          default: reset_disabled
          selector:
            select:
              options:
              - label: No timed reset for manual override
                value: reset_disabled
              - label: Reset at a specified time (see below)
                value: reset_fixed_time
              - label: Reset after a timeout in minutes (see below)
                value: reset_timeout
              multiple: false
              sort: false
              custom_value: false
              mode: list
        reset_override_time:
          name: "\U0001F5D1️ Time to reset manual override"
          description: At what time do you want the manual detection to be reset?
          default: 00:01:00
          selector:
            time: {}
        reset_override_timeout:
          name: "\U0001F5D1️ Number of minutes until reset manual override"
          description: After how many minutes should it be reset?
          default: 5
          selector:
            number:
              min: 0.0
              max: 1440.0
              unit_of_measurement: minutes
              step: 1.0
              mode: slider
    delay_section:
      description: <center> <br /> Take into account that, for example, waiting times
        are added when brightness changes or shading is started. It is therefore better
        to use smaller values here. It is only a matter of separating the different
        covers in terms of time. <br /><br /> </center>
      name: Delay Settings
      icon: mdi:timer-outline
      collapsed: true
      input:
        drive_delay_fix:
          name: "\U0001F55B Fixed Drive Delay"
          description: Fixed drive delay to avoid radio interferences. <br /><br />`Optional`
          default: 0
          selector:
            number:
              min: 0.0
              max: 600.0
              unit_of_measurement: seconds
              step: 1.0
              mode: slider
        drive_delay_random:
          name: "\U0001F55B Random Drive Delay"
          description: Additional random delay. <br /><br />`Optional`
          default: 5
          selector:
            number:
              min: 0.0
              max: 600.0
              unit_of_measurement: seconds
              step: 1.0
              mode: slider
    condition_section:
      name: Additional Conditions
      description: <br /> <center><code>All these settings are optional</code></center><br
        />
      icon: mdi:help-rhombus-outline
      collapsed: true
      input:
        auto_global_condition:
          name: ❓ Additional Condition for the entire automation
          description: This condition allows you to control the execution of the <ins>entire</ins>
            automation dynamically and outside of the blueprint configuration. With
            this option you could enable a party mode. <br /><br /> If the result
            of this condition is <ins>true</ins>, the automation will continue.<br
            /> The result of the conditions must be <ins>false</ins>, for the automation
            to stop in this sequence. <br /><br /> Forcing Open/Close/Shading/Ventilation
            is therefore only possible if this condition remains empty or becomes
            valid.
          default: []
          selector:
            condition: {}
        auto_up_condition:
          name: "\U0001F53C Additional Condition For Opening The Cover"
          description: This condition can be used to dynamically control the <ins>opening</ins>
            of the cover. You can use this, for example, if the covers normally don't
            open, but you really want to do it on vacation. <br /><br /> If the result
            of this condition is <ins>true</ins>, the automation will continue.<br
            /> The result of the conditions must be <ins>false</ins>, for the automation
            to stop in this sequence.
          default: []
          selector:
            condition: {}
        auto_down_condition:
          name: "\U0001F53B Additional Condition For Closing The Cover"
          description: This condition can be used to dynamically control the <ins>closing</ins>
            of the cover. You can use this, for example, at Christmas time or if you
            want the covers to behave differently while on vacation. <br /><br />
            If the result of this condition is <ins>true</ins>, the automation will
            continue.<br /> The result of the conditions must be <ins>false</ins>,
            for the automation to stop in this sequence.
          default: []
          selector:
            condition: {}
        auto_ventilate_condition:
          name: "\U0001F4A8 Additional Condition For Activating Ventilation"
          description: This condition can be used to dynamically control the <ins>start
            of the ventilation</ins> of the cover. <br /><br /> If the result of this
            condition is <ins>true</ins>, the automation will continue.<br /> The
            result of the conditions must be <ins>false</ins>, for the automation
            to stop in this sequence.
          default: []
          selector:
            condition: {}
        auto_ventilate_end_condition:
          name: "\U0001F4A8 Additional Condition For Disabling Ventilation"
          description: This condition can be used to dynamically control the <ins>end
            of the ventilation</ins> of the cover. <br /><br /> If the result of this
            condition is <ins>true</ins>, the automation will continue.<br /> The
            result of the conditions must be <ins>false</ins>, for the automation
            to stop in this sequence.
          default: []
          selector:
            condition: {}
        auto_shading_start_condition:
          name: "\U0001F975 Additional Condition When Activating Sun Shading"
          description: 'This condition can be used to dynamically control the <ins>shading-IN-automation</ins>
            of the cover. This can be useful if you want to temporarily disable automation
            (e.g. because of control by other automations). <br /><br /> If the result
            of this condition is <ins>true</ins>, the automation will continue.<br
            /> The result of the conditions must be <ins>false</ins>, for the automation
            to stop in this sequence. <br /> Another example: Here you could also
            set that the shading is only triggered in the summer season.'
          default: []
          selector:
            condition: {}
        auto_shading_tilt_condition:
          name: "\U0001F975 Additional Condition For Sun Shading Tilt"
          description: 'This condition can be used to dynamically control the <ins>shading_tilt-IN-automation</ins>
            of the cover. This can be useful if you want to temporarily disable automation
            (e.g. because of control by other automations). <br /> Another example:
            Here you could also set that the tilting is only triggered in the summer
            season. <br /><br /> If the result of this condition is <ins>true</ins>,
            the automation will continue.<br /> The result of the conditions must
            be <ins>false</ins>, for the automation to stop in this sequence.'
          default: []
          selector:
            condition: {}
        auto_shading_end_condition:
          name: "\U0001F975 Additional Condition When Deactivating  Sun Shading"
          description: This condition can be used to dynamically control the <ins>shading-OUT-automation</ins>
            of the cover. This can be useful if you want to temporarily disable automation
            (e.g. because of control by other automations). <br /> If the result of
            this condition is <ins>true</ins>, the automation will continue.<br />
            The result of the conditions must be <ins>false</ins>, for the automation
            to stop in this sequence.
          default: []
          selector:
            condition: {}
    force_section:
      name: Force Features
      description: "<center> <br /> Emergency override controls for weather protection
        and special scenarios<br />\nForce functions allow you to immediately move
        covers to specific positions, overriding all other automation logic. Use cases
        include: rain protection, wind protection, frost prevention, or temporary
        manual control. </center>\n<details> <summary><code><strong>IMPORTANT: How
        Force Works</strong></code></summary>\n\n  **Basic Behavior (Without Background
        State Tracking):**\n  - When a force is activated, the cover moves immediately
        to the target position\n  - All other automation logic is disabled while force
        is active\n  - When the force is deactivated, CCA remains idle until the next
        trigger\n  - You must manually reset the cover to the desired position or
        rely on other automations\n\n  **Advanced: Automatic Return to Target (With
        Background State Tracking)**\n  - Enable \"Return to Target State After Force
        Disable\" to automatically restore the cover\n  - While a force is active,
        CCA continues tracking what position the cover *should* be in\n  - When you
        deactivate the force, the cover automatically returns to that target position\n
        \ - Example: Force-close for rain protection → Rain stops → Cover automatically
        returns to open or shading position\n\n  **Important Constraints:**\n  - Only
        ONE force function can be active at a time\n  - Force functions have the highest
        priority - no other automation can override them\n  - To stop forcing, manually
        deactivate the force entity\n  - Multiple simultaneous force activations will
        trigger a configuration warning\n\n</details>\n<center> <br /><code>All settings
        in this section are optional</code><br /><br /> </center>"
      icon: mdi:arm-flex
      collapsed: true
      input:
        auto_recover_after_force:
          name: "\U0001F519 Return to Target State After Force Disable"
          description: "**\U0001F680 Seamless control with automatic recovery!**\nWhen
            enabled, the cover automatically returns to its intended position when
            a force function is disabled.\n<details> <summary><code><strong>HOW IT
            WORKS:</strong> Smart Background Tracking</code></summary>\n\n**Continuous
            Status Tracking:**\n- The helper constantly monitors what the cover *should*
            be doing (open, close, shading, ventilate)\n- Even when a force function
            is active, the background state is always up-to-date\n- When the force
            function is disabled, the cover knows exactly where to return\n\n**Perfect
            for:**\n- \U0001F327️ Rain Protection (force-close, auto-recover to shading)\n-
            \U0001F4A8 Wind Protection (force-open, auto-recover to normal state)\n-
            ❄️ Frost Protection (force-open, auto-resume after sunrise)\n- \U0001F525
            Emergency Scenarios (temporary manual control, then auto-recovery)\n-
            \U0001F3E0 Cleaning/Maintenance (force-open, then auto-return when done)\n\n</details>"
          default: auto_recover_disabled
          selector:
            select:
              options:
              - label: ✅ Enable Automatic Return to Target State
                value: auto_recover_enabled
              - label: ❌ Disable Automatic Return to Target State
                value: auto_recover_disabled
              multiple: false
              sort: false
              custom_value: false
              mode: list
        auto_up_force:
          name: "\U0001F53C Force Immediate Opening via Entity"
          description: If the status of this entity changes to on or true, the cover
            is opened immediately and without further checking.
          default: []
          selector:
            entity:
              domain:
              - input_boolean
              - binary_sensor
              - switch
              reorder: false
              multiple: false
        auto_down_force:
          name: "\U0001F53B Force Immediate Closing via Entity"
          description: If the status of this entity changes to on or true, the cover
            is closed immediately and without further checking.
          default: []
          selector:
            entity:
              domain:
              - input_boolean
              - binary_sensor
              - switch
              reorder: false
              multiple: false
        auto_ventilate_force:
          name: "\U0001F4A8 Force Immediate Ventilation via Entity"
          description: If the status of this entity changes to on or true, the cover
            is immediately set to ventilation mode and without further checking.
          default: []
          selector:
            entity:
              domain:
              - input_boolean
              - binary_sensor
              - switch
              reorder: false
              multiple: false
        auto_shading_start_force:
          name: "\U0001F975 Force Activation Sun Shading via Entity"
          description: If the status of this entity changes to on or true, the shading
            is immediately activated and without further checking.
          default: []
          selector:
            entity:
              domain:
              - input_boolean
              - binary_sensor
              - switch
              reorder: false
              multiple: false
    actions_section:
      name: Additional Actions
      description: <br /> <center><code>All these settings are optional</code></center><br
        />
      icon: mdi:run
      collapsed: true
      input:
        auto_up_action_before:
          name: "\U0001F53C Additional Actions Before Opening The Cover"
          description: Additional actions to run <ins>before</ins> opening the cover
          default: []
          selector:
            action: {}
        auto_up_action:
          name: "\U0001F53C Additional Actions After Opening The Cover"
          description: Additional actions to run <ins>after</ins> opening the cover
          default: []
          selector:
            action: {}
        auto_down_action_before:
          name: "\U0001F53B Additional Actions Before Closing The Cover"
          description: Additional actions to run <ins>before</ins> closing the cover
          default: []
          selector:
            action: {}
        auto_down_action:
          name: "\U0001F53B Additional Actions After Closing The Cover"
          description: Additional actions to run <ins>after</ins> closing the cover
          default: []
          selector:
            action: {}
        auto_ventilate_action_before:
          name: "\U0001F4A8 Additional Actions Before Ventilating The Cover"
          description: Additional actions to run <ins>before</ins> ventilating the
            cover
          default: []
          selector:
            action: {}
        auto_ventilate_action:
          name: "\U0001F4A8 Additional Actions After Ventilating The Cover"
          description: Additional actions to run <ins>after</ins> ventilating the
            cover
          default: []
          selector:
            action: {}
        auto_shading_start_action_before:
          name: "\U0001F975 Additional Actions Before Activating Sun Shading"
          description: Additional actions to run <ins>before</ins> activating sun
            shading
          default: []
          selector:
            action: {}
        auto_shading_start_action:
          name: "\U0001F975 Additional Actions After Activating Sun Shading"
          description: Additional actions to run <ins>after</ins> activating sun shading
          default: []
          selector:
            action: {}
        auto_shading_end_action_before:
          name: "\U0001F975 Additional Actions Before Disabling Sun Shading"
          description: Additional actions to run <ins>before</ins> disabling sun shading
          default: []
          selector:
            action: {}
        auto_shading_end_action:
          name: "\U0001F975 Additional Actions After Disabling Sun Shading"
          description: Additional actions to run <ins>after</ins> disabling sun shading
          default: []
          selector:
            action: {}
        auto_manual_action:
          name: "\U0001F590️ Additional Actions After Manual Change"
          description: Additional actions after a manual change to the covers
          default: []
          selector:
            action: {}
        auto_override_reset_action:
          name: "\U0001F5D1️ Additional Actions After Override Reset"
          description: Additional actions to be taken after resetting the manual override
          default: []
          selector:
            action: {}
    configcheck_section:
      name: Configuration Check
      description: "<br /> <center> <strong>\U0001F517 Use the <a href=\"https://hvorragend.github.io/ha-blueprints/validator/\"
        target=\"_blank\">Online Configuration Validator</a></strong> <br /> Validate
        your YAML configuration for errors, typos, and deprecated parameters before
        deploying. <br /> ✅ Instant feedback • \U0001F50D Typo detection • ⚠️ Migration
        guidance • \U0001F512 Privacy-friendly (client-side) <br /><br /> </center>"
      icon: mdi:invoice-text-check-outline
      collapsed: true
      input:
        check_config:
          name: ✔️ Check Configuration
          description: With this boolean, you can enable or disable the basic plausibility
            check for the configuration. The check only takes place if the automation
            is executed manually. <br /><br /> Use the <a href="https://hvorragend.github.io/ha-blueprints/validator/"
            target="_blank">online validator</a> for another validation.
          default: false
          selector:
            boolean: {}
        check_config_debuglevel:
          name: ✔️ Check Configuration - Debug level
          description: Choose the debug level for Syslog messages in case of configuration
            issues <br /> Please make sure that it suits your Home Assistant logger
            default level.
          default: info
          selector:
            select:
              multiple: false
              mode: dropdown
              options:
              - critical
              - debug
              - error
              - info
              - warning
              sort: false
              custom_value: false
trigger_variables:
  blind: !input blind
  cover_type: !input cover_type
  is_awning: '{{ cover_type == ''awning'' }}'
  resident_sensor: !input resident_sensor
  position_source: !input position_source
  custom_position_sensor: !input custom_position_sensor
  open_position: !input open_position
  close_position: !input close_position
  ventilate_position: !input ventilate_position
  shading_position: !input shading_position
  position_tolerance: !input position_tolerance
  open_tilt_position: !input open_tilt_position
  close_tilt_position: !input close_tilt_position
  ventilate_tilt_position: !input ventilate_tilt_position
  shading_tilt_elevation_1: !input shading_tilt_elevation_1
  shading_tilt_elevation_2: !input shading_tilt_elevation_2
  shading_tilt_elevation_3: !input shading_tilt_elevation_3
  shading_tilt_position_0: !input shading_tilt_position_0
  shading_tilt_position_1: !input shading_tilt_position_1
  shading_tilt_position_2: !input shading_tilt_position_2
  shading_tilt_position_3: !input shading_tilt_position_3
  auto_options: !input auto_options
  cover_tilt_config: !input cover_tilt_config
  cover_tilt_reposition_config: !input cover_tilt_reposition_config
  is_cover_tilt_enabled: '{{ not is_awning and ''cover_tilt_enabled'' in cover_tilt_config
    }}'
  tilt_wait_mode: !input cover_tilt_wait_mode
  tilt_wait_timeout: !input cover_tilt_wait_timeout
  is_tilt_wait_idle_mode: '{{ tilt_wait_mode == ''wait_idle'' }}'
  time_up_early: !input time_up_early
  time_up_early_non_workday: !input time_up_early_non_workday
  time_up_late: !input time_up_late
  time_up_late_non_workday: !input time_up_late_non_workday
  time_down_early: !input time_down_early
  time_down_early_non_workday: !input time_down_early_non_workday
  time_down_late: !input time_down_late
  time_down_late_non_workday: !input time_down_late_non_workday
  workday_sensor_today: !input workday_sensor
  workday_sensor_tomorrow: !input workday_sensor_tomorrow
  time_control: !input time_control
  calendar_entity: !input calendar_entity
  calendar_open_title: !input calendar_open_title
  calendar_close_title: !input calendar_close_title
  calendar_open_title_lc: '{{ (calendar_open_title | default('''')) | lower | trim
    }}'
  calendar_close_title_lc: '{{ (calendar_close_title | default('''')) | lower | trim
    }}'
  is_calendar_enabled: '{{ ''time_control_calendar'' in time_control and calendar_entity
    != [] }}'
  default_brightness_sensor: !input default_brightness_sensor
  brightness_up: !input brightness_up
  brightness_down: !input brightness_down
  brightness_hysteresis: !input brightness_hysteresis
  default_sun_sensor: !input default_sun_sensor
  sun_elevation_mode: !input sun_elevation_mode
  sun_elevation_up: !input sun_elevation_up
  sun_elevation_down: !input sun_elevation_down
  sun_elevation_up_sensor: !input sun_elevation_up_sensor
  sun_elevation_down_sensor: !input sun_elevation_down_sensor
  contact_window_opened: !input contact_window_opened
  contact_window_tilted: !input contact_window_tilted
  lockout_tilted_options: !input lockout_tilted_options
  shading_azimuth_start: !input shading_azimuth_start
  shading_azimuth_end: !input shading_azimuth_end
  shading_elevation_min: !input shading_elevation_min
  shading_elevation_max: !input shading_elevation_max
  shading_sun_brightness_start: !input shading_sun_brightness_start
  shading_sun_brightness_end: !input shading_sun_brightness_end
  shading_sun_brightness_hysteresis: !input shading_sun_brightness_hysteresis
  shading_brightness_sensor: !input shading_brightness_sensor
  shading_temperatur_sensor1: !input shading_temperatur_sensor1
  shading_temperatur_sensor2: !input shading_temperatur_sensor2
  shading_min_temperatur1: !input shading_min_temperatur1
  shading_min_temperatur2: !input shading_min_temperatur2
  shading_temperature_hysteresis1: !input shading_temperature_hysteresis1
  shading_temperature_hysteresis2: !input shading_temperature_hysteresis2
  shading_forecast_sensor: !input shading_forecast_sensor
  shading_forecast_temp_sensor: !input shading_forecast_temp_sensor
  shading_forecast_type: !input shading_forecast_type
  shading_forecast_temp: !input shading_forecast_temp
  shading_forecast_temp_hysteresis: !input shading_forecast_temp_hysteresis
  shading_weather_conditions: !input shading_weather_conditions
  shading_conditions_start_and: !input shading_conditions_start_and
  shading_conditions_start_or: !input shading_conditions_start_or
  shading_conditions_end_and: !input shading_conditions_end_and
  shading_conditions_end_or: !input shading_conditions_end_or
  lockout_tilted_when_closing: '{{ ''lockout_tilted_closing'' in lockout_tilted_options
    }}'
  lockout_tilted_when_shading_starts: '{{ ''lockout_tilted_shading_start'' in lockout_tilted_options
    }}'
  lockout_tilted_when_shading_ends: '{{ ''lockout_tilted_shading_end'' in lockout_tilted_options
    }}'
  is_shading_enabled: '{{ ''auto_shading_enabled'' in auto_options }}'
  is_up_enabled: '{{ ''auto_up_enabled'' in auto_options }}'
  is_down_enabled: '{{ ''auto_down_enabled'' in auto_options }}'
  is_brightness_enabled: '{{ ''auto_brightness_enabled'' in auto_options }}'
  is_sun_elevation_enabled: '{{ ''auto_sun_enabled'' in auto_options }}'
  is_time_field_enabled: '{{ ''time_control_input'' in time_control }}'
  is_time_control_disabled: '{{ ''time_control_disabled'' in time_control }}'
  is_ventilation_enabled: '{{ not is_awning and ''auto_ventilate_enabled'' in auto_options
    }}'
  reset_override_config: !input reset_override_config
  reset_override_time: !input reset_override_time
  reset_override_timeout: !input reset_override_timeout
  is_reset_disabled: '{{ ''reset_disabled'' in reset_override_config }}'
  is_reset_fixed_time: '{{ ''reset_fixed_time'' in reset_override_config }}'
  is_reset_timeout: '{{ ''reset_timeout'' in reset_override_config }}'
  auto_up_force: !input auto_up_force
  auto_down_force: !input auto_down_force
  auto_ventilate_force: !input auto_ventilate_force
  auto_shading_start_force: !input auto_shading_start_force
  cover_status_options: !input cover_status_options
  cover_status_helper: !input cover_status_helper
  invalid_states:
  - ''
  - unavailable
  - unknown
  - none
  - None
  - 'null'
  - query failed
  - []
variables:
  version: 2026.01.26
  blind_entities: '{{ expand(blind) | map(attribute=''entity_id'') | list }}'
  current_position: "{% set source = position_source %} {% if source == 'current_position_attr'
    %}\n  {{ state_attr(blind, 'current_position') | int(default=101) }}\n{% elif
    source == 'position_attr' %}\n  {{ state_attr(blind, 'position') | int(default=101)
    }}\n{% elif source == 'custom_sensor' and custom_position_sensor != [] %}\n  {{
    states(custom_position_sensor) | int(default=101) }}\n{% else %}\n  {{ 101 }}\n{%
    endif %}"
  current_tilt_position: '{{ state_attr(blind, ''current_tilt_position'') | int(default=101)
    }}'
  position_comparisons:
    current_above_open: '{{ current_position < open_position if is_awning else current_position
      > open_position }}'
    current_above_close: '{{ current_position < close_position if is_awning else current_position
      > close_position }}'
    current_above_shading: '{{ current_position < shading_position if is_awning else
      current_position > shading_position }}'
    current_above_ventilate: '{{ current_position < ventilate_position if is_awning
      else current_position > ventilate_position }}'
    current_below_open: '{{ current_position > open_position if is_awning else current_position
      < open_position }}'
    current_below_close: '{{ current_position > close_position if is_awning else current_position
      < close_position }}'
    current_below_shading: '{{ current_position > shading_position if is_awning else
      current_position < shading_position }}'
    current_below_ventilate: '{{ current_position > ventilate_position if is_awning
      else current_position < ventilate_position }}'
    open_above_close: '{{ open_position < close_position if is_awning else open_position
      > close_position }}'
    open_above_ventilate: '{{ open_position < ventilate_position if is_awning else
      open_position > ventilate_position }}'
    shading_above_close: '{{ shading_position < close_position if is_awning else shading_position
      > close_position }}'
    shading_below_open: '{{ shading_position > open_position if is_awning else shading_position
      < open_position }}'
    ventilate_above_close: '{{ ventilate_position < close_position if is_awning else
      ventilate_position > close_position }}'
  current_sun_azimuth: '{{ state_attr(default_sun_sensor, ''azimuth'') }}'
  current_sun_elevation: '{{ state_attr(default_sun_sensor, ''elevation'') }}'
  sun_elevation_up_current: "{% set mode = sun_elevation_mode %} {% set sensor = sun_elevation_up_sensor
    %} {% set fixed_value = sun_elevation_up | float %} {% set sensor_valid = sensor
    != [] and states(sensor) not in invalid_states and is_number(states(sensor)) %}
    {% if mode == 'dynamic' %}\n  {% if sensor_valid %}\n    {{ states(sensor) | float
    }}\n  {% else %}\n    {{ fixed_value }}\n  {% endif %}\n{% elif mode == 'hybrid'
    %}\n  {% if sensor_valid %}\n    {{ (states(sensor) | float) + fixed_value }}\n
    \ {% else %}\n    {{ fixed_value }}\n  {% endif %}\n{% else %}\n  {{ fixed_value
    }}\n{% endif %}"
  sun_elevation_down_current: "{% set mode = sun_elevation_mode %} {% set sensor =
    sun_elevation_down_sensor %} {% set fixed_value = sun_elevation_down | float %}
    {% set sensor_valid = sensor != [] and states(sensor) not in invalid_states and
    is_number(states(sensor)) %} {% if mode == 'dynamic' %}\n  {% if sensor_valid
    %}\n    {{ states(sensor) | float }}\n  {% else %}\n    {{ fixed_value }}\n  {%
    endif %}\n{% elif mode == 'hybrid' %}\n  {% if sensor_valid %}\n    {{ (states(sensor)
    | float) + fixed_value }}\n  {% else %}\n    {{ fixed_value }}\n  {% endif %}\n{%
    else %}\n  {{ fixed_value }}\n{% endif %}"
  is_today_off: '{{ workday_sensor_today != [] and is_state(workday_sensor_today,
    ''off'') }}'
  is_tomorrow_off: '{{ workday_sensor_tomorrow != [] and is_state(workday_sensor_tomorrow,
    ''off'') }}'
  is_tomorrow_on: '{{ workday_sensor_tomorrow != [] and is_state(workday_sensor_tomorrow,
    ''on'') }}'
  is_cover_tilt_enabled_and_possible: '{{ is_cover_tilt_enabled and state_attr(blind,
    ''current_tilt_position'') != none }}'
  is_cover_tilt_reposition_enabled: '{{ ''cover_tilt_reposition_enabled'' in cover_tilt_reposition_config
    }}'
  drive_delay_fix: !input drive_delay_fix
  drive_delay_random: !input drive_delay_random
  drive_time: !input drive_time
  time_up_early_today: '{{ time_up_early_non_workday if is_today_off else time_up_early
    }}'
  time_up_late_today: '{{ time_up_late_non_workday if is_today_off else time_up_late
    }}'
  time_down_early_today: "{{\n  time_down_early_non_workday if is_tomorrow_off else\n
    \ time_down_early if is_tomorrow_on else\n  time_down_early_non_workday if is_today_off
    else\n  time_down_early\n}}"
  time_down_late_today: "{{\n  time_down_late_non_workday if is_tomorrow_off else\n
    \ time_down_late if is_tomorrow_on else\n  time_down_late_non_workday if is_today_off
    else\n  time_down_late\n}}"
  shading_tilt_position: "{% set elevation = current_sun_elevation | int(default=0)
    %} {% if elevation >= shading_tilt_elevation_3 | int %}\n  {{ shading_tilt_position_3
    | int }}\n{% elif elevation >= shading_tilt_elevation_2 | int %}\n  {{ shading_tilt_position_2
    | int }}\n{% elif elevation >= shading_tilt_elevation_1 | int %}\n  {{ shading_tilt_position_1
    | int }}\n{% else %}\n  {{ shading_tilt_position_0 | int }}\n{% endif %}"
  in_open_position: "{% set min = open_position - position_tolerance %} {% set max
    = open_position + position_tolerance %} {{\n  (current_position >= min and current_position
    <= max)\n  and (\n    (not is_cover_tilt_enabled_and_possible) or\n    (is_cover_tilt_enabled_and_possible
    and current_tilt_position == open_tilt_position | int)\n  )\n}}"
  in_close_position: "{% set min = close_position - position_tolerance %} {% set max
    = close_position + position_tolerance %} {{\n  (current_position >= min and current_position
    <= max)\n  and (\n    (not is_cover_tilt_enabled_and_possible) or\n    (is_cover_tilt_enabled_and_possible
    and current_tilt_position == close_tilt_position | int)\n  )\n}}"
  in_shading_position: "{% set min = shading_position - position_tolerance %} {% set
    max = shading_position + position_tolerance %} {{\n  is_shading_enabled\n  and
    (current_position >= min and current_position <= max)\n  and (\n    (not is_cover_tilt_enabled_and_possible)
    or\n    (current_tilt_position == shading_tilt_position | int)\n  )\n}}"
  in_ventilate_position: "{% set min = ventilate_position - position_tolerance %}
    {% set max = ventilate_position + position_tolerance %} {{\n  is_ventilation_enabled\n
    \ and (current_position >= min and current_position <= max)\n  and (\n    (not
    is_cover_tilt_enabled_and_possible) or\n    (current_tilt_position == ventilate_tilt_position
    | int)\n  )\n}}"
  brightness_time_duration: !input brightness_time_duration
  sun_time_duration: !input sun_time_duration
  shading_waitingtime_start: !input shading_waitingtime_start
  shading_waitingtime_end: !input shading_waitingtime_end
  shading_start_max_duration: !input shading_start_max_duration
  shading_end_max_duration: !input shading_end_max_duration
  shading_config: !input shading_config
  shading_end_immediate_by_sun_position: !input shading_end_immediate_by_sun_position
  is_shading_end_immediate_by_sun_position: '{{ shading_end_immediate_by_sun_position
    }}'
  forecast_source:
    use_sensor: '{{ shading_forecast_temp_sensor != [] }}'
    use_weather: '{{ shading_forecast_sensor != [] and shading_forecast_temp_sensor
      == [] }}'
    prevent_service: '{{ ''weather_attributes'' in shading_forecast_type }}'
  shading_start_condition_enabled:
    azimuth: '{{ ''cond_azimuth'' in shading_conditions_start_and or ''cond_azimuth''
      in shading_conditions_start_or }}'
    elevation: '{{ ''cond_elevation'' in shading_conditions_start_and or ''cond_elevation''
      in shading_conditions_start_or }}'
    brightness: '{{ ''cond_brightness'' in shading_conditions_start_and or ''cond_brightness''
      in shading_conditions_start_or }}'
    temp1: '{{ ''cond_temp1'' in shading_conditions_start_and or ''cond_temp1'' in
      shading_conditions_start_or }}'
    temp2: '{{ ''cond_temp2'' in shading_conditions_start_and or ''cond_temp2'' in
      shading_conditions_start_or }}'
    forecast_temp: '{{ ''cond_forecast_temp'' in shading_conditions_start_and or ''cond_forecast_temp''
      in shading_conditions_start_or }}'
    forecast_weather: '{{ ''cond_forecast_weather'' in shading_conditions_start_and
      or ''cond_forecast_weather'' in shading_conditions_start_or }}'
  shading_end_condition_enabled:
    azimuth: '{{ ''cond_azimuth'' in shading_conditions_end_and or ''cond_azimuth''
      in shading_conditions_end_or }}'
    elevation: '{{ ''cond_elevation'' in shading_conditions_end_and or ''cond_elevation''
      in shading_conditions_end_or }}'
    brightness: '{{ ''cond_brightness'' in shading_conditions_end_and or ''cond_brightness''
      in shading_conditions_end_or }}'
    temp1: '{{ ''cond_temp1'' in shading_conditions_end_and or ''cond_temp1'' in shading_conditions_end_or
      }}'
    temp2: '{{ ''cond_temp2'' in shading_conditions_end_and or ''cond_temp2'' in shading_conditions_end_or
      }}'
    forecast_temp: '{{ ''cond_forecast_temp'' in shading_conditions_end_and or ''cond_forecast_temp''
      in shading_conditions_end_or }}'
    forecast_weather: '{{ ''cond_forecast_weather'' in shading_conditions_end_and
      or ''cond_forecast_weather'' in shading_conditions_end_or }}'
  is_status_helper_enabled: "{{\n  'cover_helper_enabled' in cover_status_options
    and\n  cover_status_helper != [] and\n  states(cover_status_helper) not in invalid_states
    and\n  states(cover_status_helper) | regex_match(\"((\\[[^\\}]+)?\\{s*[^\\}\\{]{3,}?:.*\\}([^\\{]+\\])?)\")\n}}"
  helper_json: "{% if is_status_helper_enabled %}\n  {{ states(cover_status_helper)
    | from_json }}\n{% else %}\n  {{\n    {\n      'open': {'a': 0, 't': 0},\n      'close':
    {'a': 0, 't': 0},\n      'shading': {'a': 0, 't': 0, 'p': 0, 'q': 0},\n      'vpart':
    {'a': 0, 't': 0},\n      'vfull': {'a': 0, 't': 0},\n      'manual': {'a': 0,
    't': 0},\n      'v': 5,\n      't': as_timestamp(now()) | round(0)\n    } | to_json
    | from_json\n  }}\n{% endif %}\n"
  helper_status:
    open: '{{ (is_status_helper_enabled | default(false)) and (helper_json | regex_search(''open''))
      and (helper_json.open.a | default(false) | bool) }}'
    closed: '{{ (is_status_helper_enabled | default(false)) and (helper_json | regex_search(''close''))
      and (helper_json.close.a | default(false) | bool) }}'
    shaded: '{{ (is_status_helper_enabled | default(false)) and (helper_json | regex_search(''shading''))
      and (helper_json.shading.a | default(false) | bool) and not (helper_json.shading.p
      | default(false) | bool) }}'
    shading_start_pending: '{{ (is_status_helper_enabled | default(false)) and (helper_json
      | regex_search(''shading'')) and (helper_json.shading.p | default(false) | bool)
      }}'
    shading_end_pending: '{{ (is_status_helper_enabled | default(false)) and (helper_json
      | regex_search(''shading'')) and (helper_json.shading.q | default(false) | bool)
      }}'
    vpart: '{{ (is_status_helper_enabled | default(false)) and (helper_json | regex_search(''vpart''))
      and (helper_json.vpart.a | default(false) | bool) }}'
    vfull: '{{ (is_status_helper_enabled | default(false)) and (helper_json | regex_search(''vfull''))
      and (helper_json.vfull.a | default(false) | bool) }}'
    manual: '{{ (is_status_helper_enabled | default(false)) and (helper_json | regex_search(''manual''))
      and (helper_json.manual.a | default(false) | bool) }}'
  helper_ts:
    open: '{{ (helper_json.open.t | default(0)) if ((is_status_helper_enabled | default(false))
      and (helper_json | regex_search(''open''))) else 0 }}'
    closed: '{{ (helper_json.close.t | default(0)) if ((is_status_helper_enabled |
      default(false)) and (helper_json | regex_search(''close''))) else 0 }}'
    shaded: '{{ (helper_json.shading.t | default(0)) if ((is_status_helper_enabled
      | default(false)) and (helper_json | regex_search(''shading''))) else 0 }}'
    vpart: '{{ (helper_json.vpart.t | default(0)) if ((is_status_helper_enabled |
      default(false)) and (helper_json | regex_search(''vpart''))) else 0 }}'
    vfull: '{{ (helper_json.vfull.t | default(0)) if ((is_status_helper_enabled |
      default(false)) and (helper_json | regex_search(''vfull''))) else 0 }}'
    manual: '{{ (helper_json.manual.t | default(0)) if ((is_status_helper_enabled
      | default(false)) and (helper_json | regex_search(''manual''))) else 0 }}'
  is_cover_movement_blocked:
    open: '{{ (auto_down_force != [] and states(auto_down_force) in [''on'', ''true''])
      or (auto_ventilate_force != [] and states(auto_ventilate_force) in [''on'',
      ''true'']) or (auto_shading_start_force != [] and states(auto_shading_start_force)
      in [''on'', ''true'']) }}'
    close: '{{ (auto_up_force != [] and states(auto_up_force) in [''on'', ''true''])
      or (auto_ventilate_force != [] and states(auto_ventilate_force) in [''on'',
      ''true'']) or (auto_shading_start_force != [] and states(auto_shading_start_force)
      in [''on'', ''true'']) }}'
    ventilate: '{{ (auto_up_force != [] and states(auto_up_force) in [''on'', ''true''])
      or (auto_down_force != [] and states(auto_down_force) in [''on'', ''true''])
      or (auto_shading_start_force != [] and states(auto_shading_start_force) in [''on'',
      ''true'']) }}'
    shading: '{{ (auto_up_force != [] and states(auto_up_force) in [''on'', ''true''])
      or (auto_down_force != [] and states(auto_down_force) in [''on'', ''true''])
      or (auto_ventilate_force != [] and states(auto_ventilate_force) in [''on'',
      ''true'']) }}'
    any: '{{ (auto_up_force != [] and states(auto_up_force) in [''on'', ''true''])
      or (auto_down_force != [] and states(auto_down_force) in [''on'', ''true''])
      or (auto_ventilate_force != [] and states(auto_ventilate_force) in [''on'',
      ''true'']) or (auto_shading_start_force != [] and states(auto_shading_start_force)
      in [''on'', ''true'']) }}'
  resident_config: !input resident_config
  resident_flags:
    opening_enabled: '{{ ''resident_opening_enabled'' in resident_config }}'
    closing_enabled: '{{ ''resident_closing_enabled'' in resident_config }}'
    allow_shading: '{{ ''resident_allow_shading'' in resident_config }}'
    allow_opening: '{{ ''resident_allow_opening'' in resident_config }}'
    allow_ventilation: '{{ ''resident_allow_ventilation'' in resident_config }}'
  ignore_after_manual_config: !input ignore_after_manual_config
  override_flags:
    opening: '{{ ''ignore_opening_after_manual'' in ignore_after_manual_config }}'
    closing: '{{ ''ignore_closing_after_manual'' in ignore_after_manual_config }}'
    ventilation: '{{ ''ignore_ventilation_after_manual'' in ignore_after_manual_config
      }}'
    shading: '{{ ''ignore_shading_after_manual'' in ignore_after_manual_config }}'
  auto_ventilate_options: !input auto_ventilate_options
  contact_delay_trigger: !input contact_delay_trigger
  contact_delay_status: !input contact_delay_status
  ventilation_flags:
    delay_enabled: '{{ ''ventilation_delay_enabled'' in auto_ventilate_options }}'
    if_lower_enabled: '{{ ''ventilation_if_lower_enabled'' in auto_ventilate_options
      }}'
    after_shading_end: '{{ ''ventilation_after_shading_end'' in auto_ventilate_options
      }}'
  prevent_config: !input individual_config
  prevent_flags:
    higher_position_closing: '{{ ''prevent_higher_position_closing'' in prevent_config
      }}'
    lowering_when_closing_if_shaded: '{{ ''prevent_lowering_when_closing_if_shaded''
      in prevent_config }}'
    shading_end_if_closed: '{{ ''prevent_shading_end_if_closed'' in prevent_config
      }}'
    opening_after_shading_end: '{{ ''prevent_opening_after_shading_end'' in prevent_config
      }}'
    opening_after_ventilation_end: '{{ ''prevent_opening_after_ventilation_end'' in
      prevent_config }}'
    default_cover_actions: '{{ ''prevent_default_cover_actions'' in prevent_config
      }}'
    shading_multiple_times: '{{ ''prevent_shading_multiple_times'' in prevent_config
      }}'
    opening_multiple_times: '{{ ''prevent_opening_multiple_times'' in prevent_config
      }}'
    closing_multiple_times: '{{ ''prevent_closing_multiple_times'' in prevent_config
      }}'
  auto_recover_after_force: !input auto_recover_after_force
  is_auto_recover_enabled: '{{ ''auto_recover_enabled'' in auto_recover_after_force
    }}'
  check_config: !input check_config
  check_config_debuglevel: !input check_config_debuglevel
  check_status_helper_length: '{{ state_attr(cover_status_helper, ''max'') if cover_status_helper
    != [] else None }}'
mode: queued
max: 10
max_exceeded: warning
triggers:
- trigger: template
  value_template: '{% set is_today_off = workday_sensor_today != [] and is_state(workday_sensor_today,
    ''off'') %} {% set time_compare = time_up_early_non_workday if is_today_off else
    time_up_early %} {{ now() >= today_at(time_compare) }}'
  enabled: '{{ is_time_field_enabled }}'
  id: t_open_1
- trigger: template
  value_template: '{% set is_today_off = workday_sensor_today != [] and is_state(workday_sensor_today,
    ''off'') %} {% set early = time_up_early_non_workday if is_today_off else time_up_early
    %} {% set late = time_up_late_non_workday if is_today_off else time_up_late %}
    {{ early != late and now() >= today_at(late) }}'
  enabled: '{{ is_time_field_enabled }}'
  id: t_open_2
- trigger: template
  value_template: '{{ states(default_brightness_sensor) | float(default=brightness_up)
    > (brightness_up + brightness_hysteresis) }}'
  for:
    seconds: !input brightness_time_duration
  enabled: '{{ is_brightness_enabled and default_brightness_sensor != [] }}'
  id: t_open_4
- trigger: template
  value_template: "{% set mode = sun_elevation_mode %} {% set sensor = sun_elevation_up_sensor
    %} {% set fixed_value = sun_elevation_up | float %} {% set current_elevation =
    state_attr(default_sun_sensor, 'elevation') | float(0) %} {% set sensor_valid
    = sensor != [] and states(sensor) not in invalid_states and is_number(states(sensor))
    %} {% if mode == 'dynamic' %}\n  {% if sensor_valid %}\n    {% set threshold =
    states(sensor) | float %}\n  {% else %}\n    {% set threshold = fixed_value %}\n
    \ {% endif %}\n{% elif mode == 'hybrid' %}\n  {% if sensor_valid %}\n    {% set
    threshold = (states(sensor) | float) + fixed_value %}\n  {% else %}\n    {% set
    threshold = fixed_value %}\n  {% endif %}\n{% else %}\n  {% set threshold = fixed_value
    %}\n{% endif %} {{ current_elevation > threshold }}"
  for:
    seconds: !input sun_time_duration
  enabled: '{{ is_sun_elevation_enabled and default_sun_sensor != [] }}'
  id: t_open_5
- trigger: state
  entity_id: !input resident_sensor
  from: 'on'
  to: 'off'
  enabled: '{{ resident_sensor != [] }}'
  id: t_open_6
- trigger: state
  entity_id: !input auto_up_force
  from: 'off'
  to: 'on'
  enabled: '{{ auto_up_force != [] }}'
  id: t_force_enabled_open
- trigger: state
  entity_id: !input auto_up_force
  from: 'on'
  to: 'off'
  enabled: '{{ auto_up_force != [] }}'
  id: t_force_disabled_open
- trigger: template
  value_template: "{% set is_tomorrow_off = workday_sensor_tomorrow != [] and is_state(workday_sensor_tomorrow,
    'off') %} {% set is_tomorrow_on = workday_sensor_tomorrow != [] and is_state(workday_sensor_tomorrow,
    'on') %} {% set is_today_off = workday_sensor_today != [] and is_state(workday_sensor_today,
    'off') %} {% set time_compare =\n  time_down_early_non_workday if is_tomorrow_off
    else\n  time_down_early if is_tomorrow_on else\n  time_down_early_non_workday
    if is_today_off else\n  time_down_early %}\n{{ now() >= today_at(time_compare)
    }}"
  enabled: '{{ is_time_field_enabled }}'
  id: t_close_1
- trigger: template
  value_template: "{% set is_tomorrow_off = workday_sensor_tomorrow != [] and is_state(workday_sensor_tomorrow,
    'off') %} {% set is_tomorrow_on = workday_sensor_tomorrow != [] and is_state(workday_sensor_tomorrow,
    'on') %} {% set is_today_off = workday_sensor_today != [] and is_state(workday_sensor_today,
    'off') %} {% set time_compare =\n  time_down_late_non_workday if is_tomorrow_off
    else\n  time_down_late if is_tomorrow_on else\n  time_down_late_non_workday if
    is_today_off else\n  time_down_late %}\n{{ (time_down_early_non_workday != time_compare
    and time_down_early != time_compare) and now() >= today_at(time_compare) }}"
  enabled: '{{ is_time_field_enabled }}'
  id: t_close_2
- trigger: template
  value_template: '{{ states(default_brightness_sensor) | float(default=brightness_down)
    < (brightness_down - brightness_hysteresis) }}'
  for:
    seconds: !input brightness_time_duration
  enabled: '{{ is_brightness_enabled and default_brightness_sensor != [] }}'
  id: t_close_4
- trigger: template
  value_template: "{% set mode = sun_elevation_mode %} {% set sensor = sun_elevation_down_sensor
    %} {% set fixed_value = sun_elevation_down | float %} {% set current_elevation
    = state_attr(default_sun_sensor, 'elevation') | float(0) %} {% set sensor_valid
    = sensor != [] and states(sensor) not in invalid_states and is_number(states(sensor))
    %} {% if mode == 'dynamic' %}\n  {% if sensor_valid %}\n    {% set threshold =
    states(sensor) | float %}\n  {% else %}\n    {% set threshold = fixed_value %}\n
    \ {% endif %}\n{% elif mode == 'hybrid' %}\n  {% if sensor_valid %}\n    {% set
    threshold = (states(sensor) | float) + fixed_value %}\n  {% else %}\n    {% set
    threshold = fixed_value %}\n  {% endif %}\n{% else %}\n  {% set threshold = fixed_value
    %}\n{% endif %} {{ current_elevation < threshold }}"
  for:
    seconds: !input sun_time_duration
  enabled: '{{ is_sun_elevation_enabled and default_sun_sensor != [] }}'
  id: t_close_5
- trigger: state
  entity_id: !input resident_sensor
  from: 'off'
  to: 'on'
  enabled: '{{ resident_sensor != [] }}'
  id: t_close_6
- trigger: state
  entity_id: !input auto_down_force
  from: 'off'
  to: 'on'
  enabled: '{{ auto_down_force != [] }}'
  id: t_force_enabled_close
- trigger: state
  entity_id: !input auto_down_force
  from: 'on'
  to: 'off'
  enabled: '{{ auto_down_force != [] }}'
  id: t_force_disabled_close
- trigger: state
  entity_id: !input calendar_entity
  to: 'on'
  enabled: '{{ is_calendar_enabled }}'
  id: t_calendar_event_start
- trigger: state
  entity_id: !input calendar_entity
  to: 'off'
  enabled: '{{ is_calendar_enabled }}'
  id: t_calendar_event_end
- trigger: state
  entity_id: !input contact_window_tilted
  enabled: '{{ is_ventilation_enabled and contact_window_tilted != [] }}'
  id: t_contact_tilted_changed
  for:
    seconds: !input contact_delay_trigger
- trigger: state
  entity_id: !input contact_window_opened
  enabled: '{{ is_ventilation_enabled and contact_window_opened != [] }}'
  id: t_contact_opened_changed
  for:
    seconds: !input contact_delay_trigger
- trigger: state
  entity_id: !input auto_ventilate_force
  from: 'off'
  to: 'on'
  enabled: '{{ auto_ventilate_force != [] }}'
  id: t_force_enabled_ventilate
- trigger: state
  entity_id: !input auto_ventilate_force
  from: 'on'
  to: 'off'
  enabled: '{{ auto_ventilate_force != [] }}'
  id: t_force_disabled_ventilate
- trigger: template
  value_template: "{{\n  state_attr(default_sun_sensor, 'azimuth') | float(default=shading_azimuth_start)
    > shading_azimuth_start and\n  state_attr(default_sun_sensor, 'azimuth') | float(default=shading_azimuth_end)
    < shading_azimuth_end and\n  state_attr(default_sun_sensor, 'elevation') | float(default=shading_elevation_min)
    > shading_elevation_min and\n  state_attr(default_sun_sensor, 'elevation') | float(default=shading_elevation_max)
    < shading_elevation_max\n}}"
  enabled: '{{ is_shading_enabled and default_sun_sensor != [] }}'
  id: t_shading_start_pending_1
- trigger: template
  value_template: '{{ states(shading_brightness_sensor) not in invalid_states and
    states(shading_brightness_sensor) | float(default=shading_sun_brightness_start)
    > (shading_sun_brightness_start + shading_sun_brightness_hysteresis) }}'
  enabled: '{{ is_shading_enabled and shading_brightness_sensor != [] }}'
  id: t_shading_start_pending_2
- trigger: template
  value_template: '{{ states(shading_temperatur_sensor1) not in invalid_states and
    states(shading_temperatur_sensor1) | float(default=shading_min_temperatur1) >
    (shading_min_temperatur1 + shading_temperature_hysteresis1) }}'
  enabled: '{{ is_shading_enabled and shading_temperatur_sensor1 != [] }}'
  id: t_shading_start_pending_3
- trigger: template
  value_template: '{{ states(shading_temperatur_sensor2) not in invalid_states and
    states(shading_temperatur_sensor2) | float(default=shading_min_temperatur2) >
    (shading_min_temperatur2 + shading_temperature_hysteresis2) }}'
  enabled: '{{ is_shading_enabled and shading_temperatur_sensor2 != [] }}'
  id: t_shading_start_pending_4
- trigger: template
  value_template: '{{ states(shading_forecast_sensor) not in invalid_states and states(shading_forecast_sensor)
    in shading_weather_conditions }}'
  enabled: '{{ is_shading_enabled and shading_forecast_sensor != [] }}'
  id: t_shading_start_pending_5
- trigger: template
  value_template: '{{ states(shading_forecast_temp_sensor) not in invalid_states and
    states(shading_forecast_temp_sensor) | float(default=shading_forecast_temp) >
    (shading_forecast_temp + shading_forecast_temp_hysteresis) }}'
  enabled: '{{ is_shading_enabled and shading_forecast_temp_sensor != [] and shading_forecast_temp
    != [] }}'
  id: t_shading_start_pending_6
- trigger: template
  value_template: '{{ now() >= today_at([time_up_early, time_up_early_non_workday]
    | min) - timedelta(hours = 1) }}'
  enabled: '{{ is_shading_enabled }}'
  id: t_shading_start_pending_7
- trigger: template
  value_template: "{% set helper_state = states(cover_status_helper) %} {{\n  helper_state
    not in invalid_states and\n  helper_state | regex_match(\"((\\[[^\\}]+)?\\{s*[^\\}\\{]{3,}?:.*\\}([^\\{]+\\])?)\")
    and\n  helper_state | from_json | regex_search('shading') and\n  (helper_state
    | from_json).shading.p is defined and\n  (helper_state | from_json).shading.p
    > 0 and\n  now() >= ((helper_state | from_json).shading.p) | as_datetime | as_local\n}}"
  enabled: '{{ is_shading_enabled and cover_status_helper != [] }}'
  id: t_shading_start_execution
- trigger: state
  entity_id: !input auto_shading_start_force
  from: 'off'
  to: 'on'
  enabled: '{{ auto_shading_start_force != [] }}'
  id: t_force_enabled_shading
- trigger: state
  entity_id: !input auto_shading_start_force
  from: 'on'
  to: 'off'
  enabled: '{{ auto_shading_start_force != [] }}'
  id: t_force_disabled_shading
- trigger: template
  value_template: "{{\n  states(cover_status_helper) not in invalid_states and\n  states(cover_status_helper)
    | regex_search('\"shading\"\\s*:\\s*\\{[^}]*\"a\"\\s*:\\s*1') and\n  state_attr(default_sun_sensor,
    'elevation') < shading_tilt_elevation_1\n}}"
  enabled: '{{ is_shading_enabled and cover_status_helper != [] and is_cover_tilt_enabled
    and default_sun_sensor != [] }}'
  id: t_shading_tilt_1
- trigger: template
  value_template: "{{\n  states(cover_status_helper) not in invalid_states and\n  states(cover_status_helper)
    | regex_search('\"shading\"\\s*:\\s*\\{[^}]*\"a\"\\s*:\\s*1') and\n  state_attr(default_sun_sensor,
    'elevation') < shading_tilt_elevation_2 and\n  state_attr(default_sun_sensor,
    'elevation') >= shading_tilt_elevation_1\n}}"
  enabled: '{{ is_shading_enabled and cover_status_helper != [] and is_cover_tilt_enabled
    and default_sun_sensor != [] }}'
  id: t_shading_tilt_2
- trigger: template
  value_template: "{{\n  states(cover_status_helper) not in invalid_states and\n  states(cover_status_helper)
    | regex_search('\"shading\"\\s*:\\s*\\{[^}]*\"a\"\\s*:\\s*1') and\n  state_attr(default_sun_sensor,
    'elevation') < shading_tilt_elevation_3 and\n  state_attr(default_sun_sensor,
    'elevation') >= shading_tilt_elevation_2\n}}"
  enabled: '{{ is_shading_enabled and cover_status_helper != [] and is_cover_tilt_enabled
    and default_sun_sensor != [] }}'
  id: t_shading_tilt_3
- trigger: template
  value_template: "{{\n  states(cover_status_helper) not in invalid_states and\n  states(cover_status_helper)
    | regex_search('\"shading\"\\s*:\\s*\\{[^}]*\"a\"\\s*:\\s*1') and\n  state_attr(default_sun_sensor,
    'elevation') >= shading_tilt_elevation_3\n}}"
  enabled: '{{ is_shading_enabled and cover_status_helper != [] and is_cover_tilt_enabled
    and default_sun_sensor != [] }}'
  id: t_shading_tilt_4
- trigger: template
  value_template: '{{ states(shading_temperatur_sensor1) not in invalid_states and
    states(shading_temperatur_sensor1) | float(default=shading_min_temperatur1) <
    (shading_min_temperatur1 - shading_temperature_hysteresis1) }}'
  enabled: '{{ is_shading_enabled and cover_status_helper != [] and shading_temperatur_sensor1
    != [] }}'
  id: t_shading_end_pending_1
- trigger: template
  value_template: '{{ states(shading_temperatur_sensor2) not in invalid_states and
    states(shading_temperatur_sensor2) | float(default=shading_min_temperatur2) <
    (shading_min_temperatur2 - shading_temperature_hysteresis2) }}'
  enabled: '{{ is_shading_enabled and cover_status_helper != [] and shading_temperatur_sensor2
    != [] }}'
  id: t_shading_end_pending_2
- trigger: template
  value_template: '{{ states(shading_brightness_sensor) not in invalid_states and
    states(shading_brightness_sensor) | float(default=shading_sun_brightness_end)
    < (shading_sun_brightness_end - shading_sun_brightness_hysteresis) }}'
  enabled: '{{ is_shading_enabled and cover_status_helper != [] and shading_brightness_sensor
    != [] }}'
  id: t_shading_end_pending_3
- trigger: template
  value_template: '{{ states(shading_forecast_sensor) not in shading_weather_conditions
    }}'
  enabled: '{{ is_shading_enabled and cover_status_helper != [] and shading_weather_conditions
    != [] and shading_forecast_sensor != [] }}'
  id: t_shading_end_pending_4
- trigger: template
  value_template: "{{\n  (state_attr(default_sun_sensor, 'azimuth') | float(default=shading_azimuth_start)
    < shading_azimuth_start) or\n  (state_attr(default_sun_sensor, 'azimuth') | float(default=shading_azimuth_end)
    > shading_azimuth_end) or\n  (state_attr(default_sun_sensor, 'elevation') | float(default=shading_elevation_max)
    > shading_elevation_max) or\n  (state_attr(default_sun_sensor, 'elevation') |
    float(default=shading_elevation_min) < shading_elevation_min)\n}}"
  enabled: '{{ is_shading_enabled and cover_status_helper != [] and default_sun_sensor
    != [] }}'
  id: t_shading_end_pending_5
- trigger: template
  value_template: '{{ states(shading_forecast_temp_sensor) not in invalid_states and
    states(shading_forecast_temp_sensor) | float(default=shading_forecast_temp) <
    (shading_forecast_temp - shading_forecast_temp_hysteresis) }}'
  enabled: '{{ is_shading_enabled and cover_status_helper != [] and shading_forecast_temp_sensor
    != [] and shading_forecast_temp != [] }}'
  id: t_shading_end_pending_6
- trigger: template
  value_template: "{% set helper_state = states(cover_status_helper) %} {{\n  helper_state
    not in invalid_states and\n  helper_state | regex_match(\"((\\[[^\\}]+)?\\{s*[^\\}\\{]{3,}?:.*\\}([^\\{]+\\])?)\")
    and\n  helper_state | from_json | regex_search('shading') and\n  (helper_state
    | from_json).shading.q is defined and\n  (helper_state | from_json).shading.q
    > 0 and\n  now() >= ((helper_state | from_json).shading.q) | as_datetime | as_local\n}}"
  enabled: '{{ is_shading_enabled and cover_status_helper != [] }}'
  id: t_shading_end_execution
- trigger: template
  value_template: '{{ now() >= today_at(''23:55:00'') }}'
  id: t_shading_reset
  enabled: '{{ is_shading_enabled }}'
- trigger: state
  entity_id: !input blind
  attribute: current_position
  enabled: '{{ position_source == ''current_position_attr'' }}'
  id: t_manual_position
  for:
    seconds: 60
- trigger: state
  entity_id: !input blind
  attribute: position
  enabled: '{{ position_source == ''position_attr'' }}'
  id: t_manual_position
  for:
    seconds: 60
- trigger: state
  entity_id: !input custom_position_sensor
  enabled: '{{ position_source == ''custom_sensor'' and custom_position_sensor !=
    [] }}'
  id: t_manual_position
  for:
    seconds: 60
- trigger: state
  entity_id: !input blind
  attribute: current_tilt_position
  id: t_manual_tilt
  for:
    minutes: 1
  enabled: '{{ is_cover_tilt_enabled }}'
- trigger: template
  value_template: '{{ now() >= today_at(reset_override_time) }}'
  enabled: '{{ is_reset_fixed_time }}'
  id: t_reset_fixedtime
  for:
    minutes: 2
- trigger: template
  value_template: "{% set helper_state = states(cover_status_helper) %} {{\n  helper_state
    not in invalid_states and\n  helper_state | regex_match(\"((\\[[^\\}]+)?\\{s*[^\\}\\{]{3,}?:.*\\}([^\\{]+\\])?)\")
    and\n  helper_state | from_json | regex_search('manual') and\n  (helper_state
    | from_json).manual.t is defined and\n  (helper_state | from_json).manual.a is
    defined and\n  (helper_state | from_json).manual.a | bool is true and\n   now()
    >= ((helper_state | from_json).manual.t + 60 * reset_override_timeout) | as_datetime
    | as_local\n}}"
  enabled: '{{ is_reset_timeout and cover_status_helper != [] }}'
  id: t_reset_timeout
  for:
    seconds: 2
conditions:
- condition: !input auto_global_condition
- or:
  - '{{ trigger.to_state is not defined }}'
  - '{{ trigger.to_state is defined and trigger.to_state.state not in invalid_states
    }}'
- or:
  - '{{ trigger.id is not defined }}'
  - '{{ not trigger.id | regex_match(''^t_calendar_event'') }}'
  - and:
    - '{{ trigger.id | regex_match(''^t_calendar_event'') }}'
    - '{{ state_attr(calendar_entity, ''message'') is not none }}'
    - "{{\n  calendar_open_title_lc in (state_attr(calendar_entity, 'message') | lower)
      or\n  calendar_close_title_lc in (state_attr(calendar_entity, 'message') | lower)\n}}"
- condition: template
  value_template: "{% if is_status_helper_enabled %}\n  {% if trigger.id is match('^t_shading_start_pending_[1-6]$')
    %}\n    {{ states(cover_status_helper) | regex_search('\"shading\"\\s*:\\s*\\{[^}]*\"a\"\\s*:\\s*0')
    }}\n  {% elif trigger.id is match('^t_shading_end_pending_[1-6]$') %}\n    {{
    states(cover_status_helper) | regex_search('\"shading\"\\s*:\\s*\\{[^}]*\"a\"\\s*:\\s*1')
    }}\n  {% else %}\n    true\n  {% endif %}\n{% else %}\n  true\n{% endif %}"
actions:
- variables:
    cover_move_action: &id002
      sequence:
      - condition: template
        value_template: '{{ not is_cover_movement_blocked.any or trigger.id is match(''^t_force_'')
          }}'
      - condition: template
        value_template: '{{ not prevent_flags.default_cover_actions }}'
      - repeat:
          for_each: '{{ blind_entities | list }}'
          sequence:
          - choose:
            - conditions: '{{ target_position | default(101) | int == 0 }}'
              sequence:
              - service: cover.close_cover
                target:
                  entity_id: '{{ repeat.item if repeat is defined else '''' }}'
            - conditions: '{{ target_position | default(101) | int == 100 }}'
              sequence:
              - service: cover.open_cover
                target:
                  entity_id: '{{ repeat.item if repeat is defined else '''' }}'
            - conditions: '{{ 0 < target_position | default(101) | int < 100 }}'
              sequence:
              - service: cover.set_cover_position
                data:
                  position: '{{ target_position | default(101) | int }}'
                target:
                  entity_id: '{{ repeat.item if repeat is defined else '''' }}'
          - delay:
              seconds: '{{ range(1, 3) | random }}'
    tilt_move_action: &id003
      sequence:
      - condition: template
        value_template: '{{ not is_cover_movement_blocked.any or trigger.id is match(''^t_force_'')
          }}'
      - condition: template
        value_template: '{{ not prevent_flags.default_cover_actions and is_cover_tilt_enabled_and_possible
          }}'
      - repeat:
          for_each: '{{ blind_entities | list }}'
          sequence:
          - if: '{{ state_attr(repeat.item if repeat is defined else '''', ''current_tilt_position'')
              is defined }}'
            then:
            - if: '{{ tilt_first | default(false) }}'
              then:
              - service: cover.set_cover_tilt_position
                data:
                  tilt_position: 0
                target:
                  entity_id: '{{ repeat.item if repeat is defined else '''' }}'
              - delay:
                  seconds: !input tilt_delay
            - choose:
              - conditions: '{{ is_tilt_wait_idle_mode | default(false) }}'
                sequence:
                - wait_template: '{{ states(repeat.item if repeat is defined else
                    '''') in [''open'', ''closed''] }}'
                  timeout: '{{ tilt_wait_timeout | int }}'
                  continue_on_timeout: true
                - if: '{{ wait.completed if wait is defined else '''' }}'
                  then:
                  - delay: 1
              default:
              - delay:
                  seconds: !input tilt_delay
            - service: cover.set_cover_tilt_position
              data:
                tilt_position: '{{ target_tilt_position | default(101) }}'
              target:
                entity_id: '{{ repeat.item if repeat is defined else '''' }}'
            - delay:
                seconds: '{{ range(1, 3) | random }}'
    helper_update: &id001
      if:
      - '{{ is_status_helper_enabled }}'
      then:
      - service: input_text.set_value
        data:
          entity_id: !input cover_status_helper
          value: '{% set dict_var = helper_json %}

            {% set dict_new = dict(dict_var, **(update_values | default({})) ) %}

            {{ dict_new | to_json }}'
- if:
  - '{{ is_shading_enabled }}'
  - '{{ forecast_source.use_weather and not forecast_source.prevent_service }}'
  - '{{ states(shading_forecast_sensor) not in invalid_states }}'
  - '{{ trigger.id is defined }}'
  - '{{ trigger.id | regex_match(''^(t_shading_start|t_open_1|t_open_3)'') }}'
  then:
  - action: weather.get_forecasts
    target:
      entity_id: !input shading_forecast_sensor
    data:
      type: !input shading_forecast_type
    response_variable: weather_forecast
    continue_on_error: true
- if:
  - '{{ is_calendar_enabled }}'
  - '{{ calendar_entity != [] }}'
  - '{{ states(calendar_entity) not in invalid_states }}'
  - '{{ trigger.id is defined }}'
  - or:
    - '{{ trigger.id | regex_match(''^t_open'') }}'
    - '{{ trigger.id | regex_match(''^t_close'') }}'
    - '{{ trigger.id | regex_match(''^t_shading_start'') }}'
    - '{{ trigger.id | regex_match(''^t_calendar_event'') }}'
  then:
  - action: calendar.get_events
    target:
      entity_id: !input calendar_entity
    data:
      start_date_time: '{{ today_at(''00:00:00'').isoformat() }}'
      end_date_time: '{{ (today_at(''23:59:59'') + timedelta(seconds=1)).isoformat()
        }}'
    response_variable: calendar_events
    continue_on_error: true
- variables:
    forecast_temp_raw: "{% set sensor = shading_forecast_temp_sensor %} {% set weather
      = shading_forecast_sensor %} {% if sensor != [] and states(sensor) not in invalid_states
      %}\n  {{ states(sensor) | float(default=None) }}\n{% elif weather == [] or states(weather)
      in invalid_states %}\n  {{ None }}\n{% elif 'weather_attributes' in shading_forecast_type
      %}\n  {{ state_attr(weather, 'temperature') | float(default=None) }}\n{% elif
      weather_forecast is defined and weather in weather_forecast %}\n  {{ weather_forecast[weather].forecast[0].temperature
      | default(None) | float(default=None) }}\n{% else %}\n  {{ None }}\n{% endif
      %}"
    forecast_weather_condition_raw: "{% set weather = shading_forecast_sensor %} {%
      if weather == [] or states(weather) in invalid_states %}\n  {{ None }}\n{% elif
      'weather_attributes' in shading_forecast_type %}\n  {{ state_attr(weather, 'condition')
      }}\n{% elif weather_forecast is defined and weather in weather_forecast %}\n
      \ {{ weather_forecast[weather].forecast[0].condition | default(None) }}\n{%
      else %}\n  {{ None }}\n{% endif %}"
    shading_start_condition_states:
      azimuth_valid: "{{\n  not is_shading_enabled or\n  not shading_start_condition_enabled.azimuth
        or\n  default_sun_sensor == [] or\n  (\n    current_sun_azimuth | float(default=0)
        > shading_azimuth_start and\n    current_sun_azimuth | float(default=0) <
        shading_azimuth_end\n  )\n}}"
      elevation_valid: "{{\n  not is_shading_enabled or\n  not shading_start_condition_enabled.elevation
        or\n  default_sun_sensor == [] or\n  (\n    current_sun_elevation | float(default=0)
        > shading_elevation_min and\n    current_sun_elevation | float(default=0)
        < shading_elevation_max\n  )\n}}"
      brightness_valid: "{{\n  not is_shading_enabled or\n  not shading_start_condition_enabled.brightness
        or\n  shading_brightness_sensor == [] or\n  (\n    states(shading_brightness_sensor)
        not in invalid_states and\n    states(shading_brightness_sensor) | float(default=shading_sun_brightness_start)
        > (shading_sun_brightness_start + shading_sun_brightness_hysteresis)\n  )\n}}"
      temp1_valid: "{{\n  not is_shading_enabled or\n  not shading_start_condition_enabled.temp1
        or\n  shading_temperatur_sensor1 == [] or\n  (\n    states(shading_temperatur_sensor1)
        not in invalid_states and\n    states(shading_temperatur_sensor1) | float(default=shading_min_temperatur1)
        > (shading_min_temperatur1 + shading_temperature_hysteresis1)\n  )\n}}"
      temp2_valid: "{{\n  not is_shading_enabled or\n  not shading_start_condition_enabled.temp2
        or\n  shading_temperatur_sensor2 == [] or\n  (\n    states(shading_temperatur_sensor2)
        not in invalid_states and\n    states(shading_temperatur_sensor2) | float(default=shading_min_temperatur2)
        > (shading_min_temperatur2 + shading_temperature_hysteresis2)\n  )\n}}"
      forecast_temp_valid: "{{\n  not is_shading_enabled or\n  not shading_start_condition_enabled.forecast_temp
        or\n  shading_forecast_temp == [] or\n  (\n    forecast_temp_raw is not none
        and\n    forecast_temp_raw > (shading_forecast_temp + shading_forecast_temp_hysteresis)\n
        \ ) or\n  (\n    'shading_compare_forecast_with_sensor2' in shading_config
        and\n    shading_temperatur_sensor2 != [] and\n    shading_forecast_temp !=
        [] and\n    (states(shading_temperatur_sensor2) | float(default=0) > (shading_forecast_temp
        + shading_forecast_temp_hysteresis))\n  )\n}}"
      forecast_weather_valid: "{{\n  not is_shading_enabled or\n  not shading_start_condition_enabled.forecast_weather
        or\n  shading_weather_conditions == [] or\n  forecast_source.use_sensor or\n
        \ (\n    forecast_weather_condition_raw is not none and\n    forecast_weather_condition_raw
        not in invalid_states and\n    forecast_weather_condition_raw in shading_weather_conditions\n
        \ )\n}}"
    shading_start_and_result: "{{\n  (\n    ('cond_azimuth' not in shading_conditions_start_and
      or\n    default_sun_sensor == [] or\n    shading_start_condition_states.azimuth_valid)\n
      \   and\n    ('cond_elevation' not in shading_conditions_start_and or\n    default_sun_sensor
      == [] or\n    shading_start_condition_states.elevation_valid)\n    and\n    ('cond_brightness'
      not in shading_conditions_start_and or\n    shading_brightness_sensor == []
      or\n    shading_start_condition_states.brightness_valid)\n    and\n    ('cond_temp1'
      not in shading_conditions_start_and or\n    shading_temperatur_sensor1 == []
      or\n    shading_start_condition_states.temp1_valid)\n    and\n    ('cond_temp2'
      not in shading_conditions_start_and or\n    shading_temperatur_sensor2 == []
      or\n    shading_start_condition_states.temp2_valid)\n    and\n    ('cond_forecast_temp'
      not in shading_conditions_start_and or\n    shading_forecast_temp == [] or\n
      \   shading_start_condition_states.forecast_temp_valid)\n    and\n    ('cond_forecast_weather'
      not in shading_conditions_start_and or\n    shading_weather_conditions == []
      or\n    shading_start_condition_states.forecast_weather_valid)\n  )\n}}"
    shading_start_or_result: "{{\n  (shading_conditions_start_or | count == 0) or\n
      \ (\n    ('cond_azimuth' in shading_conditions_start_or and\n    default_sun_sensor
      != [] and\n    shading_start_condition_states.azimuth_valid)\n    or\n    ('cond_elevation'
      in shading_conditions_start_or and\n    default_sun_sensor != [] and\n    shading_start_condition_states.elevation_valid)\n
      \   or\n    ('cond_brightness' in shading_conditions_start_or and\n    shading_brightness_sensor
      != [] and\n    shading_start_condition_states.brightness_valid)\n    or\n    ('cond_temp1'
      in shading_conditions_start_or and\n    shading_temperatur_sensor1 != [] and\n
      \   shading_start_condition_states.temp1_valid)\n    or\n    ('cond_temp2' in
      shading_conditions_start_or and\n    shading_temperatur_sensor2 != [] and\n
      \   shading_start_condition_states.temp2_valid)\n    or\n    ('cond_forecast_temp'
      in shading_conditions_start_or and\n    shading_forecast_temp != [] and\n    shading_start_condition_states.forecast_temp_valid)\n
      \   or\n    ('cond_forecast_weather' in shading_conditions_start_or and\n    shading_weather_conditions
      != [] and\n    shading_start_condition_states.forecast_weather_valid)\n  )\n}}"
    shading_start_conditions_met: "{{\n  is_shading_enabled and\n  shading_start_and_result
      and\n  shading_start_or_result\n}}"
    shading_end_condition_states:
      azimuth_invalid: "{{\n  is_shading_enabled and\n  shading_end_condition_enabled.azimuth
        and\n  default_sun_sensor != [] and\n  (\n    current_sun_azimuth | float(default=999)
        <= shading_azimuth_start or\n    current_sun_azimuth | float(default=0) >=
        shading_azimuth_end\n  )\n}}"
      elevation_invalid: "{{\n  is_shading_enabled and\n  shading_end_condition_enabled.elevation
        and\n  default_sun_sensor != [] and\n  (\n    current_sun_elevation | float(default=999)
        <= shading_elevation_min or\n    current_sun_elevation | float(default=0)
        >= shading_elevation_max\n  )\n}}"
      brightness_invalid: "{{\n  is_shading_enabled and\n  shading_end_condition_enabled.brightness
        and\n  shading_brightness_sensor != [] and\n  states(shading_brightness_sensor)
        not in invalid_states and\n  states(shading_brightness_sensor) | float(default=999999)
        < (shading_sun_brightness_end - shading_sun_brightness_hysteresis)\n}}"
      temp1_invalid: "{{\n  is_shading_enabled and\n  shading_end_condition_enabled.temp1
        and\n  shading_temperatur_sensor1 != [] and\n  states(shading_temperatur_sensor1)
        not in invalid_states and\n  states(shading_temperatur_sensor1) | float(default=999)
        < (shading_min_temperatur1 - shading_temperature_hysteresis1)\n}}"
      temp2_invalid: "{{\n  is_shading_enabled and\n  shading_end_condition_enabled.temp2
        and\n  shading_temperatur_sensor2 != [] and\n  states(shading_temperatur_sensor2)
        not in invalid_states and\n  states(shading_temperatur_sensor2) | float(default=999)
        < (shading_min_temperatur2 - shading_temperature_hysteresis2)\n}}"
      forecast_temp_invalid: "{{\n  is_shading_enabled and\n  shading_end_condition_enabled.forecast_temp
        and\n  shading_forecast_temp != [] and\n  forecast_temp_raw is not none and\n
        \ forecast_temp_raw < (shading_forecast_temp - shading_forecast_temp_hysteresis)\n}}"
      forecast_weather_invalid: "{{\n  is_shading_enabled and\n  shading_end_condition_enabled.forecast_weather
        and\n  shading_weather_conditions != [] and\n  (\n    forecast_weather_condition_raw
        is none or\n    forecast_weather_condition_raw in invalid_states or\n    forecast_weather_condition_raw
        not in shading_weather_conditions\n  )\n}}"
    shading_end_and_result: "{{\n  shading_conditions_end_and | count > 0 and\n  (\n
      \   ('cond_azimuth' not in shading_conditions_end_and or shading_end_condition_states.azimuth_invalid)\n
      \   and\n    ('cond_elevation' not in shading_conditions_end_and or shading_end_condition_states.elevation_invalid)\n
      \   and\n    ('cond_brightness' not in shading_conditions_end_and or shading_end_condition_states.brightness_invalid)\n
      \   and\n    ('cond_temp1' not in shading_conditions_end_and or shading_end_condition_states.temp1_invalid)\n
      \   and\n    ('cond_temp2' not in shading_conditions_end_and or shading_end_condition_states.temp2_invalid)\n
      \   and\n    ('cond_forecast_temp' not in shading_conditions_end_and or shading_end_condition_states.forecast_temp_invalid)\n
      \   and\n    ('cond_forecast_weather' not in shading_conditions_end_and or shading_end_condition_states.forecast_weather_invalid)\n
      \ )\n}}"
    shading_end_or_result: "{{\n  shading_conditions_end_or | count > 0 and\n  (\n
      \   shading_end_condition_states.azimuth_invalid or\n    shading_end_condition_states.elevation_invalid
      or\n    shading_end_condition_states.brightness_invalid or\n    shading_end_condition_states.temp1_invalid
      or\n    shading_end_condition_states.temp2_invalid or\n    shading_end_condition_states.forecast_temp_invalid
      or\n    shading_end_condition_states.forecast_weather_invalid\n  )\n}}"
    shading_end_conditions_met: "{{\n  is_shading_enabled and\n  (\n    shading_end_and_result
      or\n    shading_end_or_result\n  )\n}}"
    calendar_open_event: "{% set result = namespace(found=false, start=none, end=none)
      %} {% if is_calendar_enabled and calendar_events is defined and calendar_entity
      in calendar_events %}\n  {% set events = calendar_events[calendar_entity].events
      | default([]) %}\n  {% for event in events %}\n    {% set summary = (event.summary
      | default('')) | lower | trim %}\n    {% if calendar_open_title_lc != '' and
      calendar_open_title_lc in summary %}\n      {% set result.found = true %}\n
      \     {% set result.start = event.start %}\n      {% set result.end = event.end
      %}\n    {% endif %}\n  {% endfor %}\n{% endif %} {{ {\n  'found': result.found,\n
      \ 'start': result.start,\n  'end': result.end\n} }}"
    calendar_close_event: "{% set result = namespace(found=false, start=none, end=none)
      %} {% if is_calendar_enabled and calendar_events is defined and calendar_entity
      in calendar_events %}\n  {% set events = calendar_events[calendar_entity].events
      | default([]) %}\n  {% for event in events %}\n    {% set summary = (event.summary
      | default('')) | lower | trim %}\n    {% if calendar_close_title_lc != '' and
      calendar_close_title_lc in summary %}\n      {% set result.found = true %}\n
      \     {% set result.start = event.start %}\n      {% set result.end = event.end
      %}\n    {% endif %}\n  {% endfor %}\n{% endif %} {{ {\n  'found': result.found,\n
      \ 'start': result.start,\n  'end': result.end\n} }}"
    now_ts: '{{ as_timestamp(now()) }}'
    calendar_open_start: "{% if calendar_open_event.found and calendar_open_event.start
      is not none %}\n  {{ as_timestamp(calendar_open_event.start) }}\n{% else %}\n
      \ {{ none }}\n{% endif %}"
    calendar_open_end: "{% if calendar_open_event.found and calendar_open_event.end
      is not none %}\n  {{ as_timestamp(calendar_open_event.end) }}\n{% else %}\n
      \ {{ none }}\n{% endif %}"
    calendar_close_start: "{% if calendar_close_event.found and calendar_close_event.start
      is not none %}\n  {{ as_timestamp(calendar_close_event.start) }}\n{% else %}\n
      \ {{ none }}\n{% endif %}"
    calendar_close_end: "{% if calendar_close_event.found and calendar_close_event.end
      is not none %}\n  {{ as_timestamp(calendar_close_event.end) }}\n{% else %}\n
      \ {{ none }}\n{% endif %}"
    is_opening_phase: "{{\n  (\n    is_time_field_enabled and\n    now() >= today_at(time_up_early_today)
      and\n    now() < today_at(time_up_late_today)\n  ) or\n  (\n    is_calendar_enabled
      and\n    calendar_open_start is not none and\n    calendar_open_end is not none
      and\n    now_ts >= calendar_open_start and\n    now_ts < calendar_open_end\n
      \ )\n}}"
    is_daytime_phase: "{{\n  is_time_control_disabled or\n  (\n    is_time_field_enabled
      and\n    now() >= today_at(time_up_early_today) and\n    now() < today_at(time_down_early_today))
      or\n  (\n    is_calendar_enabled and\n    calendar_open_start is not none and\n
      \   calendar_close_start is not none and\n    now_ts >= calendar_open_start
      and\n    now_ts < calendar_close_start)\n}}"
    is_closing_phase: "{{\n  (\n    is_time_field_enabled and\n    now() >= today_at(time_down_early_today)
      and\n    now() < today_at(time_down_late_today)\n  ) or\n  (\n    is_calendar_enabled
      and\n    calendar_close_start is not none and\n    calendar_close_end is not
      none and\n    now_ts >= calendar_close_start and\n    now_ts < calendar_close_end\n
      \ )\n}}"
    is_evening_phase: "{{\n  not is_time_control_disabled and\n  (\n    (\n      is_time_field_enabled
      and\n      now() >= today_at(time_down_early_today)\n    ) or\n    (\n      is_calendar_enabled
      and\n      calendar_close_start is not none and\n      now_ts >= calendar_close_start\n
      \   )\n  )\n}}"
    is_time_up_late: "{{\n  (\n    is_time_field_enabled and\n    now() >= today_at(time_up_late_today)
      and\n    now() < today_at(time_down_early_today)\n  ) or\n  (\n    is_calendar_enabled
      and\n    calendar_open_end is not none and\n    calendar_close_start is not
      none and\n    now_ts >= calendar_open_end and\n    now_ts < calendar_close_start\n
      \ )\n}}"
    is_time_down_late: "{{\n  (\n    is_time_field_enabled and\n    now() >= today_at(time_down_late_today)\n
      \ ) or\n  (\n    is_calendar_enabled and\n    calendar_close_end is not none
      and\n    now_ts >= calendar_close_end\n  )\n}}"
    is_shading_allowed_window: "{{\n  is_time_control_disabled or\n  (\n    is_time_field_enabled
      and\n    now() >= today_at(time_up_early_today) and\n    now() <= today_at(time_down_late_today)
      + timedelta(seconds = 5)\n  ) or\n  (\n    is_calendar_enabled and\n    calendar_open_start
      is not none and\n    calendar_close_end is not none and\n    now_ts >= calendar_open_start
      and\n    now_ts <= calendar_close_end\n  )\n}}"
    environment_allows_opening: "{{\n  (\n    not is_brightness_enabled or\n    default_brightness_sensor
      == [] or\n    states(default_brightness_sensor) | float(default=brightness_up)
      > (brightness_up + brightness_hysteresis)\n  ) and\n  (\n    not is_sun_elevation_enabled
      or\n    default_sun_sensor == [] or\n    current_sun_elevation | float(default=sun_elevation_up_current)
      > sun_elevation_up_current\n  )\n}}"
    environment_allows_closing: "{{\n  (\n    is_brightness_enabled and\n    (\n      default_brightness_sensor
      == [] or\n      states(default_brightness_sensor) | float(default=brightness_down)
      < (brightness_down - brightness_hysteresis)\n    )\n  ) or\n  (\n    is_sun_elevation_enabled
      and\n    (\n      default_sun_sensor == [] or\n      current_sun_elevation |
      float(default=sun_elevation_down_current) < sun_elevation_down_current\n    )\n
      \ )\n}}"
    should_be_open_now: "{{\n  is_daytime_phase and\n  not is_evening_phase and\n
      \ (\n    (not is_brightness_enabled and not is_sun_elevation_enabled) or\n    environment_allows_opening\n
      \ )\n}}"
    should_be_closed_now: "{{\n  is_evening_phase and\n  (\n    (not is_brightness_enabled
      and not is_sun_elevation_enabled) or\n    environment_allows_closing\n  )\n}}"
- if:
  - '{{ ''cover_helper_enabled'' in cover_status_options }}'
  - '{{ cover_status_helper != [] }}'
  - or:
    - '{{ states(cover_status_helper) in invalid_states }}'
    - '{{ not states(cover_status_helper) | regex_match("((\[[^\}]+)?\{s*[^\}\{]{3,}?:.*\}([^\{]+\])?)")
      }}'
  then:
  - action: input_text.set_value
    data:
      entity_id: !input cover_status_helper
      value: '{{ helper_json | to_json }}'
- if:
  - '{{ is_status_helper_enabled }}'
  - '{{ helper_json.v != 5 }}'
  then:
  - variables:
      ts_now: '{{ as_timestamp(now()) | round(0) }}'
  - variables:
      update_values:
        open:
          a: '{{ helper_status.open | int | default(0) }}'
          t: '{{ helper_ts.open | default(ts_now) }}'
        close:
          a: '{{ helper_status.closed | int | default(0) }}'
          t: '{{ helper_ts.closed | default(ts_now) }}'
        shading:
          a: '{{ helper_status.shaded | int | default(0) }}'
          t: '{{ helper_ts.shaded | default(ts_now) }}'
          p: 0
          q: 0
        vpart:
          a: '{{ helper_status.vpart | int | default(0) }}'
          t: '{{ helper_ts.vpart | default(ts_now) }}'
        vfull:
          a: '{{ helper_status.vfull | int | default(0) }}'
          t: '{{ helper_ts.vfull | default(ts_now) }}'
        manual:
          a: '{{ helper_status.manual | int | default(0) }}'
          t: '{{ helper_ts.manual | default(ts_now) }}'
        v: 5
        t: '{{ helper_json.t | round(0) }}'
  - *id001
- choose:
  - alias: Check for opening
    conditions:
    - '{{ is_up_enabled }}'
    - '{{ trigger.id is defined }}'
    - '{{ trigger.id | regex_match(''^(t_open|t_calendar_event)'') }}'
    - condition: !input auto_up_condition
    - or:
      - '{{ is_auto_recover_enabled }}'
      - '{{ not is_cover_movement_blocked.open }}'
    - or:
      - '{{ is_status_helper_enabled and not helper_status.open }}'
      - '{{ not in_open_position }}'
    - '{{ not (is_status_helper_enabled and helper_status.manual and override_flags.opening)
      }}'
    - or:
      - '{{ not is_status_helper_enabled }}'
      - '{{ not prevent_flags.opening_multiple_times }}'
      - '{{ is_status_helper_enabled and prevent_flags.opening_multiple_times and
        ((now().day != helper_ts.open|timestamp_custom(''%-d'')|int) or helper_ts.manual
        <= helper_ts.open) }}'
    - or:
      - '{{ trigger.id != ''t_open_6'' }}'
      - and:
        - '{{ trigger.id == ''t_open_6'' }}'
        - '{{ resident_flags.opening_enabled }}'
        - '{{ should_be_open_now }}'
    - or:
      - '{{ resident_sensor == [] }}'
      - '{{ states(resident_sensor) in [''false'', ''off''] }}'
      - and:
        - '{{ resident_flags.allow_opening }}'
        - '{{ states(resident_sensor) in [''true'', ''on''] }}'
    - or:
      - '{{ is_time_control_disabled }}'
      - and:
        - '{{ is_time_up_late }}'
      - and:
        - '{{ is_opening_phase or is_daytime_phase }}'
        - '{{ not is_evening_phase }}'
        - '{{ environment_allows_opening }}'
    sequence:
    - delay:
        seconds: '{{ range(drive_delay_fix|int(0), drive_delay_fix|int(0) + drive_delay_random|int(0)
          +1) | random }}'
    - choose:
      - alias: Shading detected. Move to shading position
        conditions:
        - '{{ is_status_helper_enabled }}'
        - or:
          - '{{ helper_status.shaded }}'
          - '{{ helper_status.shading_start_pending }}'
        - '{{ not in_shading_position }}'
        sequence:
        - choose: []
          default: !input auto_shading_start_action_before
        - variables:
            target_position: !input shading_position
            target_tilt_position: '{{ shading_tilt_position | int }}'
            ts_now: '{{ as_timestamp(now()) | round(0) }}'
            update_values:
              open:
                a: 1
                t: '{{ ts_now }}'
              close:
                a: 0
                t: '{{ helper_json.close.t }}'
              shading:
                a: 1
                t: '{{ helper_json.shading.t }}'
                p: 0
                q: 0
              vpart:
                a: 0
                t: '{{ helper_json.vpart.t }}'
              vfull:
                a: 0
                t: '{{ helper_json.vfull.t }}'
              manual:
                a: 0
                t: '{{ helper_json.manual.t }}'
              t: '{{ ts_now }}'
        - if: '{{ not is_cover_movement_blocked.shading }}'
          then:
          - *id002
          - *id003
          - choose: []
            default: !input auto_shading_start_action
        - if:
          - '{{ is_auto_recover_enabled or not is_cover_movement_blocked.shading }}'
          then:
          - *id001
        - stop: 'Opening skipped: Shading detected'
      - alias: Normal opening of the cover
        conditions:
        - '{{ not in_shading_position }}'
        - or:
          - '{{ is_status_helper_enabled and not helper_status.open }}'
          - '{{ not in_open_position }}'
        sequence:
        - choose: []
          default: !input auto_up_action_before
        - variables:
            target_position: !input open_position
            target_tilt_position: '{{ open_tilt_position | int }}'
            ts_now: '{{ as_timestamp(now()) | round(0) }}'
            update_values:
              open:
                a: 1
                t: '{{ ts_now }}'
              close:
                a: 0
                t: '{{ helper_json.close.t }}'
              shading:
                a: 0
                t: '{{ helper_json.shading.t }}'
                p: 0
                q: 0
              vpart:
                a: 0
                t: '{{ helper_json.vpart.t }}'
              vfull:
                a: 0
                t: '{{ helper_json.vfull.t }}'
              manual:
                a: 0
                t: '{{ helper_json.manual.t }}'
              t: '{{ ts_now }}'
        - if: '{{ not is_cover_movement_blocked.open }}'
          then:
          - *id002
          - *id003
          - choose: []
            default: !input auto_up_action
        - if:
          - '{{ is_auto_recover_enabled or not is_cover_movement_blocked.open }}'
          then:
          - *id001
        - stop: Opening executed
  - alias: Check for closing cover
    conditions:
    - '{{ is_down_enabled }}'
    - '{{ trigger.id is defined }}'
    - '{{ trigger.id | regex_match(''^(t_close|t_calendar_event)'') }}'
    - condition: !input auto_down_condition
    - or:
      - '{{ is_auto_recover_enabled }}'
      - '{{ not is_cover_movement_blocked.close }}'
    - or:
      - '{{ is_status_helper_enabled and not helper_status.closed }}'
      - '{{ not in_close_position }}'
    - '{{ not (is_status_helper_enabled and helper_status.manual and override_flags.closing)
      }}'
    - or:
      - '{{ not is_status_helper_enabled }}'
      - '{{ not prevent_flags.closing_multiple_times }}'
      - '{{ is_status_helper_enabled and prevent_flags.closing_multiple_times and
        ((helper_ts.closed < today_at(time_down_early_today) | as_timestamp) or helper_ts.manual
        <= helper_ts.closed) }}'
    - or:
      - '{{ is_time_control_disabled }}'
      - and:
        - '{{ is_time_down_late }}'
      - and:
        - '{{ trigger.id == ''t_close_6'' }}'
        - '{{ resident_sensor != [] }}'
        - '{{ states(resident_sensor) in [''true'', ''on''] }}'
        - '{{ resident_flags.closing_enabled }}'
      - and:
        - '{{ is_closing_phase or is_evening_phase }}'
        - '{{ environment_allows_closing }}'
    sequence:
    - choose:
      - alias: Lockout protection when closing
        conditions:
        - '{{ is_ventilation_enabled }}'
        - '{{ is_status_helper_enabled }}'
        - or:
          - and:
            - '{{ contact_window_opened != [] }}'
            - '{{ states(contact_window_opened) in [''true'', ''on''] }}'
          - and:
            - '{{ lockout_tilted_when_closing }}'
            - '{{ contact_window_tilted != [] }}'
            - '{{ states(contact_window_tilted) in [''true'', ''on''] }}'
        sequence:
        - variables:
            ts_now: '{{ as_timestamp(now()) | round(0) }}'
            update_values:
              open:
                a: 0
                t: '{{ helper_json.open.t }}'
              close:
                a: 1
                t: '{{ ts_now }}'
              shading:
                a: 0
                t: '{{ helper_json.shading.t }}'
                p: 0
                q: 0
              vpart:
                a: 0
                t: '{{ helper_json.vpart.t }}'
              vfull:
                a: 1
                t: '{{ ts_now }}'
              manual:
                a: 0
                t: '{{ helper_json.manual.t }}'
              t: '{{ ts_now }}'
        - *id001
        - stop: 'Closing skipped: Lockout enabled'
      - alias: Window tilted. No lockout. Move to ventilation position instead of
          closing
        conditions:
        - '{{ is_ventilation_enabled }}'
        - '{{ is_status_helper_enabled }}'
        - '{{ contact_window_tilted != [] }}'
        - '{{ states(contact_window_tilted) in [''true'', ''on''] }}'
        - '{{ not lockout_tilted_when_closing }}'
        - or:
          - '{{ contact_window_opened == [] }}'
          - '{{ states(contact_window_opened) in [''false'', ''off''] }}'
        sequence:
        - choose: []
          default: !input auto_ventilate_action_before
        - delay:
            seconds: '{{ range(drive_delay_fix|int(0), drive_delay_fix|int(0) + drive_delay_random|int(0)
              +1) | random }}'
        - variables:
            target_position: !input ventilate_position
            target_tilt_position: !input ventilate_tilt_position
            ts_now: '{{ as_timestamp(now()) | round(0) }}'
            update_values:
              open:
                a: 0
                t: '{{ helper_json.open.t }}'
              close:
                a: 1
                t: '{{ ts_now }}'
              shading:
                a: 0
                t: '{{ helper_json.shading.t }}'
                p: 0
                q: 0
              vpart:
                a: 1
                t: '{{ ts_now }}'
              vfull:
                a: 0
                t: '{{ helper_json.vfull.t }}'
              manual:
                a: 0
                t: '{{ helper_json.manual.t }}'
              t: '{{ ts_now }}'
        - if: '{{ not is_cover_movement_blocked.ventilate }}'
          then:
          - *id002
          - *id003
          - choose: []
            default: !input auto_ventilate_action
        - if:
          - '{{ is_auto_recover_enabled or not is_cover_movement_blocked.ventilate
            }}'
          then:
          - *id001
        - stop: 'Closing skipped: Window tilted (Ventilation activated)'
      - alias: Only status change if cover is already 'near' the close position
        conditions:
        - or:
          - and:
            - '{{ is_status_helper_enabled }}'
            - '{{ prevent_flags.lowering_when_closing_if_shaded }}'
            - '{{ helper_status.shaded }}'
            - '{{ not position_comparisons.shading_above_close }}'
          - and:
            - '{{ prevent_flags.lowering_when_closing_if_shaded }}'
            - '{{ in_shading_position }}'
            - '{{ not position_comparisons.shading_above_close }}'
          - and:
            - '{{ prevent_flags.higher_position_closing }}'
            - '{{ position_comparisons.current_below_close or current_position ==
              close_position }}'
        sequence:
        - variables:
            ts_now: '{{ as_timestamp(now()) | round(0) }}'
            update_values:
              open:
                a: 0
                t: '{{ helper_json.open.t }}'
              close:
                a: 1
                t: '{{ ts_now }}'
              shading:
                a: 0
                t: '{{ helper_json.shading.t }}'
                p: 0
                q: 0
              vpart:
                a: 0
                t: '{{ helper_json.vpart.t }}'
              vfull:
                a: 0
                t: '{{ helper_json.vfull.t }}'
              t: '{{ ts_now }}'
        - *id001
        - stop: 'Closing skipped: Cover is already at or near targeted position (or
            lowering blocked by shading settings)'
      default:
      - choose: []
        default: !input auto_down_action_before
      - alias: Normal closing of the cover
        delay:
          seconds: '{{ range(drive_delay_fix|int(0), drive_delay_fix|int(0) + drive_delay_random|int(0)
            +1) | random }}'
      - variables:
          target_position: !input close_position
          target_tilt_position: !input close_tilt_position
          ts_now: '{{ as_timestamp(now()) | round(0) }}'
          update_values:
            open:
              a: 0
              t: '{{ helper_json.open.t }}'
            close:
              a: 1
              t: '{{ ts_now }}'
            shading:
              a: 0
              t: '{{ helper_json.shading.t }}'
              p: 0
              q: 0
            vpart:
              a: 0
              t: '{{ helper_json.vpart.t }}'
            vfull:
              a: 0
              t: '{{ helper_json.vfull.t }}'
            manual:
              a: 0
              t: '{{ helper_json.manual.t }}'
            t: '{{ ts_now }}'
      - if: '{{ not is_cover_movement_blocked.close }}'
        then:
        - *id002
        - *id003
        - choose: []
          default: !input auto_down_action
      - if:
        - '{{ is_auto_recover_enabled or not is_cover_movement_blocked.close }}'
        then:
        - *id001
      - stop: Closing executed
  - alias: Check for shading start
    conditions:
    - '{{ is_shading_enabled }}'
    - '{{ trigger.id is defined }}'
    - '{{ trigger.id | regex_match(''^(t_shading_start|t_open_1|t_open_3)'') }}'
    - '{{ is_status_helper_enabled }}'
    - condition: !input auto_shading_start_condition
    - or:
      - '{{ is_auto_recover_enabled }}'
      - '{{ not is_cover_movement_blocked.shading }}'
    - or:
      - '{{ not helper_status.shaded }}'
      - '{{ not (helper_status.vpart or helper_status.vfull) }}'
    - or:
      - '{{ not is_cover_tilt_enabled_and_possible and not in_shading_position }}'
      - '{{ is_cover_tilt_enabled_and_possible }}'
    - '{{ not (helper_status.manual and override_flags.shading) }}'
    - or:
      - '{{ not prevent_flags.shading_multiple_times }}'
      - '{{ prevent_flags.shading_multiple_times and ((now().day != helper_ts.shaded|timestamp_custom(''%-d'')
        | int) or helper_ts.manual <= helper_ts.shaded) }}'
      - '{{ trigger.id | regex_match(''^(t_shading_start_execution)'') }}'
    sequence:
    - if:
      - or:
        - and:
          - '{{ ''shading_temp_comparison_independent'' in shading_config }}'
          - '{{ shading_start_condition_states.forecast_temp_valid }}'
        - '{{ shading_start_conditions_met }}'
      then:
      - choose:
        - alias: Shading detected. Save next execution time and pending status
          conditions:
          - '{{ trigger.id | regex_match(''^(t_shading_start_pending)'') }}'
          - '{{ not helper_status.shading_start_pending }}'
          sequence:
          - variables:
              local_waitingtime_start: '{{ shading_waitingtime_start | int }}'
              ts_now: '{{ as_timestamp(now()) | round(0) }}'
              update_values:
                shading:
                  a: 1
                  t: '{{ ts_now }}'
                  p: '{{ (ts_now + local_waitingtime_start) | round(0) }}'
                  q: 0
                t: '{{ ts_now }}'
          - *id001
          - stop: 'Shading Start Pending: Waiting'
        - alias: Consider lockout protection when shading starts
          conditions:
          - '{{ trigger.id | regex_match(''^(t_shading_start_execution)'') }}'
          - '{{ helper_status.shading_start_pending }}'
          - '{{ is_ventilation_enabled }}'
          - or:
            - and:
              - '{{ contact_window_opened != [] }}'
              - '{{ states(contact_window_opened) in [''true'', ''on''] }}'
            - and:
              - '{{ lockout_tilted_when_shading_starts }}'
              - '{{ contact_window_tilted != [] }}'
              - '{{ states(contact_window_tilted) in [''true'', ''on''] }}'
          sequence:
          - variables:
              ts_now: '{{ as_timestamp(now()) | round(0) }}'
              update_values:
                open:
                  a: 1
                  t: '{{ ts_now }}'
                close:
                  a: 0
                  t: '{{ helper_json.close.t }}'
                shading:
                  a: 1
                  t: '{{ ts_now }}'
                  p: 0
                  q: 0
                vpart:
                  a: 0
                  t: '{{ helper_json.vpart.t }}'
                vfull:
                  a: 1
                  t: '{{ ts_now }}'
                manual:
                  a: 0
                  t: '{{ helper_json.manual.t }}'
                t: '{{ ts_now }}'
          - *id001
          - stop: 'Shading Start skipped: Lockout enabled'
        - alias: Start Shading
          conditions:
          - '{{ trigger.id | regex_match(''^(t_shading_start_execution)'') }}'
          - '{{ helper_status.shading_start_pending }}'
          - or:
            - '{{ is_cover_movement_blocked.any and is_auto_recover_enabled }}'
            - and:
              - '{{ not is_cover_tilt_enabled_and_possible }}'
              - '{{ position_comparisons.current_above_shading }}'
            - and:
              - '{{ is_cover_tilt_enabled_and_possible }}'
              - '{{ position_comparisons.current_above_shading or current_position
                == shading_position }}'
          - or:
            - '{{ is_shading_allowed_window }}'
          - or:
            - '{{ resident_sensor == [] }}'
            - '{{ states(resident_sensor) in [''false'', ''off''] }}'
            - and:
              - '{{ resident_flags.allow_shading }}'
              - '{{ states(resident_sensor) in [''true'', ''on''] }}'
          sequence:
          - delay:
              seconds: '{{ range(drive_delay_fix | int(0), drive_delay_fix | int(0)
                + drive_delay_random | int(0) +1) | random }}'
          - choose: []
            default: !input auto_shading_start_action_before
          - variables:
              target_position: !input shading_position
              target_tilt_position: '{{ shading_tilt_position | int }}'
              ts_now: '{{ as_timestamp(now()) | round(0) }}'
              update_values:
                open:
                  a: 1
                  t: '{{ ts_now }}'
                close:
                  a: 0
                  t: '{{ helper_json.close.t }}'
                shading:
                  a: 1
                  t: '{{ ts_now }}'
                  p: 0
                  q: 0
                vpart:
                  a: 0
                  t: '{{ helper_json.vpart.t }}'
                vfull:
                  a: 0
                  t: '{{ helper_json.vfull.t }}'
                manual:
                  a: 0
                  t: '{{ helper_json.manual.t }}'
                t: '{{ ts_now }}'
          - if: '{{ not is_cover_movement_blocked.shading }}'
            then:
            - *id002
            - *id003
            - choose: []
              default: !input auto_shading_start_action
          - if:
            - '{{ is_auto_recover_enabled or not is_cover_movement_blocked.shading
              }}'
            then:
            - *id001
          - stop: Shading Start executed
        - alias: Save shading state for the future
          conditions:
          - '{{ trigger.id | regex_match(''^(t_shading_start_execution)'') }}'
          - '{{ helper_status.shading_start_pending }}'
          - '{{ helper_status.closed }}'
          - '{{ not is_cover_movement_blocked.any }}'
          sequence:
          - variables:
              ts_now: '{{ as_timestamp(now()) | round(0) }}'
              update_values:
                shading:
                  a: 1
                  t: '{{ ts_now }}'
                  p: 0
                  q: 0
                t: '{{ ts_now }}'
          - if:
            - '{{ not is_cover_movement_blocked.any }}'
            then:
            - *id003
          - *id001
          - stop: 'Shading Start skipped: State saved'
      else:
      - choose:
        - alias: Shading start conditions not met - check if we should continue
          conditions:
          - '{{ trigger.id | regex_match(''^(t_shading_start_execution)'') }}'
          - '{{ helper_status.shading_start_pending }}'
          - '{{ shading_start_max_duration > 0 }}'
          - or:
            - '{{ is_shading_allowed_window }}'
          - '{{ (as_timestamp(now()) - helper_json.shading.t) <= shading_start_max_duration
            }}'
          sequence:
          - variables:
              local_waitingtime_start: '{{ shading_waitingtime_start | int }}'
              ts_now: '{{ as_timestamp(now()) | round(0) }}'
              update_values:
                shading:
                  a: 1
                  t: '{{ helper_json.shading.t }}'
                  p: '{{ (ts_now + local_waitingtime_start) | round(0) }}'
                  q: 0
                t: '{{ ts_now }}'
          - *id001
          - stop: 'Shading Start Pending: Continue waiting'
        - alias: Shading start / Stop retry
          conditions:
          - '{{ trigger.id | regex_match(''^(t_shading_start_execution)'') }}'
          - '{{ helper_status.shading_start_pending }}'
          sequence:
          - variables:
              ts_now: '{{ as_timestamp(now()) | round(0) }}'
              update_values:
                shading:
                  a: 0
                  t: '{{ ts_now }}'
                  p: 0
                  q: 0
                t: '{{ ts_now}}'
          - *id001
          - stop: 'Shading Start aborted: Timeout or invalid'
  - alias: Check for shading tilt
    conditions:
    - '{{ is_shading_enabled }}'
    - '{{ trigger.id is defined }}'
    - '{{ trigger.id | regex_match(''^(t_shading_tilt)'') }}'
    - '{{ is_status_helper_enabled }}'
    - and:
      - '{{ helper_status.shaded }}'
      - '{{ not helper_status.shading_start_pending }}'
    - '{{ is_cover_tilt_enabled_and_possible }}'
    - condition: !input auto_shading_tilt_condition
    - '{{ not is_cover_movement_blocked.shading }}'
    - '{{ not (helper_status.vpart or helper_status.vfull) }}'
    - '{{ not (helper_status.manual and override_flags.shading) }}'
    sequence:
    - variables:
        target_tilt_position: '{{ shading_tilt_position | int }}'
        tilt_first: '{{ is_cover_tilt_reposition_enabled | bool }}'
        update_values:
          t: '{{ as_timestamp(now()) | round(0) }}'
    - *id003
    - *id001
    - stop: Shading Tilt executed
  - alias: Check for shading end
    conditions:
    - '{{ is_shading_enabled }}'
    - '{{ trigger.id is defined }}'
    - '{{ trigger.id | regex_match(''^(t_shading_end)'') }}'
    - '{{ is_status_helper_enabled }}'
    - condition: !input auto_shading_end_condition
    - or:
      - '{{ is_auto_recover_enabled }}'
      - '{{ not is_cover_movement_blocked.any }}'
    - or:
      - '{{ is_shading_allowed_window }}'
    - '{{ not (helper_status.manual and override_flags.shading) }}'
    - '{{ not helper_status.closed }}'
    - or:
      - '{{ helper_status.shaded }}'
      - '{{ in_shading_position }}'
    - or:
      - '{{ not prevent_flags.shading_end_if_closed }}'
      - '{{ prevent_flags.shading_end_if_closed and not in_close_position}}'
    - or:
      - '{{ resident_sensor == [] }}'
      - '{{ states(resident_sensor) in [''false'', ''off''] }}'
    sequence:
    - if:
      - '{{ shading_end_conditions_met }}'
      then:
      - choose:
        - alias: Shading end detected. Save next execution time and pending status
          conditions:
          - '{{ trigger.id | regex_match(''^(t_shading_end_pending)'') }}'
          - '{{ not helper_status.shading_end_pending }}'
          sequence:
          - variables:
              local_waitingtime_end: '{% set local_waitingtime_end = shading_waitingtime_end
                | int %} {% if (is_shading_end_immediate_by_sun_position | default(false))
                | bool and trigger.id == ''t_shading_end_pending_5'' %} {# Almost
                immediately end shading when sun position out of range. Earlier triggers
                do not work. #} {% set local_waitingtime_end = 20 %} {% endif %} {{
                local_waitingtime_end }}

                '
              ts_now: '{{ as_timestamp(now()) | round(0) }}'
              update_values:
                shading:
                  a: 1
                  t: '{{ ts_now }}'
                  p: 0
                  q: '{{ (ts_now + (local_waitingtime_end | int)) | round(0) }}'
                t: '{{ ts_now }}'
          - *id001
          - stop: 'Shading End Pending: Waiting'
        - alias: Only tilt open after shading ends
          conditions:
          - '{{ trigger.id | regex_match(''^(t_shading_end_execution)'') }}'
          - '{{ helper_status.shading_end_pending }}'
          - '{{ is_cover_tilt_enabled_and_possible }}'
          - '{{ prevent_flags.opening_after_shading_end}}'
          sequence:
          - variables:
              target_tilt_position: 50
              ts_now: '{{ as_timestamp(now()) | round(0) }}'
              update_values:
                shading:
                  a: 0
                  t: '{{ ts_now }}'
                  p: 0
                  q: 0
                t: '{{ ts_now }}'
          - if:
            - '{{ not is_cover_movement_blocked.any }}'
            then:
            - *id003
          - *id001
          - stop: 'Shading End: Tilt executed'
        - alias: Lockout protection when shading ends
          conditions:
          - '{{ trigger.id | regex_match(''^(t_shading_end_execution)'') }}'
          - '{{ helper_status.shading_end_pending }}'
          - '{{ is_ventilation_enabled }}'
          - '{{ position_comparisons.current_below_ventilate }}'
          - or:
            - and:
              - '{{ contact_window_opened != [] }}'
              - '{{ states(contact_window_opened) in [''true'', ''on''] }}'
            - and:
              - '{{ contact_window_tilted != [] }}'
              - '{{ states(contact_window_tilted) in [''true'', ''on''] }}'
              - '{{ lockout_tilted_when_shading_ends }}'
          sequence:
          - variables:
              ts_now: '{{ as_timestamp(now()) | round(0) }}'
              update_values:
                open:
                  a: 1
                  t: '{{ ts_now }}'
                close:
                  a: 0
                  t: '{{ helper_json.close.t }}'
                shading:
                  a: 0
                  t: '{{ helper_json.shading.t }}'
                  p: 0
                  q: 0
                vpart:
                  a: 0
                  t: '{{ helper_json.vpart.t }}'
                vfull:
                  a: 1
                  t: '{{ ts_now }}'
                manual:
                  a: 0
                  t: '{{ helper_json.manual.t }}'
                t: '{{ ts_now }}'
          - *id001
          - stop: 'Shading End skipped: Lockout enabled'
        - alias: Ventilation after shading ends
          conditions:
          - '{{ trigger.id | regex_match(''^(t_shading_end_execution)'') }}'
          - '{{ helper_status.shading_end_pending }}'
          - '{{ is_ventilation_enabled }}'
          - '{{ ventilation_flags.after_shading_end }}'
          - '{{ contact_window_tilted != [] }}'
          - '{{ states(contact_window_tilted) in [''true'', ''on''] }}'
          - '{{ not lockout_tilted_when_closing }}'
          - or:
            - '{{ contact_window_opened == [] }}'
            - '{{ states(contact_window_opened) in [''false'', ''off''] }}'
          - or:
            - '{{ position_comparisons.current_below_ventilate }}'
            - '{{ ventilation_flags.if_lower_enabled and position_comparisons.current_above_ventilate
              or current_position == ventilate_position }}'
          sequence:
          - delay:
              seconds: "{% if (is_shading_end_immediate_by_sun_position | default(false))
                | bool %}\n  {{ range(0, 2) | random }}\n{% else %}\n  {{ range(drive_delay_fix|int(0),
                drive_delay_fix|int(0) + drive_delay_random|int(0) +1) | random }}\n{%
                endif %}\n"
          - choose: []
            default: !input auto_ventilate_action_before
          - variables:
              target_position: !input ventilate_position
              target_tilt_position: !input ventilate_tilt_position
              ts_now: '{{ as_timestamp(now()) | round(0) }}'
              update_values:
                open:
                  a: 1
                  t: '{{ ts_now }}'
                close:
                  a: 0
                  t: '{{ helper_json.close.t }}'
                shading:
                  a: 0
                  t: '{{ helper_json.shading.t }}'
                  p: 0
                  q: 0
                vpart:
                  a: 1
                  t: '{{ ts_now }}'
                vfull:
                  a: 0
                  t: '{{ helper_json.vfull.t }}'
                manual:
                  a: 0
                  t: '{{ helper_json.manual.t }}'
                t: '{{ ts_now }}'
          - if: '{{ not is_cover_movement_blocked.any }}'
            then:
            - *id002
            - *id003
            - choose: []
              default: !input auto_ventilate_action
          - if:
            - '{{ is_auto_recover_enabled or not is_cover_movement_blocked.any }}'
            then:
            - *id001
          - stop: 'Shading End: Ventilation activated'
        - alias: Move cover after shading end - conditions still valid
          conditions:
          - '{{ trigger.id | regex_match(''^(t_shading_end_execution)'') }}'
          - '{{ helper_status.shading_end_pending }}'
          - or:
            - '{{ is_auto_recover_enabled }}'
            - or:
              - '{{ current_sun_azimuth > shading_azimuth_end }}'
              - '{{ current_sun_elevation > shading_elevation_max }}'
              - '{{ current_sun_elevation < shading_elevation_min }}'
              - '{{ shading_brightness_sensor != [] and states(shading_brightness_sensor)
                | float(default=999999) < (shading_sun_brightness_end - shading_sun_brightness_hysteresis)
                }}'
              - '{{ shading_temperatur_sensor1 != [] and states(shading_temperatur_sensor1)
                | float(default=999) < (shading_min_temperatur1 - shading_temperature_hysteresis1)
                }}'
              - '{{ shading_temperatur_sensor2 != [] and states(shading_temperatur_sensor2)
                | float(default=999) < (shading_min_temperatur2 - shading_temperature_hysteresis2)
                }}'
              - '{{ shading_end_condition_states.forecast_temp_invalid }}'
          sequence:
          - delay:
              seconds: "{% if (is_shading_end_immediate_by_sun_position | default(false))
                | bool %}\n  {{ range(0, 2) | random }}\n{% else %}\n  {{ range(drive_delay_fix|int(0),
                drive_delay_fix|int(0) + drive_delay_random|int(0) +1) | random }}\n{%
                endif %}\n"
          - choose: []
            default: !input auto_shading_end_action_before
          - if:
            - '{{ not prevent_flags.opening_after_shading_end}}'
            then:
            - variables:
                target_position: '{{ open_position }}'
                target_tilt_position: '{{ open_tilt_position | int }}'
                ts_now: '{{ as_timestamp(now()) | round(0) }}'
                update_values:
                  open:
                    a: 1
                    t: '{{ ts_now }}'
                  close:
                    a: 0
                    t: '{{ helper_json.close.t }}'
                  shading:
                    a: 0
                    t: '{{ helper_json.shading.t }}'
                    p: 0
                    q: 0
                  vpart:
                    a: 0
                    t: '{{ helper_json.vpart.t }}'
                  vfull:
                    a: 0
                    t: '{{ helper_json.vfull.t }}'
                  manual:
                    a: 0
                    t: '{{ helper_json.manual.t }}'
                  t: '{{ ts_now }}'
            - if: '{{ not is_cover_movement_blocked.any }}'
              then:
              - *id002
              - *id003
              - choose: []
                default: !input auto_shading_end_action
              - *id001
              else:
              - variables:
                  update_values:
                    shading:
                      a: 0
                      t: '{{ helper_json.shading.t }}'
                      p: 0
                      q: 0
                    t: '{{ as_timestamp(now()) | round(0) }}'
              - *id001
          - stop: Shading End (Opening after shading) executed
        - alias: Shading end conditions not met - check if we should continue
          conditions:
          - '{{ trigger.id | regex_match(''^(t_shading_end_execution)'') }}'
          - '{{ helper_status.shading_end_pending }}'
          - '{{ shading_end_max_duration > 0 }}'
          - or:
            - '{{ is_shading_allowed_window }}'
          - '{{ (as_timestamp(now()) - helper_json.shading.t) <= shading_end_max_duration
            }}'
          sequence:
          - variables:
              local_waitingtime_end: '{{ shading_waitingtime_end | int }}'
              ts_now: '{{ as_timestamp(now()) | round(0) }}'
              update_values:
                shading:
                  a: 1
                  t: '{{ helper_json.shading.t }}'
                  p: 0
                  q: '{{ (ts_now + local_waitingtime_end) | round(0) }}'
                t: '{{ ts_now }}'
          - *id001
          - stop: 'Shading End Pending: Continue waiting'
        - alias: Shading end / Stop retry
          conditions:
          - '{{ trigger.id | regex_match(''^(t_shading_end_execution)'') }}'
          - '{{ helper_status.shading_end_pending }}'
          sequence:
          - variables:
              update_values:
                shading:
                  a: 1
                  t: '{{ helper_json.shading.t }}'
                  p: 0
                  q: 0
                t: '{{ as_timestamp(now()) | round(0) }}'
          - *id001
          - stop: 'Shading End aborted: Timeout or invalid'
  - alias: Contact sensor state changed
    conditions:
    - '{{ is_ventilation_enabled }}'
    - '{{ trigger.id is defined }}'
    - '{{ trigger.id in [''t_contact_tilted_changed'', ''t_contact_opened_changed'']
      }}'
    - '{{ is_status_helper_enabled }}'
    - '{{ trigger.from_state.state not in invalid_states }}'
    - '{{ trigger.to_state.state not in invalid_states }}'
    - or:
      - '{{ resident_sensor == [] }}'
      - '{{ states(resident_sensor) in [''false'', ''off''] }}'
      - and:
        - '{{ resident_flags.allow_ventilation }}'
        - '{{ states(resident_sensor) in [''true'', ''on''] }}'
    sequence:
    - delay:
        seconds: !input contact_delay_status
    - variables:
        contact_opened_now: '{{ contact_window_opened != [] and states(contact_window_opened)
          in [''true'', ''on''] }}'
        contact_tilted_now: '{{ contact_window_tilted != [] and states(contact_window_tilted)
          in [''true'', ''on''] }}'
    - choose:
      - alias: Window opened - Full ventilation (lockout)
        conditions:
        - '{{ contact_opened_now }}'
        - '{{ not in_open_position }}'
        - or:
          - '{{ is_auto_recover_enabled }}'
          - '{{ not is_cover_movement_blocked.ventilate }}'
        - condition: !input auto_ventilate_condition
        sequence:
        - variables:
            target_position: !input open_position
            target_tilt_position: !input open_tilt_position
            ts_now: '{{ as_timestamp(now()) | round(0) }}'
            update_values:
              vpart:
                a: 0
                t: '{{ helper_json.vpart.t }}'
              vfull:
                a: 1
                t: '{{ ts_now }}'
              t: '{{ ts_now }}'
        - if: '{{ not is_cover_movement_blocked.open }}'
          then:
          - *id002
          - *id003
        - if:
          - or:
            - '{{ is_auto_recover_enabled and is_cover_movement_blocked.open }}'
            - '{{ not is_cover_movement_blocked.open }}'
          then:
          - *id001
        - stop: Ventilation Start (Full) executed
      - alias: Window tilted - Partial ventilation
        conditions:
        - '{{ contact_tilted_now }}'
        - '{{ not contact_opened_now }}'
        - or:
          - '{{ is_auto_recover_enabled }}'
          - '{{ not is_cover_movement_blocked.ventilate }}'
        - condition: !input auto_ventilate_condition
        - or:
          - '{{ position_comparisons.current_below_ventilate }}'
          - and:
            - '{{ is_cover_tilt_enabled_and_possible }}'
            - '{{ position_comparisons.current_below_ventilate or current_position
              == ventilate_position }}'
            - '{{ current_tilt_position <= ventilate_tilt_position }}'
          - '{{ ventilation_flags.if_lower_enabled and (position_comparisons.current_above_ventilate
            or current_position == ventilate_position) }}'
          - and:
            - '{{ is_status_helper_enabled }}'
            - '{{ helper_status.vfull }}'
            - '{{ position_comparisons.current_above_ventilate }}'
        sequence:
        - choose: []
          default: !input auto_ventilate_action_before
        - variables:
            target_position: !input ventilate_position
            target_tilt_position: !input ventilate_tilt_position
            ts_now: '{{ as_timestamp(now()) | round(0) }}'
            update_values:
              vpart:
                a: 1
                t: '{{ ts_now }}'
              vfull:
                a: 0
                t: '{{ helper_json.vfull.t }}'
              t: '{{ ts_now }}'
        - if: '{{ not is_cover_movement_blocked.ventilate }}'
          then:
          - *id002
          - *id003
          - choose: []
            default: !input auto_ventilate_action
        - if:
          - '{{ is_auto_recover_enabled or not is_cover_movement_blocked.ventilate
            }}'
          then:
          - *id001
        - stop: Ventilation Start (Partial) executed
      - alias: Window closed - Return to shading
        conditions:
        - '{{ not contact_opened_now and not contact_tilted_now }}'
        - '{{ helper_status.shaded }}'
        - or:
          - '{{ helper_status.vpart }}'
          - '{{ helper_status.vfull }}'
          - '{{ in_ventilate_position }}'
          - '{{ in_open_position }}'
        - or:
          - '{{ is_auto_recover_enabled }}'
          - '{{ not is_cover_movement_blocked.any }}'
        - '{{ not (helper_status.manual and override_flags.ventilation) }}'
        - condition: !input auto_ventilate_end_condition
        sequence:
        - if:
          - '{{ ventilation_flags.delay_enabled }}'
          then:
          - delay:
              seconds: '{{ range(drive_delay_fix|int(0), drive_delay_fix|int(0) +
                drive_delay_random|int(0) +1) | random }}'
        - choose: []
          default: !input auto_shading_start_action_before
        - variables:
            target_position: !input shading_position
            target_tilt_position: '{{ shading_tilt_position | int }}'
            ts_now: '{{ as_timestamp(now()) | round(0) }}'
            update_values:
              open:
                a: 1
                t: '{{ ts_now }}'
              close:
                a: 0
                t: '{{ helper_json.close.t }}'
              shading:
                a: 1
                t: '{{ ts_now }}'
                p: 0
                q: 0
              vpart:
                a: 0
                t: '{{ helper_json.vpart.t }}'
              vfull:
                a: 0
                t: '{{ helper_json.vfull.t }}'
              manual:
                a: 0
                t: '{{ helper_json.manual.t }}'
              t: '{{ ts_now }}'
        - *id002
        - *id003
        - choose: []
          default: !input auto_shading_start_action
        - *id001
        - stop: 'Ventilation End: Return to Shading executed'
      - alias: Window closed - Return to open position
        conditions:
        - '{{ not contact_opened_now and not contact_tilted_now }}'
        - '{{ helper_status.open }}'
        - '{{ not helper_status.shaded }}'
        - or:
          - '{{ helper_status.vpart }}'
          - '{{ helper_status.vfull }}'
          - '{{ in_ventilate_position }}'
          - '{{ in_open_position }}'
        - or:
          - '{{ is_auto_recover_enabled }}'
          - '{{ not is_cover_movement_blocked.any }}'
        - '{{ not (helper_status.manual and override_flags.ventilation) }}'
        - condition: !input auto_ventilate_end_condition
        sequence:
        - if:
          - '{{ ventilation_flags.delay_enabled }}'
          then:
          - delay:
              seconds: '{{ range(drive_delay_fix|int(0), drive_delay_fix|int(0) +
                drive_delay_random|int(0) +1) | random }}'
        - choose: []
          default: !input auto_up_action_before
        - variables:
            target_position: !input open_position
            target_tilt_position: !input open_tilt_position
            ts_now: '{{ as_timestamp(now()) | round(0) }}'
            update_values:
              open:
                a: 1
                t: '{{ ts_now }}'
              close:
                a: 0
                t: '{{ helper_json.close.t }}'
              shading:
                a: 0
                t: '{{ helper_json.shading.t }}'
                p: 0
                q: 0
              vpart:
                a: 0
                t: '{{ helper_json.vpart.t }}'
              vfull:
                a: 0
                t: '{{ helper_json.vfull.t }}'
              manual:
                a: 0
                t: '{{ helper_json.manual.t }}'
              t: '{{ ts_now }}'
        - if:
          - '{{ not prevent_flags.opening_after_ventilation_end }}'
          then:
          - *id002
          - *id003
          - choose: []
            default: !input auto_up_action
        - *id001
        - stop: 'Ventilation End: Return to Open executed'
      - alias: Window closed - Return to close position
        conditions:
        - '{{ not contact_opened_now and not contact_tilted_now }}'
        - '{{ helper_status.closed }}'
        - or:
          - '{{ helper_status.vpart }}'
          - '{{ helper_status.vfull }}'
          - '{{ in_ventilate_position }}'
          - '{{ in_open_position }}'
        - or:
          - '{{ is_auto_recover_enabled }}'
          - '{{ not is_cover_movement_blocked.any }}'
        - '{{ not (helper_status.manual and override_flags.ventilation) }}'
        - condition: !input auto_ventilate_end_condition
        sequence:
        - if:
          - '{{ ventilation_flags.delay_enabled }}'
          then:
          - delay:
              seconds: '{{ range(drive_delay_fix|int(0), drive_delay_fix|int(0) +
                drive_delay_random|int(0) +1) | random }}'
        - choose: []
          default: !input auto_down_action_before
        - variables:
            target_position: !input close_position
            target_tilt_position: !input close_tilt_position
            ts_now: '{{ as_timestamp(now()) | round(0) }}'
            update_values:
              open:
                a: 0
                t: '{{ helper_json.open.t }}'
              close:
                a: 1
                t: '{{ ts_now }}'
              shading:
                a: 0
                t: '{{ helper_json.shading.t }}'
                p: 0
                q: 0
              vpart:
                a: 0
                t: '{{ helper_json.vpart.t }}'
              vfull:
                a: 0
                t: '{{ helper_json.vfull.t }}'
              manual:
                a: 0
                t: '{{ helper_json.manual.t }}'
              t: '{{ ts_now }}'
        - *id002
        - *id003
        - choose: []
          default: !input auto_down_action
        - *id001
        - stop: 'Ventilation End: Return to Close executed'
  - alias: Forced opening of the cover
    conditions:
    - '{{ trigger.id is defined }}'
    - '{{ trigger.id == ''t_force_enabled_open'' }}'
    - '{{ not is_cover_movement_blocked.open }}'
    sequence:
    - choose: []
      default: !input auto_up_action_before
    - variables:
        target_position: !input open_position
        target_tilt_position: !input open_tilt_position
        ts_now: '{{ as_timestamp(now()) | round(0) }}'
        update_values:
          t: '{{ ts_now }}'
    - *id002
    - *id003
    - *id001
    - choose: []
      default: !input auto_up_action
    - stop: Force Open executed
  - alias: Forced closing of the cover
    conditions:
    - '{{ trigger.id is defined }}'
    - '{{ trigger.id == ''t_force_enabled_close'' }}'
    - '{{ not is_cover_movement_blocked.close }}'
    sequence:
    - choose: []
      default: !input auto_down_action_before
    - variables:
        target_position: !input close_position
        target_tilt_position: !input close_tilt_position
        ts_now: '{{ as_timestamp(now()) | round(0) }}'
        update_values:
          t: '{{ ts_now }}'
    - *id002
    - *id003
    - *id001
    - choose: []
      default: !input auto_down_action
    - stop: Force Close executed
  - alias: Forced ventilation of the cover
    conditions:
    - '{{ trigger.id is defined }}'
    - '{{ trigger.id == ''t_force_enabled_ventilate'' }}'
    - '{{ is_status_helper_enabled }}'
    - '{{ not is_cover_movement_blocked.ventilate }}'
    sequence:
    - choose: []
      default: !input auto_ventilate_action_before
    - variables:
        target_position: !input ventilate_position
        target_tilt_position: !input ventilate_tilt_position
        ts_now: '{{ as_timestamp(now()) | round(0) }}'
        update_values:
          t: '{{ ts_now }}'
    - *id002
    - *id003
    - *id001
    - choose: []
      default: !input auto_ventilate_action
    - stop: Force Ventilation executed
  - alias: Forced activating of the sun shading
    conditions:
    - '{{ trigger.id is defined }}'
    - '{{ trigger.id == ''t_force_enabled_shading'' }}'
    - '{{ is_status_helper_enabled }}'
    - '{{ not is_cover_movement_blocked.shading }}'
    sequence:
    - choose: []
      default: !input auto_shading_start_action_before
    - variables:
        target_position: !input shading_position
        target_tilt_position: '{{ shading_tilt_position |int }}'
        ts_now: '{{ as_timestamp(now()) | round(0) }}'
        update_values:
          t: '{{ ts_now }}'
    - *id002
    - *id003
    - *id001
    - choose: []
      default: !input auto_shading_start_action
    - stop: Force Shading executed
  - alias: Checking for manual position changes
    conditions:
    - '{{ trigger.id is defined }}'
    - '{{ trigger.id in [''t_manual_position'', ''t_manual_tilt''] }}'
    - '{{ this.attributes.current | int(99) == 0 }}'
    - '{{ ''cover_helper_enabled'' in cover_status_options }}'
    - '{{ cover_status_helper != [] }}'
    - '{{ as_timestamp(now()) > helper_json.t + drive_time | default(90) + 60 }}'
    - '{{ trigger.from_state is not none and trigger.to_state is not none }}'
    - '{{ trigger.from_state.state not in invalid_states }}'
    - '{{ trigger.to_state.state not in invalid_states }}'
    - or:
      - and:
        - '{{ trigger.id == ''t_manual_position'' }}'
        - '{{ position_source == ''custom_sensor'' }}'
        - '{{ trigger.from_state.state not in invalid_states }}'
        - '{{ trigger.to_state.state not in invalid_states }}'
        - '{{ is_number(trigger.from_state.state) }}'
        - '{{ is_number(trigger.to_state.state) }}'
        - '{{ trigger.from_state.state != trigger.to_state.state }}'
      - and:
        - '{{ trigger.id == ''t_manual_position'' }}'
        - '{{ position_source == ''current_position_attr'' }}'
        - '{{ trigger.attribute == ''current_position'' }}'
        - '{{ trigger.from_state.attributes.current_position not in invalid_states
          }}'
        - '{{ trigger.to_state.attributes.current_position not in invalid_states }}'
        - '{{ is_number(trigger.from_state.attributes.current_position) }}'
        - '{{ is_number(trigger.to_state.attributes.current_position) }}'
        - '{{ trigger.from_state.attributes.current_position != trigger.to_state.attributes.current_position
          }}'
      - and:
        - '{{ trigger.id == ''t_manual_position'' }}'
        - '{{ position_source == ''position_attr'' }}'
        - '{{ trigger.attribute == ''position'' }}'
        - '{{ trigger.from_state.attributes.position not in invalid_states }}'
        - '{{ trigger.to_state.attributes.position not in invalid_states }}'
        - '{{ is_number(trigger.from_state.attributes.position) }}'
        - '{{ is_number(trigger.to_state.attributes.position) }}'
        - '{{ trigger.from_state.attributes.position != trigger.to_state.attributes.position
          }}'
      - and:
        - '{{ trigger.id == ''t_manual_tilt'' }}'
        - '{{ trigger.from_state.attributes.current_tilt_position not in invalid_states
          }}'
        - '{{ trigger.to_state.attributes.current_tilt_position not in invalid_states
          }}'
        - '{{ is_number(trigger.from_state.attributes.current_tilt_position | default(none))
          }}'
        - '{{ is_number(trigger.to_state.attributes.current_tilt_position | default(none))
          }}'
        - '{{ trigger.from_state.attributes.current_tilt_position != trigger.to_state.attributes.current_tilt_position
          }}'
    sequence:
    - variables:
        ts_now: '{{ as_timestamp(now()) | round(0) }}'
    - choose:
      - conditions:
        - '{{ trigger.id == ''t_manual_tilt'' }}'
        sequence:
        - variables:
            update_values:
              manual:
                a: 1
                t: '{{ ts_now }}'
              t: '{{ ts_now }}'
        - *id001
        - choose: []
          default: !input auto_manual_action
        - stop: Manual Tilt detected
      - conditions:
        - '{{ is_up_enabled }}'
        - or:
          - '{{ in_open_position }}'
          - '{{ position_comparisons.current_above_open or current_position == open_position
            }}'
          - '{{ (is_awning and current_position == 0) or (not is_awning and current_position
            == 100) }}'
        sequence:
        - variables:
            update_values:
              open:
                a: 1
                t: '{{ ts_now }}'
              close:
                a: 0
                t: '{{ helper_json.close.t }}'
              shading:
                a: 0
                t: '{{ helper_json.shading.t }}'
                p: 0
                q: 0
              vpart:
                a: 0
                t: '{{ helper_json.vpart.t }}'
              vfull:
                a: 0
                t: '{{ helper_json.vfull.t }}'
              manual:
                a: 1
                t: '{{ ts_now }}'
              t: '{{ ts_now }}'
        - *id001
        - choose: []
          default: !input auto_manual_action
        - stop: Manual Open detected
      - conditions:
        - '{{ is_ventilation_enabled }}'
        - '{{ in_ventilate_position }}'
        - '{{ not in_open_position }}'
        sequence:
        - variables:
            update_values:
              open:
                a: 0
                t: '{{ helper_json.open.t }}'
              close:
                a: 0
                t: '{{ helper_json.close.t }}'
              shading:
                a: 0
                t: '{{ helper_json.shading.t }}'
                p: 0
                q: 0
              vpart:
                a: 1
                t: '{{ ts_now }}'
              vfull:
                a: 0
                t: '{{ helper_json.vfull.t }}'
              manual:
                a: 1
                t: '{{ ts_now }}'
              t: '{{ ts_now }}'
        - *id001
        - choose: []
          default: !input auto_manual_action
        - stop: Manual Ventilation detected
      - conditions:
        - '{{ is_shading_enabled }}'
        - '{{ in_shading_position }}'
        sequence:
        - variables:
            update_values:
              open:
                a: 0
                t: '{{ helper_json.open.t }}'
              close:
                a: 0
                t: '{{ helper_json.close.t }}'
              shading:
                a: 1
                t: '{{ ts_now }}'
                p: 0
                q: 0
              vpart:
                a: 0
                t: '{{ helper_json.vpart.t }}'
              vfull:
                a: 0
                t: '{{ helper_json.vfull.t }}'
              manual:
                a: 1
                t: '{{ ts_now }}'
              t: '{{ ts_now }}'
        - *id001
        - choose: []
          default: !input auto_manual_action
        - stop: Manual Shading detected
      - conditions:
        - '{{ is_down_enabled }}'
        - '{{ not in_shading_position }}'
        - or:
          - '{{ in_close_position }}'
          - '{{ position_comparisons.current_below_close or current_position == close_position
            }}'
          - '{{ (is_awning and current_position == 100) or (not is_awning and current_position
            == 0) }}'
        sequence:
        - variables:
            update_values:
              open:
                a: 0
                t: '{{ helper_json.open.t }}'
              close:
                a: 1
                t: '{{ ts_now }}'
              shading:
                a: 0
                t: '{{ helper_json.shading.t }}'
                p: 0
                q: 0
              vpart:
                a: 0
                t: '{{ helper_json.vpart.t }}'
              vfull:
                a: 0
                t: '{{ helper_json.vfull.t }}'
              manual:
                a: 1
                t: '{{ ts_now }}'
              t: '{{ ts_now }}'
        - *id001
        - choose: []
          default: !input auto_manual_action
        - stop: Manual Close detected
      - conditions:
        - not:
          - '{{ is_up_enabled and in_open_position }}'
          - '{{ is_down_enabled and in_close_position }}'
          - '{{ is_ventilation_enabled and in_ventilate_position }}'
          - '{{ is_shading_enabled and in_shading_position }}'
        sequence:
        - variables:
            update_values:
              open:
                a: 0
                t: '{{ helper_json.open.t }}'
              close:
                a: 0
                t: '{{ helper_json.close.t }}'
              shading:
                a: 0
                t: '{{ helper_json.shading.t }}'
                p: 0
                q: 0
              vpart:
                a: 0
                t: '{{ helper_json.vpart.t }}'
              vfull:
                a: 0
                t: '{{ helper_json.vfull.t }}'
              manual:
                a: 1
                t: '{{ ts_now }}'
              t: '{{ ts_now }}'
        - *id001
        - choose: []
          default: !input auto_manual_action
        - stop: Manual position (unknown) detected
  - alias: Reset manual detection
    conditions:
    - '{{ trigger.id is defined }}'
    - '{{ not is_reset_disabled }}'
    - '{{ trigger.id in [''t_reset_fixedtime'', ''t_reset_timeout''] }}'
    sequence:
    - variables:
        ts_now: '{{ as_timestamp(now()) | round(0) }}'
        update_values:
          manual:
            a: 0
            t: '{{ ts_now }}'
          t: '{{ ts_now }}'
    - *id001
    - choose: []
      default: !input auto_override_reset_action
    - stop: Reset Manual Override executed
  - alias: Return to background position after force disable
    conditions:
    - '{{ trigger.id is defined }}'
    - '{{ trigger.id.startswith(''t_force_disabled'') }}'
    - '{{ is_auto_recover_enabled }}'
    - '{{ is_status_helper_enabled }}'
    - '{{ not is_cover_movement_blocked.any }}'
    sequence:
    - delay:
        seconds: '{{ range(drive_delay_fix|int(0), drive_delay_fix|int(0) + drive_delay_random|int(0)
          +1) | random }}'
    - variables:
        background_is_open: '{{ helper_status.open }}'
        background_is_closed: '{{ helper_status.closed }}'
        background_is_shaded: '{{ helper_status.shaded }}'
        background_is_vpart: '{{ helper_status.vpart }}'
        background_is_vfull: '{{ helper_status.vfull }}'
    - choose:
      - conditions:
        - '{{ background_is_closed }}'
        - '{{ not background_is_vpart }}'
        - '{{ not background_is_vfull }}'
        sequence:
        - choose: []
          default: !input auto_down_action_before
        - variables:
            target_position: !input close_position
            target_tilt_position: '{{ close_tilt_position | int }}'
        - *id002
        - *id003
        - choose: []
          default: !input auto_down_action
        - stop: Return to Close executed
      - conditions:
        - '{{ background_is_shaded }}'
        - or:
          - '{{ resident_sensor == [] }}'
          - '{{ states(resident_sensor) in [''false'', ''off''] }}'
          - and:
            - '{{ resident_flags.allow_shading }}'
            - '{{ states(resident_sensor) in [''true'', ''on''] }}'
        sequence:
        - choose: []
          default: !input auto_shading_start_action_before
        - variables:
            target_position: !input shading_position
            target_tilt_position: '{{ shading_tilt_position | int }}'
        - *id002
        - *id003
        - choose: []
          default: !input auto_shading_start_action
        - stop: Return to Shading executed
      - conditions:
        - '{{ background_is_vpart }}'
        - or:
          - '{{ resident_sensor == [] }}'
          - '{{ states(resident_sensor) in [''false'', ''off''] }}'
          - and:
            - '{{ resident_flags.allow_ventilation }}'
            - '{{ states(resident_sensor) in [''true'', ''on''] }}'
        sequence:
        - choose: []
          default: !input auto_ventilate_action_before
        - variables:
            target_position: !input ventilate_position
            target_tilt_position: !input ventilate_tilt_position
        - *id002
        - *id003
        - choose: []
          default: !input auto_ventilate_action
        - stop: Return to Ventilation executed
      - conditions:
        - '{{ background_is_open }}'
        - or:
          - '{{ resident_sensor == [] }}'
          - '{{ states(resident_sensor) in [''false'', ''off''] }}'
          - and:
            - '{{ resident_flags.allow_opening }}'
            - '{{ states(resident_sensor) in [''true'', ''on''] }}'
        sequence:
        - choose: []
          default: !input auto_up_action_before
        - variables:
            target_position: !input open_position
            target_tilt_position: '{{ open_tilt_position | int }}'
        - *id002
        - *id003
        - choose: []
          default: !input auto_up_action
        - stop: Return to Open executed
      default: []
  - alias: Reset shading status that is no longer required - but still saved
    conditions:
    - '{{ trigger.id is defined }}'
    - '{{ trigger.id == ''t_shading_reset'' }}'
    - '{{ is_status_helper_enabled }}'
    - or:
      - '{{ helper_status.shaded }}'
      - '{{ helper_status.shading_start_pending }}'
      - '{{ helper_status.shading_end_pending }}'
    sequence:
    - delay:
        seconds: '{{ (range(0, 60) | random | int) }}'
    - variables:
        update_values:
          shading:
            a: 0
            t: '{{ helper_json.shading.t }}'
            p: 0
            q: 0
          t: '{{ as_timestamp(now()) | round(0) }}'
    - *id001
    - stop: Midnight Reset executed
  default:
  - if:
    - '{{ check_config }}'
    then:
    - repeat:
        for_each:
        - condition: '{{ state_attr(blind, ''current_position'') is none }}'
          message: Cover entity is missing the required 'current_position' attribute
        - condition: '{{ state_attr(default_sun_sensor, ''elevation'') is none }}'
          message: Sun sensor is missing the required 'elevation' attribute
        - condition: '{{ state_attr(default_sun_sensor, ''azimuth'') is none }}'
          message: Sun sensor is missing the required 'azimuth' attribute
        - condition: '{{ resident_sensor != [] and states(resident_sensor) not in
            [''false'', ''off'', ''true'', ''on''] }}'
          message: 'resident_sensor has invalid state. Must be: on, off, true, or
            false'
        - condition: '{{ is_brightness_enabled and (default_brightness_sensor == []
            or not is_number(states(default_brightness_sensor))) }}'
          message: Brightness sensor not configured or has non-numeric state
        - condition: "{{\n  is_sun_elevation_enabled and\n  sun_elevation_mode ==
            'dynamic' and\n  sun_elevation_up_sensor == []\n}}"
          message: Sun Elevation Mode is 'dynamic' but Sun Elevation Up Sensor is
            not configured. Configure sensor or change mode to 'fixed'
        - condition: "{{\n  is_sun_elevation_enabled and\n  sun_elevation_mode ==
            'dynamic' and\n  sun_elevation_down_sensor == []\n}}"
          message: Sun Elevation Mode is 'dynamic' but Sun Elevation Down Sensor is
            not configured. Configure sensor or change mode to 'fixed'
        - condition: "{{\n  is_sun_elevation_enabled and\n  sun_elevation_mode ==
            'dynamic' and\n  sun_elevation_up_sensor != [] and\n  not is_number(states(sun_elevation_up_sensor))\n}}"
          message: Sun Elevation Up Sensor has non-numeric state. Sensor must provide
            numeric values in dynamic mode
        - condition: "{{\n  is_sun_elevation_enabled and\n  sun_elevation_mode ==
            'dynamic' and\n  sun_elevation_down_sensor != [] and\n  not is_number(states(sun_elevation_down_sensor))\n}}"
          message: Sun Elevation Down Sensor has non-numeric state. Sensor must provide
            numeric values in dynamic mode
        - condition: "{{\n  is_sun_elevation_enabled and\n  sun_elevation_mode ==
            'hybrid' and\n  sun_elevation_up_sensor == []\n}}"
          message: Sun Elevation Mode is 'hybrid' but Sun Elevation Up Sensor is not
            configured. Configure sensor or change mode to 'fixed'
        - condition: "{{\n  is_sun_elevation_enabled and\n  sun_elevation_mode ==
            'hybrid' and\n  sun_elevation_down_sensor == []\n}}"
          message: Sun Elevation Mode is 'hybrid' but Sun Elevation Down Sensor is
            not configured. Configure sensor or change mode to 'fixed'
        - condition: "{{\n  is_sun_elevation_enabled and\n  sun_elevation_mode ==
            'hybrid' and\n  sun_elevation_up_sensor != [] and\n  not is_number(states(sun_elevation_up_sensor))\n}}"
          message: Sun Elevation Up Sensor has non-numeric state. Sensor must provide
            numeric values in hybrid mode
        - condition: "{{\n  is_sun_elevation_enabled and\n  sun_elevation_mode ==
            'hybrid' and\n  sun_elevation_down_sensor != [] and\n  not is_number(states(sun_elevation_down_sensor))\n}}"
          message: Sun Elevation Down Sensor has non-numeric state. Sensor must provide
            numeric values in hybrid mode
        - condition: '{{ ''cover_helper_enabled'' in cover_status_options and state_attr(cover_status_helper,
            ''max'') < 254 }}'
          message: Cover status helper must have minimum length of 254 characters
        - condition: '{{ (is_shading_enabled or is_ventilation_enabled) and not is_status_helper_enabled
            }}'
          message: Shading and ventilation features require a cover status helper.
            Please configure one
        - condition: '{{ is_cover_tilt_enabled and state_attr(blind, ''current_tilt_position'')
            is none }}'
          message: Tilt position control enabled but cover does not support 'current_tilt_position'
            attribute. Disable tilt features or check cover integration
        - condition: "{{\n  workday_sensor_today not in [[], none] and\n  states(workday_sensor_today)
            not in ['on', 'off', 'unknown', 'unavailable']\n}}"
          message: 'Workday sensor today has invalid state. Expected: on or off'
        - condition: "{{\n  workday_sensor_tomorrow not in [[], none] and\n  states(workday_sensor_tomorrow)
            not in ['on', 'off', 'unknown', 'unavailable']\n}}"
          message: 'Workday sensor tomorrow has invalid state. Expected: on or off'
        - condition: "{{\n  auto_up_force not in [[], none] and\n  states(auto_up_force)
            not in ['on', 'off', 'true', 'false', 'unknown', 'unavailable']\n}}"
          message: 'Force Open entity has invalid state. Must be binary: on/off'
        - condition: "{{\n  auto_down_force not in [[], none] and\n  states(auto_down_force)
            not in ['on', 'off', 'true', 'false', 'unknown', 'unavailable']\n}}"
          message: 'Force Close entity has invalid state. Must be binary: on/off'
        - condition: "{{\n  auto_ventilate_force not in [[], none] and\n  states(auto_ventilate_force)
            not in ['on', 'off', 'true', 'false', 'unknown', 'unavailable']\n}}"
          message: 'Force Ventilate entity has invalid state. Must be binary: on/off'
        - condition: "{{\n  auto_shading_start_force not in [[], none] and\n  states(auto_shading_start_force)
            not in ['on', 'off', 'true', 'false', 'unknown', 'unavailable']\n}}"
          message: 'Force Shading entity has invalid state. Must be binary: on/off'
        - condition: "{{\n  [\n    auto_up_force not in [[], none] and states(auto_up_force)
            in ['on', 'true'],\n    auto_down_force not in [[], none] and states(auto_down_force)
            in ['on', 'true'],\n    auto_ventilate_force not in [[], none] and states(auto_ventilate_force)
            in ['on', 'true'],\n    auto_shading_start_force not in [[], none] and
            states(auto_shading_start_force) in ['on', 'true']\n  ].count(true) >
            1\n}}"
          message: 'CRITICAL: Multiple force entities are active. Only one force entity
            can be active at a time. Deactivate conflicting force entities'
        - condition: "{{\n  default_sun_sensor not in [[], none] and\n  states(default_sun_sensor)
            in ['unknown', 'unavailable']\n}}"
          message: Sun sensor is unavailable. Check sun integration is enabled
        - condition: '{{ forecast_source.use_weather and states(shading_forecast_sensor)
            in [''unknown'', ''unavailable''] }}'
          message: Weather entity is unavailable. Check weather integration
        - condition: '{{ forecast_source.use_sensor and states(shading_forecast_temp_sensor)
            in [''unknown'', ''unavailable''] }}'
          message: Forecast temperature sensor is unavailable
        - condition: '{{ forecast_source.use_sensor and not is_number(states(shading_forecast_temp_sensor))
            }}'
          message: Forecast temperature sensor is non-numeric
        - condition: "{{\n  sun_elevation_up_sensor != [] and\n  states(sun_elevation_up_sensor)
            in ['unknown', 'unavailable']\n}}"
          message: Dynamic sun elevation up sensor is unavailable. Check sensor configuration
        - condition: "{{\n  sun_elevation_up_sensor != [] and\n  states(sun_elevation_up_sensor)
            not in invalid_states and\n  not is_number(states(sun_elevation_up_sensor))\n}}"
          message: Dynamic sun elevation up sensor has non-numeric state
        - condition: "{{\n  sun_elevation_up_sensor != [] and\n  is_number(states(sun_elevation_up_sensor))
            and\n  (states(sun_elevation_up_sensor) | float < -90 or states(sun_elevation_up_sensor)
            | float > 90)\n}}"
          message: Dynamic sun elevation up sensor value is outside valid range (-90°
            to 90°)
        - condition: "{{\n  sun_elevation_down_sensor != [] and\n  states(sun_elevation_down_sensor)
            in ['unknown', 'unavailable']\n}}"
          message: Dynamic sun elevation down sensor is unavailable. Check sensor
            configuration
        - condition: "{{\n  sun_elevation_down_sensor != [] and\n  states(sun_elevation_down_sensor)
            not in invalid_states and\n  not is_number(states(sun_elevation_down_sensor))\n}}"
          message: Dynamic sun elevation down sensor has non-numeric state
        - condition: "{{\n  sun_elevation_down_sensor != [] and\n  is_number(states(sun_elevation_down_sensor))
            and\n  (states(sun_elevation_down_sensor) | float < -90 or states(sun_elevation_down_sensor)
            | float > 90)\n}}"
          message: Dynamic sun elevation down sensor value is outside valid range
            (-90° to 90°)
        - condition: "{{\n  sun_elevation_up_sensor != [] and\n  sun_elevation_down_sensor
            != [] and\n  is_number(states(sun_elevation_up_sensor)) and\n  is_number(states(sun_elevation_down_sensor))
            and\n  (states(sun_elevation_up_sensor) | float) <= (states(sun_elevation_down_sensor)
            | float)\n}}"
          message: Dynamic sun elevation up sensor should be higher than down sensor
        - condition: "{{\n  sun_elevation_up_sensor != [] and\n  sun_elevation_down_sensor
            == [] and\n  sun_elevation_up <= sun_elevation_down\n}}"
          message: 'WARNING: Using dynamic sensor for UP but fixed value for DOWN.
            Thresholds may be inconsistent'
        - condition: "{{\n  sun_elevation_up_sensor == [] and\n  sun_elevation_down_sensor
            != [] and\n  sun_elevation_up <= sun_elevation_down\n}}"
          message: 'WARNING: Using dynamic sensor for DOWN but fixed value for UP.
            Thresholds may be inconsistent'
        - condition: "{{\n  is_calendar_enabled and\n  calendar_entity != [] and\n
            \ states(calendar_entity) in ['unknown', 'unavailable']\n}}"
          message: Calendar entity is unavailable. Check calendar integration
        - condition: "{{\n  is_calendar_enabled and\n  calendar_entity != [] and\n
            \ state_attr(calendar_entity, 'message') is none\n}}"
          message: Calendar entity does not have 'message' attribute. Ensure it's
            a valid calendar entity
        - condition: "{{\n  position_source == 'position_attr' and\n  state_attr(blind,
            'position') is none\n}}"
          message: Position source set to 'position' attribute but cover doesn't have
            this attribute
        - condition: "{{\n  position_source == 'current_position_attr' and\n  state_attr(blind,
            'current_position') is none\n}}"
          message: Position source set to 'current_position' attribute but cover doesn't
            have this attribute (try 'position' or custom sensor)
        - condition: "{{\n  position_source == 'custom_sensor' and\n  (custom_position_sensor
            == [] or states(custom_position_sensor) in invalid_states)\n}}"
          message: Custom position sensor selected but not configured or unavailable
        - condition: "{{\n  position_source == 'custom_sensor' and\n  custom_position_sensor
            != [] and\n  not is_number(states(custom_position_sensor))\n}}"
          message: Custom position sensor has non-numeric value
        sequence:
        - if:
          - '{{ repeat.item.condition }}'
          then:
          - action: system_log.write
            data:
              level: '{{ check_config_debuglevel }}'
              message: 'CCA Config issue: {{ repeat.item.message }} - {{ this.entity_id
                }}'
              logger: blueprints.hvorragend.cca
  - stop: No operational branch matched (Opening, Closing, Shading, etc. conditions
      not met for current trigger)
